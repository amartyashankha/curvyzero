# Compatibility Ledger

Last updated: 2026-05-19

This ledger is for temporary facades, private imports, and hidden fallbacks in paths touched by this refactor. Broad backwards compatibility is not a goal. Anything kept here needs a protected caller, an expiry/deletion rule, and a test.

| Item | Status | Protected Caller | Why Kept For Now | Expiry / Delete When | Current Test Coverage |
| --- | --- | --- | --- | --- | --- |
| Trainer reward constants re-exported from `lightzero_curvyzero_stacked_debug_visual_survival_train.py` | Kept as facade | Existing tests/scripts importing `train_mod.REWARD_VARIANT_*` | Current trainer module is still the launch facade; constants now come from `reward_contracts.py`, not local duplicate definitions. | Delete only after launch/config callers import shared constants directly. | `tests/test_curvytron_live_checkpoint_eval_plumbing.py::test_stock_source_state_mixture_config_instantiates_registered_env_and_steps_scalar_action`; `tests/test_reward_contracts.py` |
| Trainer private reward helper wrappers: `_normalize_reward_variant_for_env`, `_reward_policy_for_variant`, `_lightzero_target_config_for_reward` | Kept as delegating facade | Existing private-helper tests/callers and trainer command construction | Keeps current trainer call surface stable while removing duplicate reward/support logic. | Remove or narrow after launch/config callers import public reward/config modules directly. | `tests/test_reward_contracts.py`; focused trainer plumbing tests |
| Trainer config/builder wrappers: `_build_visual_survival_configs`, `_target_config_patches`, `_set_save_ckpt_after_iter`, `_set_load_ckpt_before_run`, `_set_or_add_path`, `_get_path`, `_to_plain`, `_extract_surface`, `_env_variant_spec`, render/backend/seat validators, opponent relation helpers | Kept as delegating facade | Existing trainer tests/scripts import the trainer as the entrypoint module | Public `lightzero_config_builder.py` owns the implementations; the trainer stays a Modal launch facade while callers migrate. | Delete or shrink after tests/scripts import public modules directly and trainer tests cover only orchestration. | `tests/test_lightzero_config_builder.py`; focused trainer/eval plumbing tests |
| Broad public `build_visual_survival_configs(**kwargs)` | Kept as typed-path facade | Trainer/eval facades and existing tests still pass wide keyword sets | The primary API is now grouped `VisualSurvivalConfigSpec -> build_visual_survival_config(...) -> VisualSurvivalConfigResult`; the broad function constructs the spec, rejects unknown kwargs, and returns the historical dict shape. | Delete after trainer/eval/tests construct `VisualSurvivalConfigSpec` directly or if the broad signature has no protected caller. | `tests/test_lightzero_config_builder.py::test_visual_survival_config_spec_matches_keyword_builder`; `tests/test_lightzero_config_builder.py::test_visual_survival_config_spec_matches_keyword_builder_signature`; `tests/test_lightzero_config_builder.py::test_visual_survival_config_spec_rejects_unknown_broad_kwargs` |
| Legacy full `row["train_kwargs"]` in launch manifests | Kept as compatibility/override payload | `build_curvytron_survivaldiag_manifest.py`, `build_curvytron_next_batch_manifest.py`, `build_curvytron_opponent_mixture_manifest.py`, submitter, and manifest audit/analysis scripts | Non-migrated builders still emit explicit non-default row settings; grouped submit can normalize compact/minimal rows but still spawns Modal with flat kwargs. `tonight18` is no longer protected as a full-payload emitter. | Delete only after compact manifest schema covers initial checkpoint, opponent source, assignment refresh, reward/noise, background eval/GIF, and non-default scale overrides for the remaining builders. | `tests/test_curvytron_survivaldiag_submitter.py`; `tests/test_curvytron_tonight18_manifest.py`; `tests/test_curvytron_shared_contracts.py` |
| Frozen opponent snapshot fallback in trainer | Inventory only | Frozen checkpoint opponent loading | Identified by doc audit as a hidden fallback candidate; not touched in reward cut. | Decide keep/delete during opponent assignment/checkpoint metadata extraction. | Not assessed in reward cut |

## Resolved / Do Not Reintroduce

| Item | Status | Protected Caller | Why It Changed | Guardrail | Current Test Coverage |
| --- | --- | --- | --- | --- | --- |
| Eval import of `_target_config_patches` from trainer | Removed | `lightzero_curvytron_visual_survival_eval.py` model-shape reconstruction, including checkpoint-inferred support branch | Eval now imports public `target_config_patches` from `curvyzero.training.lightzero_config_builder`. | Keep removed; do not reintroduce trainer-private import. | `tests/test_lightzero_config_builder.py`; focused eval/trainer plumbing tests |
| Eval import of `_build_visual_survival_configs` from trainer | Removed | `lightzero_curvytron_visual_survival_eval.py` policy/env construction | Eval now imports public `build_visual_survival_configs` from `curvyzero.training.lightzero_config_builder`. | Keep removed; do not reintroduce trainer-private import. | `tests/test_lightzero_config_builder.py`; focused eval/trainer plumbing tests |
