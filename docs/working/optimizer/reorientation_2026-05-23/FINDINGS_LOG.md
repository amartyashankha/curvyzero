# Optimizer Findings Log

Date: 2026-06-07

Purpose: append discoveries that changed an implementation decision, test gate,
or planning priority.

| ID | Date | Source | Finding | Decision |
| --- | --- | --- | --- | --- |
| F-OPT132-FIXED-SOA-SLOT-CANDIDATE-DIGESTDEFER-LOCAL | 2026-06-07 | Local implementation and smoke `opt132-local-fixed-soa-slot-digestdefer-smoke-20260607-r3` | Fixed-SoA slot-candidate replay is locally wired and composes with owner-ref digest deferral in the speed-row entrypoint. The r3 smoke is `profile_no_death` wiring proof only, not speed evidence. It passed with owner-search threaded proxy, direct transition replay `12` logical transitions through `3` transports, fixed SoA requested/used true, `12` fixed-SoA slot writes, columnar append false with columnar slot count `0`, fixed-SoA entry/step/learner-ready/table-entry/table-concat counts `0`, fallback `0/none`, learner slot-candidate path true, selected fixed-SoA records `6`, action-only search payload bytes `0`, owner-ref digest deferral true, and learner-side digest time `0.0s`. | Preserve both patches as default-off support/ablation gates. This does not justify an H100 speed claim or make fixed-SoA the main lane again; H100 use must be labeled ablation/support or paired with broader owner-buffer/root/search/learner-publication movement. |
| F-OPT132-OWNER-REF-DIGEST-DEFER-LOCAL | 2026-06-07 | Owner-search digest-deferral patch plus service tests | Owner-ref digest deferral skips the learner-side model hash only for same-process copied model-state publication and requires search refresh to load/hash the provided state dict. The service fails closed if the model state dict is missing or if the returned digest source is not `search_worker_after_load`. It is not equivalent to the fully safe shared-model token path and should not be described as a general correctness shortcut. | Keep default-off and use only as an explicit ablation/support field. Do not promote it as the final publication proof for copied state; the durable correctness story remains full digest comparison or true shared-model/token semantics. |
| F-OPT132-DIRECT-ROOT-BUILD-REQUEST-LOCAL-R3 | 2026-06-07 | Aquinas/Locke/Euler/Hilbert sidecar audits plus local implementation, local smoke `opt132-local-owner-rootbuildrequest-threaded-directtransition-smoke-20260607-r3`, and H100 row `opt132-h100-rootbuildrequest-columnar-directtable-b1024a1-normal-unroll2-m724-w180-r1-20260607` | The true root-view/root-build boundary is larger than the resident-root/stub proof: parent action assembly and telemetry previously still depended on a parent-built `CompactRootBatchV1`. Local code now adds `CompactRootBuildRequestV1`, `compact_root_build_request_v1_from_batch()`, owner-side `build_compact_root_batch_v1_from_request()`, `CompactDirectRootStoreV1.publish_root_build_request()`, `CompactOwnerSearchSlabProxyV1.run_action_step_from_root_build_request()`, lazy direct-proxy routing, and `CompactOwnerSearchDirectStepperV1(direct_root_build_request=True)`. Focused tests prove both a fake request hook and the real `CompactLazyThreadedOwnerSearchSlabProxyV1` path complete when the legacy parent builder symbol is monkeypatched to raise, using request sidecars for parent action validation/assembly. A regression fix reasserts the outer owner replay handle/digest after inner search metadata merge, and root-build-request action metadata now reports owner pid/bytes/counts for the speed-row guard. Full owner-search service suite passes (`45 passed`), speed-row smoke suite passes (`87 passed`), and local r3 passed with publish/resolve/owner-build `15/15/15`, parent builder used/calls `false/0`, request observation bytes `0`, resident-root/stub/action-only gates preserved, parent rows `0/0`, search payload bytes `0`, direct replay `3/12/3`, action mismatches `0`, and maintenance/policy lag `0/0`. H100 kept proof closed at scale (`904/904/904`, parent builder `false/0`, root build `0.0s`, request observation bytes `0`, parent rows/search payload bytes `0/0`, direct replay `181/724/181`, normal-death proof true) but speed regressed to `11327.75 env/s`, `65.4477s`. | Root-build-request is support, not a standalone speed lane. It removed parent root-build work, but parent wait, replay append, learner train, worker search, observation, and slab wall regressed. Do not repeat unchanged; move to a larger owner-resident buffer/search/learner-publication surface. |
| F-OPT132-RESIDENT-HOST-OBS-STUB-LOCAL | 2026-06-07 | Lovelace/Poincare sidecar audit plus local direct-root owner-search patch | The resident-root proof still left parent `build_compact_root_batch_v1` alive. The new default-off direct-root owner-search gate `--owner-search-resident-root-host-observation-stub` requires slab bypass plus resident-root-view and builds only a zero-stride shape-only host observation stub for resident-device roots. Local smoke `opt132-local-owner-resident-root-hoststub-threaded-directtransition-smoke-20260607-r1` passes with stub requested/stubbed true, kind `zero_stride_shape_only_v1`, materialized bytes `0`, logical bytes `262144` last step / `3932160` total, resident-root-view required/proved true, H2D/D2H `0.0/0.0`, host fallback false, parent rows `0/0`, search payload bytes `0`, and final owner maintenance closed. | Keep this as a fail-closed support gate only. It removes parent host-observation materialization in the resident proof path, but it does not remove parent root-batch construction or justify an H100 row alone. Next implementation must be true root-view/build-request ownership or a combined owner-buffer/root/search/parent-wait/learner-publication/mechanics surface. |
| F-OPT132-ONE-SIM-DEFERRED-REPLAY-LOCAL | 2026-06-07 | Default-off compact Torch service patch plus Peirce audit | The fastest row's remaining non-replay floor includes one-simulation compact Torch search/action service time. Local code now has a default-off path that keeps action selection in `run_action_step()` but defers recurrent replay-payload materialization to replay-payload flush when `num_simulations == 1` and root noise is zero. It stamps action-time model identity, compares flush-time identity before materialization, rejects model refresh while deferred handles are pending, and surfaces action-phase request/used/pending/identity fields through slab/profile telemetry. Unit proof shows selected actions and replay payloads match the eager path, recurrent inference moves from action phase to flush, and identity drift fails closed. Validation: pycompile, ruff, and `tests/test_compact_torch_search_service.py` (`42 passed`). | This is the first concrete next-surface code after the ceiling review, but it is not speed evidence and not H100-ready. Do not wire/launch remotely until owner-search proves deferred handles flush before refresh, final pending deferred count is zero, model-refresh-crossed count is zero, and action-feedback/replay/terminal proof remains closed. |
| F-OPT132-WHOLE-OWNER-BUFFER-CEILING-REVIEW | 2026-06-07 | `artifacts/local/curvytron_compact_coach_speed_row_results/opt132-whole-owner-buffer-ceiling-review-20260607/comparison.json` | The whole-owner-buffer replay ceiling is now a generated comparison artifact for existing H100 rows, not a manual timing read. The rank is: columnar r2 observed `15852.67 env/s`, projected `24903.25 env/s` (`1.9625x`, `0.558s` short of 2x); direct-table r3 projected `24138.94 env/s` (`1.9023x`, `1.500s` short); fixed-SoA locality r2 projected `14986.22 env/s` (`1.1810x`, `20.258s` short). The fastest columnar row already has action-only owner result transport active: owner materializes replay, parent reconstruction false, parent slab commits false, search payload / visit policy / root value bytes all `0`, replay handle present, and inner two-phase deferred device replay true. | Stop treating this as a variance/longer-run issue or a replay-only implementation question. Replay/sample removal is close but short under conservative wall accounting, and the action-only boundary is already spent. The next code target must attack owner-resident root/mechanics/search dispatch, parent wait, learner-publication/update overlap, or another measured non-replay surface while preserving the action-only/replay/terminal proof gates. |
| F-OPT132-WHOLE-OWNER-BUFFER-CEILING-LOCAL | 2026-06-07 | Local projection/proof patch plus manual read of known H100 timings | Added `compact_whole_owner_buffer_replay_ceiling_*` fields to compute a conservative, projection-only ceiling from measured owner replay append, owner train sample, parent wait, worker search, learner update, wall, and env-step counts. Evidence validation now rejects projection fields that drift toward production speed claims or production speed currency. Manual application to columnar/direct-table r2 projects about `24903 env/s` (`1.96x` OPT-104) if replay append plus owner train sample vanish under the parent-wait-bounded model, still about `0.56s` short of the `2x` wall target for `741376` env steps. Fixed-SoA locality r2 projects only about `14986 env/s` (`1.18x`) because non-replay surfaces dominate. | Replay/sample removal is close but not sufficient by itself under conservative wall accounting. Before another production replay rewrite, use the ceiling fields on the fastest exact stack; if it remains below `2x`, the rewrite must also attack search, parent wait, learner update/publication, or another measured non-replay surface. |
| F-OPT132-FIXED-SOA-LOCALITY-H100-R2 | 2026-06-07 | `opt132-h100-fixed-soa-locality-g8-rowlevel-selectedgroups-b1024a1-normal-unroll2-m724-w180-r2-20260607` | The fixed-SoA locality probe passed proof and answered the causal question, but it was not a speed path. Locality group size `8` marked `fixed_soa_locality_sample_semantic_drift=true`, collapsed selected fixed-SoA records to `62` for a real `512` row learner batch, kept tensor-native `fixed_soa_direct_gather_v1` from `fixed_soa_columns_v1`, and kept fallback/table concat/object counters clean. Whole-loop speed regressed to `10428.59 env/s`, `71.0907s` wall. Owner train sample improved versus r8 (`43.14s -> 32.03s`), but replay append `17.24s`, worker search `16.68s`, parent wait `21.62s`, learner update `6.52s`, and owner train wall `40.30s` still dominate. | Selected-record fragmentation is real, but fixed-SoA gather/locality is not the main 2x/10x lane. Stop main-lane gather tweaks; only pursue exact flat/global row layout if it is part of a broader fixed owner-buffer implementation that also attacks append/search/parent-wait/learner surfaces. |
| F-OPT132-FIXED-SOA-PROJECTION-LOCAL | 2026-06-07 | Local patch after locality r2 read | Fixed-SoA report/evidence projection was too weak: the valid H100 row required nested learner telemetry to read locality, selected-record count, and fixed-SoA object counters. Local code now projects fixed-SoA requested/used/object counters, record count, selected-record count, table-row count, and `compact_rollout_slab_sample_gate_fixed_soa_locality_*` fields through summary, evidence, local Modal launcher reports, and Modal producer reports. | Documentation/proof cleanup only, no speed claim. Future rows should be readable without nested jq for the fixed-SoA causal fields. Validation passed: pycompile, ruff, and focused pytest (`3 passed`). |
| F-OPT132-FIXED-SOA-H100-R5R9 | 2026-06-07 | Fixed-SoA H100 rows r5-r9 | Fixed SoA/direct-gather is now proof-clean, but not fast enough. r5 fixed append-time successor indexing enough to reach `7466.24 env/s`; r6/r7/r8 stayed proof-clean and climbed to `8931.53`, `10662.84`, then `11616.45 env/s`. r8 is the best exact row: sample aggregate `43.14s`, last gather `0.2247s`, terminal samples `4`, selected records about `373` for `512` sampled rows. r9 copy-trim regressed to `8491.45` and was reverted. | Stop treating this as variance or a longer-run question. Exact fixed-SoA still scatters a learner sample across hundreds of record groups, so the next decision is row locality/flat global layout, not another micro-copy edit. |
| F-OPT132-FIXED-SOA-LOCALITY-PROBE | 2026-06-07 | Local patch after R8/R9 read | Added an explicitly labeled fixed-SoA locality sampler controlled by `compact_replay_fixed_soa_locality_sample_group_size` / `--owner-search-fixed-soa-locality-sample-group-size`. Values `>1` choose groups proportional to row count and sample rows in local chunks. This preserves row marginals but changes batch correlation and duplicates, so metadata marks `fixed_soa_locality_sample_semantic_drift=true`. Local pycompile, ruff, and focused pytest passed. | Use one H100 row as a causal probe for selected-record fragmentation. If speed jumps with selected-record count down, build the exact flat/global row layout. If it does not, stop spending the main lane on fixed-SoA gather tweaks. This row is diagnostic, not promotion-comparable exact replay sampling. |
| F-OPT132-FIXED-SOA-LOCAL | 2026-06-07 | Local implementation after fixed-buffer sidecar audits | Fixed SoA/direct-gather replay now exists as a default-off local path. `_CompactReplayRingV1` owns `append_fixed_soa_columnar_records()` and fixed SoA learner-batch-only unroll-2 sampling; owner-search direct transition-batch replay can request it with `--owner-search-fixed-soa-replay`; Modal launcher/producer and tensor-native validation accept `fixed_soa_direct_gather_v1` from `fixed_soa_columns_v1` only with zero object/table leakage. Local parity compares every learner tensor against maintained replay for mixed live + partial-terminal rows. Focused ruff and pytest passed. | Historical local implementation evidence, superseded by the r5-r9 H100 read above. Preserve the proof fields; do not treat the local implementation itself as speed progress. |
| F-OPT132-COLUMNAR-TELEMETRY-LOCAL | 2026-06-07 | Local patch after columnar/lazy H100 read | The prior direct-replay proof was too narrow: it could prove `ring_entry_objects=0` while hiding remaining columnar append records/views and cache/table maintenance inside one `ring_append_sec` bucket. Local code now exposes `_CompactReplayRingV1.columnar_append_telemetry_snapshot()` and speed-row fields for columnar record count, entry-view and step-view object counts, plus prepare/register/append-store/retain/evict/candidate/cache-refresh/cache-rebuild/total timings. Focused ruff and pytest passed. | Use this as support measurement for fixed SoA/direct-gather work. This is not H100 speed evidence and does not justify another remote row by itself. |
| F-OPT132-COLUMNAR-H100-R2 | 2026-06-07 | `opt132-h100-columnar-append-direct-table-b1024a1-normal-unroll2-m724-w180-r2-20260607` | Corrected no-preset columnar append is the current fastest single H100 input, but it is only a small increment. The row ran `15852.67 env/s`, `46.7666s` wall over `741376` env steps, about `1.25x` OPT-104 and about `1.01x` faster than direct-table r3. Proof stayed closed: owner-search threaded proxy, slab bypass, direct transition replay, columnar append used, `724` slots, `ring_entry_objects=0`, direct prebuilt sample path used, direct group objects `0`, maintained tensor-native table source, fallback `0`. Remaining buckets still include worker replay append `14.219s`, direct append `14.169s`, ring append `11.523s`, index-row build `1.875s`, worker search `13.295s`, learner train `9.778s`, and parent wait `17.655s`. | Preserve columnar append, but do not promote it or pretend zero ring-entry objects solved the architecture. Next implementation targets must move a broader owner boundary: root/search dispatch, parent wait, replay/table ownership as part of a fixed owner buffer, learner publication/update, or mechanics/observation ownership. |
| F-OPT132-LAZYSELECTED-H100-R2 | 2026-06-07 | `opt132-h100-lazy-selected-table-columnar-b1024a1-normal-unroll2-m724-w180-r2-20260607` | Lazy selected direct table build is a negative speed result. It passed proof but slowed to `11179.26 env/s`, `66.3171s` wall. It cut append from `14.169s` to `10.224s` and ring append from `11.523s` to `7.101s`, but moved the cost into owner train/sample: learner train rose from `9.778s` to `48.696s`, owner train sample from `2.777s` to `41.640s`, and direct group objects rose from `0` to `352`. | Keep lazy selected default-off. Reopen only if a future design also removes sample-side grouped chunks/direct group objects and proves the learner/sample bucket does not blow up. |
| F-OPT132-PRESET-OWNER-SEARCH-STOMP | 2026-06-07 | `opt132-h100-columnar-append-direct-table-b1024a1-normal-unroll2-m724-w180-r1-20260607` plus launcher audit | The accepted-fast-path preset overwrote parsed owner-search/direct-replay flags and did so again in `_launch_remote()`. The r1 columnar launch therefore used `compact_torch_search_service`, not `owner_search_threaded_proxy`, and did not exercise owner-search direct transition replay. It was invalid for the columnar/direct-table speed question. | Launcher validation now rejects accepted preset combined with owner-search override flags. Future owner-search/direct-replay rows should omit the preset and spell the full accepted shape manually. |
| F-OPT132-DIRECTTABLE-H100-R3 | 2026-06-07 | `opt132-h100-direct-table-builder-b1024a1-normal-unroll2-m724-w180-r3-20260607` | Direct record-table building is H100-useful, not just local proof polish. The row ran `15681.59 env/s`, `47.2768s` wall over `741376` env steps, the fastest maintained-table input before columnar append and about `1.24x` OPT-104. Nested owner learner telemetry proves `direct_record_table_v1`, direct build used `true`, table build `0.637s` for `9770` rows, worker replay append `13.879s`, ring append `9.848s`, table concat `0.006s`, gather `0.0016s`, and remaining `ring_entry_objects=724`. Top-level projection fields were blank in that artifact and have since been patched to fall back to nested telemetry. | Preserve direct table-builder as part of the current candidate stack, but do not promote or call `2x`; exact repeat is required for a speed claim. Columnar append now proves ring-entry removal alone is too small, so the next implementation target must combine replay/table storage with broader owner root/search/parent-wait/learner-publication movement. |
| F-OPT132-RINGBATCH-H100-R3R4 | 2026-06-07 | `opt132-h100-owner-search-transitionbatch-ringbatch-slab-bypass-vectorrng-refresh4-fusedtensor-b1024a1-normal-unroll2-m724-w180-r3/r4-20260606` | Ring-batched owner replay append/cache refresh produced a repeat-positive H100 speed movement. r3 ran `14394.77 env/s`, `51.503s`, replay append `20.204s`; r4 ran `14160.27 env/s`, `52.356s`, replay append `18.636s`. Average r3/r4 was `14277.52 env/s`, `51.930s`, replay append `19.420s`, compared with transition-batch-only r2 `13618.91 env/s`, `54.437s`, replay append `31.926s`. Proof stayed clean: transition transport `724 -> 181`, pending replay `0`, action mismatch `0`, tensor-native reused/missing `719/0`. | Accept ring-batched replay append/cache refresh as support, but do not call the goal closed. Direct-prebuilt sample and replay/table paths are support proof; the active path now needs broader owner root/search/parent-wait/learner-publication movement. |
| F-OPT132AU-H100-PERF-EVENT-UNAVAILABLE | 2026-06-03 | `opt132au-h100-generic-perfstat-image-nosampler-b1024a1-normal-unroll2-m1084-w270-r1-20260603` | After installing `linux-perf` in the compact speed-row image, the AU H100 diagnostic spawned `fc-01KT7YWEDCC60CMGTFY6388QBE` and found `/usr/bin/perf`, but `perf stat` failed before the producer ran. The stderr says `sys_perf_event_open()` returned `19 (No such device)` for `task-clock`; return code `255`; parsed event count `0`; no producer result/report was present. | Diagnostic failure only, no speed claim. External perf counters are unavailable in this Modal container, so do not retry perf unchanged. OPT-132AV took the in-process follow-up path by splitting AS's user-CPU-backed sample-gate/learner-batch-build work by child phase. |
| F-OPT132AU-LINUX-PERF-IMAGE-LOCAL | 2026-06-03 | Local patch after OPT-132AT perf-missing H100 failure | OPT-132AT proved the compact speed-row remote image did not have a `perf` binary, so the external CPU-counter diagnostic could not start. | Added `linux-perf` to `speed_row_image` only in `src/curvyzero/infra/modal/compact_coach_speed_row.py`; the shared LightZero image remains unchanged. Validation passed: ruff; focused Modal perf tests (`2` tests, `2` warnings). Next evidence is OPT-132AU exact generic no-sampler `1084/270`: counters, or a structured perf-denied/unsupported-event failure. |
| F-OPT132AT-H100-PERF-MISSING | 2026-06-03 | `opt132at-h100-generic-perfstat-nosampler-b1024a1-normal-unroll2-m1084-w270-r1-20260603` | The first external CPU perf-stat H100 attempt spawned `fc-01KT7YGKDM2Y35NCG6C204TYMY` but failed before a speed row. The remote bundle and regenerated top-level report both say `perf stat diagnostic requested but perf was not found`, `compact_profile_cpu_perf_stat_available=false`, and return code `127`. No producer result or speed row was present. | Diagnostic failure only, no speed claim. This proves the image lacked the perf binary; it does not answer AS's user-CPU question. Add `linux-perf` to the speed-row image and retry once (OPT-132AU). |
| F-OPT132AT-CPU-PERF-STAT-LOCAL | 2026-06-03 | Local diagnostic patch after OPT-132AS H100 read | AS showed the remaining exact generic no-sampler sample-gate/build swing is mostly user CPU, not system CPU, page faults, or context switches. The next narrow split is lower-level CPU evidence: more retired instructions, worse IPC/cache/TLB/branch behavior, CPU migrations, or lower effective CPU frequency. | Added diagnostic-only `--compact-profile-cpu-perf-stat-diagnostics`. The Modal producer wraps the speed-row script in `perf stat -x,`, requests task-clock/cycles/ref-cycles/instructions/branch/cache/LLC/dTLB/page-fault/context-switch/CPU-migration events, writes perf stdout/stderr artifacts, parses available counters into `compact_profile_cpu_perf_stat_*` fields, projects them through row/report/comparison surfaces, and returns structured failure if perf is unavailable or fails before a row. Validation passed: ruff; targeted perf-stat tests (`4` tests, `2` warnings); broader smoke/compare slice (`20` tests, `2` warnings). Local only; next evidence is an exact H100 row or structured perf-denied failure. |
| F-OPT132AS-H100-GENERIC-RUSAGE-R1R2 | 2026-06-03 | `opt132as-h100-generic-rusage-trace-nosampler-r1-r2-comparison-20260603` | Two exact generic `1084/270` H100 rows ran with AS resource-usage trace records, GPU utilization sampling disabled, runtime-step diagnostics enabled, and CUDA-sync probes off. Identity matched exactly; accepted-fast-path, runtime-step, and CUDA-sync violations were `[]`; each row had `135` trace records. Wall was `211.257s / 182.483s` (`14.616%` of median), speed `5254.34 / 6082.84`, sample gate `126.934s / 107.934s`, learner-batch build `70.377s / 58.933s`, and builder group-loop `69.602s / 58.206s`. The sample-gate r1-minus-r2 wall delta was `18.999s`; process user/system CPU deltas were `16.17s / 0.49s`, and thread user/system CPU deltas were `15.83s / 0.58s`. Learner-build process user/system CPU deltas were `9.31s / 0.55s`. Page-fault and context-switch deltas were zero. | Diagnostic only. AS rules out system CPU, page faults, and context switches as the main explanation of the AR CPU-time-backed swing. The remaining drift is mostly user CPU on identical exposed trace state. The next diagnostic or fix must explain why identical trace state consumes different user CPU: CPU frequency, cache/memory locality, runtime/library behavior, or hidden Python/Torch work. Do not start another learner-batch builder speed patch yet. |
| F-OPT132AS-RUSAGE-TRACE-LOCAL | 2026-06-03 | Local diagnostic patch after OPT-132AR H100 read and sidecar audit | AR showed the exact generic no-sampler sample-gate/build swing is CPU-time-backed rather than mostly off-CPU wait, but AR only had aggregate process/thread CPU time. The next needed split was whether the extra CPU-backed time was user CPU, system CPU, page faults, context switches, or still lower-level frequency/cache/runtime behavior. | Added diagnostic-only `resource.getrusage` before/after/delta fields to each runtime trace record for both the full sample gate and the learner-batch-build slice: process/thread user CPU, system CPU, minor/major page faults, and voluntary/involuntary context switches. Thread fields are zero on platforms without `RUSAGE_THREAD`. Validation passed: ruff and the focused AP/AQ/AR slice (`17` tests, `2` warnings). Superseded by the AS H100 r1/r2 read; no speed claim. |
| F-OPT132AR-H100-GENERIC-CPUTIME-R1R2 | 2026-06-03 | `opt132ar-h100-generic-cputime-trace-nosampler-r1-r2-comparison-20260603` | Two exact generic `1084/270` H100 rows ran with AR CPU-time trace records, GPU utilization sampling disabled, runtime-step diagnostics enabled, and CUDA-sync probes off. Identity matched exactly; accepted-fast-path, runtime-step, and CUDA-sync violations were `[]`; each row had `135` trace records. Wall was `189.915s / 217.364s` (`13.479%` of median), speed `5844.80 / 5106.72`, sample gate `111.765s / 130.614s`, learner-batch build `60.527s / 71.857s`, and builder group-loop `59.815s / 71.015s`. Sample-gate wall delta was `18.848s`; process/thread CPU-time deltas were `17.33s / 17.18s` (`91.9% / 91.1%`). Learner-build wall delta was `11.331s`; process/thread CPU deltas were `10.44s / 10.39s` (`92.1% / 91.7%`). Replay/sample trace state and CUDA allocator state still matched exactly. | Diagnostic only. AR rules out a mostly off-CPU wait/scheduling explanation for the sample-gate/build swing; the remaining drift is CPU-time-backed. The next diagnostic or fix must explain why identical trace state consumes different CPU time: lower-level CPU work, CPU frequency, cache/memory locality, or runtime/library behavior. Do not start another learner-batch builder speed patch yet. |
| F-OPT132AR-CPUTIME-TRACE-LOCAL | 2026-06-03 | Local diagnostic patch after OPT-132AQ H100 read | AQ ruled out actual Python GC collection counters as the primary explanation for exact generic no-sampler sample-gate timing spread. The next narrow split is whether slow wall time is backed by process/current-thread CPU time or by host scheduling/wait/lower-level runtime effects. | Added diagnostic-only process and current-thread CPU-time before/after/delta fields to each runtime trace record for both the full sample gate and the learner-batch-build slice. Validation passed: ruff and the focused AP/AQ slice (`17` tests, `2` warnings). Superseded by the AR H100 r1/r2 read; no speed claim. |
| F-OPT132AQ-H100-GENERIC-GC-STATS-R1R2 | 2026-06-03 | `opt132aq-h100-generic-gcstats-trace-nosampler-r1-r2-comparison-20260603` | Two exact generic `1084/270` H100 rows ran with AQ GC-stats trace records, GPU utilization sampling disabled, runtime-step diagnostics enabled, and CUDA-sync probes off. Identity matched exactly; accepted-fast-path, runtime-step, and CUDA-sync violations were `[]`; each row had `135` trace records. Wall was `218.320s / 163.265s` (`28.856%` of median), speed `5084.35 / 6798.87`, sample gate `131.651s / 95.558s`, learner-batch build `72.267s / 51.322s`, and builder group-loop `71.432s / 50.716s`. Actual GC collection totals were nearly identical: gen0/gen1/gen2 collections `7232/659/28` versus `7234/655/28`. `100` calls had identical GC collection deltas and still carried `22.6695s` of sample-gate delta. | Diagnostic only. AQ rules out actual Python GC collection counts as the primary cause of the wall/sample-gate swing. The next diagnostic is process/thread CPU-time attribution via OPT-132AR; do not start another learner-batch builder speed patch. |
| F-OPT132AQ-GC-STATS-TRACE-LOCAL | 2026-06-03 | Local diagnostic patch after OPT-132AP H100 read | AP ruled out exposed replay/sample-state drift and simple CUDA allocator/memory drift across exact generic no-sampler H100 repeats, but Python `gc.get_count()` and RSS fields varied. The count snapshots reset often and cannot identify whether actual collections happened. | Added diagnostic-only `gc.get_stats()` fields to each runtime trace record: per-generation collections, collected, and uncollectable before/after/delta. Validation passed: ruff and the focused AP slice (`17` tests, `2` warnings). Superseded by the AQ H100 r1/r2 read and AR local CPU-time trace; no speed claim. |
| F-OPT132AP-H100-GENERIC-ALLOCATOR-TRACE-R1R2R3 | 2026-06-03 | `opt132ap-h100-generic-allocator-trace-nosampler-r1-r2-r3-comparison-20260603` | Three exact generic `1084/270` H100 rows ran with AP allocator/runtime trace records, GPU utilization sampling disabled, runtime-step diagnostics enabled, and CUDA-sync probes off. Identity matched exactly; accepted-fast-path, runtime-step, and CUDA-sync violations were `[]`; each row had `135` trace records. Wall was `219.446s / 205.371s / 153.752s` (`31.99%` of median), speed `5058.27 / 5404.94 / 7219.51`, sample gate `130.199s / 120.714s / 86.639s`, learner-batch build `70.732s / 65.983s / 46.881s`, and builder group-loop `69.882s / 65.174s / 46.350s`. Exposed replay/sample trace identity fields matched across all calls. Every CUDA memory allocated/reserved/peak counter, retry/OOM counter, and learner-batch-build CUDA memory delta also matched exactly across r1/r2/r3. Python GC count and process RSS fields varied. | Diagnostic only. AP rules out simple CUDA allocator state as the cause of the r1/r2/r3 timing spread; the remaining swing is allocator-exact and replay/sample-state exact. The next diagnostic is actual Python GC collection counters (`gc.get_stats()`) and then lower-level host/runtime scheduling if collections do not explain the swing. |
| F-OPT132AP-SAMPLE-GATE-ALLOCATOR-RUNTIME-TRACE-LOCAL | 2026-06-03 | Local diagnostic patch after OPT-132AO sidecar audits | AO proved exposed replay/sample trace state can be identical at all `135` sample-gate calls while sample-gate timing still swings. Sidecar audits recommended the smallest next probe below that state: per-call CUDA allocator counters, learner-batch-build memory deltas, Python GC/RSS runtime state, and the already-measured deeper builder child timings. | Added diagnostic-only AP trace fields to `compact_rollout_slab_sample_gate_call_trace_records`: CUDA memory allocated/reserved/peak counters, allocator retry/OOM counters, learner-batch-build boundary deltas, Python GC generation counts, process max-RSS raw state, sample-gate CUDA-sync timing, and terminal/unroll/order/finalize/metadata builder child timings. Validation passed: ruff and `17` focused tests. Superseded by the AP H100 read and AQ local GC-stats trace; no speed claim. |
| F-OPT132AO-H100-GENERIC-TRACE-NOSAMPLER-R1R2 | 2026-06-03 | `opt132ao-h100-generic-trace-nosampler-r1-r2-comparison-20260603` | Two exact generic `1084/270` H100 rows ran with AO replay-state trace records, GPU utilization sampling disabled, runtime-step diagnostics enabled, and CUDA-sync probes off. Identity matched exactly; accepted-fast-path, runtime-step, and CUDA-sync violations were `[]`; both rows had `135` trace records. R1/R2 wall was `221.118s / 254.261s` (`13.944%`), runtime-step sum `221.111s / 254.253s`, sample gate `132.338s / 163.614s`, learner-batch build `72.215s / 91.981s`, and builder group-loop `71.350s / 91.030s`. The exposed trace state mismatch count was `0/135` across call index, measured iteration, measured third, sample seed/rows/checksums, stored/raw/eligible/excluded/evicted replay counts, and replay capacity. | Diagnostic only. AO rules out replay/sample-state drift at the exposed trace level; the same call state is timing differently. Wall-swing attribution from fastest r1 to slowest r2 assigns `99.999%` of wall delta to runtime-step sum, `95.07%` to active sample-gate step sum, `94.37%` to sample gate, `59.64%` to learner-batch build, and `59.38%` to builder group-loop. The next target is allocator/cache/runtime or lower-level builder timing on identical replay/sample state, not another speed patch. |
| F-OPT132AO-SAMPLE-GATE-CALL-TRACE-LOCAL | 2026-06-03 | Local diagnostic patch after OPT-132AN | AN showed the remaining instability is broad active sample-gate call slowdown/elevation, but the report still did not expose per-call replay/sample state needed to distinguish replay-ring growth, eligible-row shape, eviction shape, or sample-order/checksum drift from allocator/runtime effects. | Added bounded `compact_rollout_slab_sample_gate_call_trace_records` with call index, iteration/measured_iteration, measured third, sample seed/row/checksum fields, stored/eligible/excluded/evicted replay counts, replay capacity, and per-call sample/builder/residual timing. Source profile, speed-row summary/compact payload, local/remote Modal report allowlists, and focused tests preserve the trace. Validation passed: ruff; source-profile runtime-step test; speed-row projection test; Modal launcher/report and comparator slice. Local only; superseded by F-OPT132AO-H100-GENERIC-TRACE-NOSAMPLER-R1R2. |
| F-OPT132AN-H100-GENERIC-ACTIVECADENCE-NOSAMPLER-R1R2 | 2026-06-03 | `opt132an-h100-generic-activecadence-nosampler-r1-r2-comparison-20260603` | Two exact generic `1084/270` H100 rows ran with AM active-call cadence fields, GPU utilization sampling disabled, runtime-step diagnostics enabled, and CUDA-sync probes off. Identity matched exactly; accepted-fast-path, runtime-step, and CUDA-sync violations were `[]`. R1/R2 wall was `206.746s / 166.095s` (`21.806%` by median), runtime-step sum `206.741s / 166.090s`, sample gate `125.169s / 93.384s`, learner-batch build `68.851s / 49.643s`, and builder group-loop `68.101s / 48.923s`. Active sample-gate p50 rose early/mid/late in both rows (`0.425/0.981/1.342s` and `0.327/0.699/1.025s`), and r1 was elevated across all thirds versus r2; late p95 was `1.714s` versus `1.392s`. | Diagnostic only. AN closes the simpler AM question: the slowdown is broad across active sample-gate calls and per-row elevation, not only a few late spikes. Wall-swing attribution from fastest r2 to slowest r1 assigns `99.999%` of wall delta to runtime-step sum, `79.36%` to sample-gate active-step sum, `78.19%` to sample gate, `47.25%` to learner-batch build, and `47.18%` to builder group-loop. The next target is per-sample-gate replay-state trace, not another builder speed patch. |
| F-OPT132AM-ACTIVE-SAMPLE-GATE-CADENCE-LOCAL | 2026-06-03 | Local diagnostic patch after OPT-132AL sidecar audits | OPT-132AL already showed exact no-sampler generic rows had identical sample-gate active counts (`135/135/135`), identical replay/sample checksums and retained snapshot counts, deferred sample/learner disabled, and the same late sample-call indices slowing down. The missing low-overhead projection was chronological active-call distribution inside each measured third; sums alone could not separate broad late-call slowdown from a few late spikes. | Added chronological active sample-gate phase distributions by measured third for sample gate, sample-gate residual, builder group-loop, learner gate, observation, and primary residual. Bucket sums now include sample-gate residual; top-slowest runtime-step records include sample-gate residual and builder sync; per-call sample-gate stats project slowest iteration and measured iteration. Validation passed: ruff; focused source-profile/smoke/comparator tests; full comparator suite; Modal launcher/report slice. Local only; superseded by the OPT-132AN H100 read and OPT-132AO trace tooling. |
| F-OPT132AL-H100-GENERIC-RUNTIMECADENCE-NOSAMPLER-R1R2R3 | 2026-06-03 | `opt132al-h100-generic-runtimecadence-nosampler-r1-r2-r3-comparison-20260603` | Three exact generic `1084/270` H100 rows ran with GPU utilization sampling disabled, runtime-step cadence diagnostics enabled, and CUDA-sync probes off. Identity matched exactly; accepted-fast-path and runtime-step violations were `[]`; CUDA-sync diagnostics and GPU sampling were false. R1/R2/R3 wall was `168.661s / 178.629s / 207.419s` (`21.70%` of median), runtime-step sum `168.656s / 178.624s / 207.413s` (`21.70%`), sample gate `97.710s / 102.538s / 121.130s` (`22.84%`), learner-batch build `54.231s / 56.034s / 65.956s` (`20.92%`), and builder group-loop `53.566s / 55.349s / 65.163s`. Active/inactive sample-gate measured-step counts were stable at `135/949`, but the late measured third grew from `73.166s` to `96.470s`, late sample-gate grew from `48.364s` to `64.010s`, and late builder group-loop grew from `23.702s` to `30.956s`. | Diagnostic only. Disabling GPU utilization sampling does not solve same-work timing instability. Wall-swing attribution from fastest r1 to slowest r3 assigns `99.995%` of wall delta to runtime-step sum, `71.76%` to sample-gate active-step sum, `60.42%` to sample gate, `60.13%` to the late measured third, `40.37%` to late sample-gate, `30.25%` to learner-batch build, and `29.92%` to builder group-loop. The next target is late measured-window sample-gate cadence slowdown, not another builder speed patch or sampler rerun. |
| F-OPT132AK-WALL-SWING-CADENCE-LOCAL | 2026-06-03 | Local diagnostic patch and `opt132ak-aj-wall-swing-attribution-refresh-20260603` | Runtime-step totals already reconciled with wall, but the comparator did not summarize which fields explained slowest-vs-fastest exact repeats, and source-profile runtime records did not expose active/inactive sample-gate steps, early/mid/late cadence, or bounded top-slowest measured records. | Added `exact_repeat_stability.wall_swing_attribution` to `scripts/compare_compact_coach_speed_rows.py`, added runtime-step cadence summaries and top-slowest records to `source_state_hybrid_observation_profile.py`, and projected those fields through local/remote speed-row reports. Local validation passed. The AJ refresh found r2-r3 wall delta `22.989s`, runtime-step sum delta `22.987s` (`99.995%`), primary accounted `22.093s` (`96.10%`), sample gate `14.022s` (`61.00%`), learner-batch build `8.247s` (`35.87%`), and builder group-loop `8.099s` (`35.23%`). Superseded by the OPT-132AL H100 no-sampler cadence read. |
| F-OPT132AJ-H100-GENERIC-RUNTIMEONLY-R1R2R3 | 2026-06-03 | `opt132aj-h100-generic-runtimeonly-gpuutil-r1-r2-r3-comparison-20260603` | Three exact generic `1084/270` H100 rows ran with GPU utilization sampling and runtime-step diagnostics enabled, but CUDA-sync probes off. Identity matched exactly; accepted-fast-path, runtime-step, and unroll2 violations were `[]`; CUDA-sync diagnostics were false with sync counts `0`. R1/R2/R3 wall was `219.055s / 221.938s / 198.950s` (`10.49%`), runtime-step sum `219.050s / 221.932s / 198.945s` (`10.49%`), sample gate `117.675s / 132.786s / 118.763s` (`12.72%`), learner-batch build `63.964s / 73.104s / 64.857s` (`14.09%`), and builder group-loop `63.102s / 72.243s / 64.144s` (`14.25%`). GPU mean util was `15.23% / 16.98% / 18.30%`, max power `226.81W / 245.75W / 243.50W`, max memory used `38867 MiB`. | Diagnostic only. Removing CUDA-sync probes narrows the generic packet relative to AH, but exact-repeat timing stability still fails and runtime-step sum reconciles with wall. The remaining issue is measured-iteration runtime swing across observation and sample-gate/builder work, not AF specialization and not CUDA-sync probes alone. |
| F-OPT132AI-RUNTIME-STEP-FLAG-LOCAL | 2026-06-03 | Local diagnostic patch after OPT-132AH | AH showed the generic builder can drift as hard as AF under exact work, but runtime-step envelope fields were tied to `--compact-profile-cuda-sync-timing-diagnostics`. That prevented a clean overhead check separating runtime-step bookkeeping from sync probes. | Added `--compact-profile-runtime-step-timing-diagnostics` through source profile config, local smoke CLI, Modal launcher/entrypoint, result-bundle config fields, and fail-closed local/remote validators. CUDA-sync diagnostics still imply runtime-step stats for backward compatibility. Validation passed: ruff; runtime-only source-profile test; Modal launcher/validator/report tests; comparator tests. Superseded by F-OPT132AJ-H100-GENERIC-RUNTIMEONLY-R1R2R3. |
| F-OPT132AH-H100-GENERIC-GPUUTIL-R1R2R3 | 2026-06-03 | `opt132ah-h100-generic-gpuutil-r1-r2-r3-comparison-20260603` | Three exact `1084/270` H100 rows ran with GPU utilization sampling enabled and the unroll-2 specialized builder disabled. Identity matched exactly and accepted-fast-path/CUDA-sync violations were `[]`; generic proof fields showed requested=false, used=false, eligible_count/call_count `0`, fallback_count `0`, impl=`none`, path=`generic`. R1/R2/R3 wall was `205.584s / 243.805s / 289.172s` (`34.28%`), speed `5399.33 / 4552.88 / 3838.61`, sample gate `123.444s / 151.007s / 196.761s` (`48.55%`), learner-batch build `75.790s / 92.226s / 123.781s` (`52.04%`), and builder group-loop `74.642s / 91.378s / 122.843s` (`52.75%`). GPU sampler worked with no errors: samples `445 / 539 / 583`, mean util `17.97% / 15.25% / 13.96%`, max util `48% / 44% / 43%`, max memory used `38867 MiB` for all rows, and max power `264.47W / 222.48W / 240.14W`. | Diagnostic only. The same-work runtime swing is not AF-specific; the generic builder drifts too, and slower rows do not correspond to higher mean GPU utilization or a clear power-throttle pattern. Do not add another learner-batch builder speed patch until diagnostic/runtime overhead or another repeatable environment bucket is bounded. |
| F-OPT132AG-H100-GPUUTIL-R4R5R6 | 2026-06-03 | `opt132ag-h100-af-gpuutil-r4-r5-r6-comparison-20260603` and `opt132af-ag-h100-af-gpuutil-r1-r6-comparison-20260603` | Three exact AF-path `1084/270` H100 rows ran with GPU utilization sampling enabled. Identity matched exactly and accepted-fast-path, CUDA-sync, and unroll2-specialized violations were `[]`. All rows proved requested=true, used=true, eligible/call_count `399`, fallback_count `0`, fallback_reason `none`, impl `unroll2_specialized_v1`, and path `unroll2_specialized`. R4/R5/R6 wall was `195.624s / 185.643s / 199.275s` (`6.97%`), sample gate `121.378s / 109.782s / 123.823s` (`11.57%`), learner-batch build `76.124s / 65.197s / 76.876s`, and builder group-loop `75.411s / 64.437s / 76.134s` (`15.51%`). GPU sampler worked with no errors: mean util `18.44% / 18.69% / 15.54%`, max power `257.90W / 260.27W / 227.38W`, max memory used `38867 MiB` for all rows. The combined AF+AG exact packet spans `185.643s -> 234.042s` wall (`23.55%` of median), sample gate `109.782s -> 146.505s` (`29.12%`), and learner-batch build `65.197s -> 90.698s` (`32.80%`). | Diagnostic only. AG breaks the simple monotonic AF slowdown pattern and does not support a simple power/thermal-throttle explanation, but measurement stability still fails. Superseded by F-OPT132AH-H100-GENERIC-GPUUTIL-R1R2R3, which shows the generic builder drifts too. |
| F-OPT132AG-GPU-COMPARATOR-LOCAL | 2026-06-03 | Local comparator patch after OPT-132AF r1/r2/r3 | The next AF slowdown diagnostic can enable the existing GPU utilization sampler, but the comparator previously did not surface sampler context. Without that, GPU utilization, memory, and power would have to be inspected manually from each report. | Added a per-row `gpu_utilization` block to `scripts/compare_compact_coach_speed_rows.py` and numeric timing/range fields for sample count, GPU utilization, memory utilization/used, and power draw. This does not alter identity matching. Validation passed: ruff for comparator/test and `tests/test_compact_coach_speed_row_compare.py` (`10 passed`). Refreshed AF r1/r2/r3 comparison at `opt132af-h100-unroll2-specialized-builder-diagnostic-r1-r2-r3-comparison-refresh-20260603` kept exact identity, disabled-sampler fields as zero/null, and `stable_speed_claim_allowed=false`. Superseded by F-OPT132AG-H100-GPUUTIL-R4R5R6. |
| F-OPT132AF-H100-UNROLL2-SPECIALIZED-R1R2R3 | 2026-06-03 | `opt132af-h100-unroll2-specialized-builder-diagnostic-r1-r2-r3-comparison-20260603` | Three exact `1084/270` H100 rows ran the guarded unroll-2 specialized builder. Identity matched exactly and accepted-fast-path, CUDA-sync, and unroll2-specialized violations were all `[]`. All rows proved requested=true, used=true, eligible_count/call_count `399`, fallback_count `0`, fallback_reason `none`, impl `unroll2_specialized_v1`, and path `unroll2_specialized`. Wall slowed monotonically `211.683s -> 225.020s -> 234.042s` (`9.94%`), speed `5243.77 -> 4932.96 -> 4742.80`, sample gate `128.433s -> 139.112s -> 146.505s` (`12.99%`), learner-batch build `78.608s -> 85.983s -> 90.698s` (`14.06%`), and builder group-loop `77.852s -> 85.168s -> 89.803s` (`14.03%`). | Diagnostic only. The H100 proof path is intact, but OPT-132AF is rejected as a stable speed claim under exact `1084/270`. Do not start another learner-batch builder speed patch until the monotonic same-work slowdown is explained or bounded. |
| F-OPT132AF-UNROLL2-SPECIALIZED-LOCAL | 2026-06-03 | Local guarded speed patch after OPT-132AE | AE showed builder group-loop cost is mostly named child work, with unroll fields the largest absolute child. A speed patch is only measurable if it preserves learner-batch semantics and emits fail-closed proof. | Added a default-off unroll-2 specialized builder for fused resident grouped learner batches only. It is guarded by unroll count, chain length, terminal-window metadata, and device replay rows; it projects requested/eligible/used/call/fallback/reason/impl/path proof fields through profile, speed-row, Modal, reducer, and comparator surfaces. Validation passed locally: ruff; source-profile parity; speed-row proof/modal tests; compact reducer/comparator suite; full speed-row smoke. Hardened local proof smoke `opt132af-local-unroll2-specialized-builder-hardened-smoke-20260603` completed `ok=true` with requested/used true, eligible/call_count `13`, fallback_count `0`, fallback_reason `none`, impl `unroll2_specialized_v1`, and path `unroll2_specialized`. Superseded by the H100 r1/r2 diagnostic; no speed claim yet. |
| F-OPT132AE-H100-BUILDER-GROUP-ACCOUNTED-R1R2 | 2026-06-03 | `opt132ae-h100-builder-loop-accounted-residual-diagnostic-r1-r2-comparison-20260603` | Two exact `1084/270` H100 rows read the formal builder group-loop accounted/residual fields. Identity matched exactly and accepted-fast-path/sync diagnostic violations were `[]`. Both rows were slow: wall `271.920s -> 284.561s`, speed `4082.14 -> 3900.80`, sample gate `185.217s -> 193.754s`, learner-batch build `114.786s -> 120.842s`, and builder group-loop `113.971s -> 119.853s`. The new split shows group-loop accounted child work `95.872s -> 101.300s` and residual only `18.099s -> 18.553s`. Unroll fields are the largest absolute child (`57.127s -> 61.190s`), terminal metadata next (`26.666s -> 27.764s`). | No speed claim. AE says the group-loop cost is mostly named child work, not residual. The next speed patch, if any, should be a default-off unroll-2 specialized builder guarded by exact learner-batch tensor/checksum/terminal-mask proof. |
| F-OPT132AE-BUILDER-GROUP-ACCOUNTED-LOCAL | 2026-06-03 | Local diagnostic patch after OPT-132AD r1/r2/r3 | AD proved the sample-gate builder group-loop was still the moving center, but the visible child accounting was computed manually and not projected as first-class fields. | Added diagnostic-only builder group-loop accounted/residual totals and per-call stats. Accounted is terminal metadata + terminal-window hint + unroll fields + write output. Source profile, speed-row summaries/compact payloads, Modal reports, and comparator fields project the totals and flat count/sum/min/max/p50/p95 fields. Validation passed: ruff; focused pytest `4 passed`; broader speed-row/report slice `63 passed`. Superseded by F-OPT132AE-H100-BUILDER-GROUP-ACCOUNTED-R1R2; no speed claim. |
| F-OPT132AD-H100-RUNTIME-PHASE-R1R2R3 | 2026-06-03 | `opt132ad-h100-runtime-phase-residual-diagnostic-r1-r2-r3-comparison-20260603` | Three exact `1084/270` H100 rows read the runtime phase/residual fields. Identity matched exactly and accepted-fast-path/sync diagnostic violations were `[]`, but timing still failed: wall `204.520s / 231.874s / 182.574s` (`24.11%`), sample gate `122.980s / 145.023s / 107.539s` (`30.48%`), learner-batch build `72.702s / 88.877s / 63.929s` (`34.32%`), sample-gate builder group-loop `71.909s / 88.019s / 63.218s` (`34.49%`), and unroll fields `36.310s / 44.992s / 31.871s` (`36.13%`). Manual child accounting gave group-loop accounted `60.586s / 74.177s / 53.093s` and residual `11.323s / 13.842s / 10.125s`. | No speed claim. AD r3 confirmed the branch does not cluster cleanly from r1/r2 alone. Formalize the group-loop accounted/residual split before speed code; superseded by OPT-132AE. |
| F-OPT132AD-H100-RUNTIME-PHASE-R1R2 | 2026-06-03 | `opt132ad-h100-runtime-phase-residual-diagnostic-r1-r2-comparison-20260603` | Two exact `1084/270` H100 rows read the runtime phase/residual fields. Identity matched exactly and accepted-fast-path/sync diagnostic violations were `[]`, but timing still failed: wall `204.520s -> 231.874s` (`12.54%`), sample gate `122.980s -> 145.023s` (`16.45%`), learner-batch build `72.702s -> 88.877s` (`20.02%`), sample-gate builder group-loop `71.909s -> 88.019s` (`20.15%`), and unroll fields `36.310s -> 44.992s` (`21.36%`). The new fields show smaller secondary movement in actor env runtime `10.628s -> 13.338s`, actor autoreset `21.910s -> 23.280s`, sample residual `25.939s -> 29.594s`, and builder sync `10.863s -> 12.768s`. | Superseded by the AD r1/r2/r3 comparison. No speed claim. |
| F-OPT132AD-RUNTIME-PHASE-RESIDUAL-ATTRIBUTION | 2026-06-03 | Local diagnostic patch after OPT-132AC r1c/r2/r3 | OPT-132AC proved the deep builder child timers project on H100 but left a broad slow branch: r1c was slower than the r2/r3 cluster across wall, actor, sample gate, builder group-loop, unroll fields, terminal metadata, and residual sample buckets. The existing slowest-step fields were not enough to tell whether the branch was actor/env drift, sample-gate residual, sync wait, or builder group-loop drift across measured steps. | Added diagnostic-only runtime-step distributions for actor env runtime, actor autoreset, sample-gate residual, sample-gate sync, sample-gate builder group-loop, and sample-gate builder sync, projected through source profile, speed-row summary/compact payload, local/remote Modal reports, and comparator timing fields. Validation passed: ruff; focused source-profile/smoke/comparator pytest; broader compact speed-row slice. Superseded by F-OPT132AD-H100-RUNTIME-PHASE-R1R2; no speed claim. |
| F-OPT132AC-H100-DEEP-BUILDER-R1C-R2-R3 | 2026-06-03 | `opt132ac-h100-builder-deep-child-diagnostic-r1c-r2-r3-comparison-20260603` | Three exact `1084/270` H100 rows read the deep builder child timers. Identity matched exactly and accepted-fast-path violations stayed `[]`, but timing failed: wall `235.855s / 174.009s / 172.247s` (`36.55%` spread), sample gate `147.161s / 106.443s / 109.114s` (`37.32%`), learner-batch build `87.449s / 64.220s / 67.871s` (`34.23%`), builder group-loop `86.514s / 63.583s / 67.224s` (`34.11%`), unroll fields `45.615s / 31.928s / 34.076s` (`40.16%`), terminal metadata `18.970s / 14.537s / 16.130s` (`27.48%`), and actor wall `38.377s / 29.370s / 27.278s` (`37.79%`). R2/R3 clustered while r1c was the slow branch. | No speed claim. The deeper timers project on H100, but they do not make the row stable. The next work is to explain or bound the r1c slow branch across actor/sample/builder group-loop before any builder speed patch. |
| F-OPT132AC-H100-DEEP-BUILDER-R1C-R2 | 2026-06-03 | `opt132ac-h100-builder-deep-child-diagnostic-r1c-r2-comparison-20260603` | Two exact `1084/270` H100 rows read the deep builder child timers. Identity matched exactly and accepted-fast-path/sync diagnostic violations were `[]`, but timing still failed: wall `235.855s -> 174.009s` (`30.18%` spread), sample gate `147.161s -> 106.443s` (`32.11%`), learner-batch build `87.449s -> 64.220s` (`30.63%`), builder group-loop `86.514s -> 63.583s` (`30.56%`), unroll fields `45.615s -> 31.928s` (`35.30%`), and terminal metadata `18.970s -> 14.537s` (`26.46%`). Deeper children show terminal final observation `11.121s -> 8.376s`, terminal tensor fallback `4.541s -> 3.501s`, unroll mask build `13.321s -> 9.635s`, terminal value `11.582s -> 7.427s`, and stack fields `10.919s -> 7.603s`. | Superseded by F-OPT132AC-H100-DEEP-BUILDER-R1C-R2-R3. No speed claim. |
| F-OPT132AC-MODAL-LAUNCH-FAILURE-ARTIFACT | 2026-06-03 | Local launcher patch after OPT-132AC r1/r1b empty-dir launch failures | The Modal launcher already wrote failure reports after a FunctionCall existed, but pre-FunctionCall `modal run --detach` rejection raised before `launch.json` or a modal report could be written. OPT-132AC r1/r1b therefore left empty output directories when Modal returned `RESOURCE_EXHAUSTED`. | Added a structured launch-failure path. Future pre-FunctionCall launch failures write `launch.json` and `compact_coach_speed_row_modal_report.json` with `ok=false`, `failure_stage=launch`, no FunctionCall id, stdout/stderr tails, accepted-fast-path diagnostic context, and `modal_launch_resource_exhausted` classification. Reachable remote spawn failures print structured `spawn_failed` JSON, and local/remote per-call prefix parity is tested. Focused ruff passed; focused launcher/projection pytest passed. This is measurement hygiene only, not H100 timing evidence. |
| F-OPT132AC-DEEP-BUILDER-GROUP-LOOP-ATTRIBUTION | 2026-06-03 | Local diagnostic patch after OPT-132AB r1/r2 | OPT-132AB put the H100 timing swing inside builder group-loop work, with unroll fields and terminal metadata as the largest visible children, but those fields were still too coarse to tell whether stack construction, terminal-value/mask work, final-observation handling, validation, or fallback tensor paths were moving. | Added diagnostic-only deep child timers and per-call stats for terminal metadata mask/tensor-fallback/validate/final-observation and unroll terminal-window-hint/identity/stack-fields/mask-build/terminal-value/mask-apply/action-stack. Source profile, speed-row summaries/compact payloads, Modal reports, and comparator fields project them. Focused ruff and pytest passed. The first two H100 launches failed before row creation with Modal `RESOURCE_EXHAUSTED`; superseded by F-OPT132AC-H100-DEEP-BUILDER-R1C-R2, where r1c/r2 completed and still failed timing stability. |
| F-OPT132AB-H100-BUILDER-CHILD-R1R2 | 2026-06-03 | `opt132ab-h100-builder-child-per-call-diagnostic-r1r2-comparison-20260603` | Two exact `1084/270` H100 rows read the builder-child per-call fields. Identity matched exactly and accepted-fast-path/sync diagnostic violations were `[]`, but timing failed hard: wall `271.062s -> 170.468s` (`45.57%` spread), sample gate `180.198s -> 102.544s` (`54.93%`), learner-batch build `105.658s -> 59.377s` (`56.09%`), builder group-loop `104.758s -> 58.756s` (`56.27%`), and unroll fields `50.816s -> 29.511s` (`53.05%`). Terminal metadata moved `23.072s -> 12.377s`; builder CUDA sync moved `9.988s -> 5.930s`. | No speed claim. The broad swing is inside builder group-loop work. Unroll-field construction is the largest visible child, with terminal metadata next; builder CUDA sync is visible but smaller. Do not patch builder speed code until this group-loop/unroll/terminal swing is isolated enough to measure. |
| F-OPT132AB-BUILDER-CHILD-PER-CALL-ATTRIBUTION | 2026-06-03 | Local diagnostic patch after OPT-132AA r1/r2 | OPT-132AA showed broad sample-gate per-call cost centered on learner-batch build, but build p50/p95 still hid which builder child was broad. The source already recorded child timers per sample-gate call, but reports/comparison did not expose per-call distributions for group-loop, terminal metadata, unroll fields, write output, order restore, finalize outputs, metadata build/sync, or builder CUDA sync. | Added diagnostic-only builder-child per-call stats through source profile, speed-row summary/compact payload, local/remote Modal report projection, and comparator timing fields. Focused ruff passed; focused comparator, smoke, and source-profile pytest passed. Superseded by the OPT-132AB H100 read above; next isolate group-loop/unroll/terminal swing before any builder speed patch. |
| F-OPT132AA-SAMPLE-GATE-PER-CALL-R1R2 | 2026-06-03 | `opt132aa-h100-sample-gate-per-call-diagnostic-r1r2-comparison-20260603` | Two exact `1084/270` H100 rows read the new sample-gate per-call fields. Identity matched exactly against each other and the OPT-132Z rows; accepted-fast-path violations and sync diagnostic violations were `[]`. AA r1/r2 wall moved `213.543s -> 224.408s` (`4.96%` spread), sample gate `128.039s -> 134.063s` (`4.60%`), and learner-batch build `76.980s -> 79.225s` (`2.88%`). Sample-gate per-call p50/p95/max stayed broadly high (`1.032/1.630/1.723s` -> `1.061/1.674/1.783s`); learner-batch-build per-call p50/p95/max stayed high and stable (`0.652/0.804/1.094s` -> `0.666/0.802/1.100s`). | No speed claim. The sample-gate problem is broad per-call cost centered on builder work, not a few pathological sample calls. Superseded by OPT-132AB: builder-child attribution was read on H100 and points to group-loop/unroll/terminal swing, still without a speed claim. |
| F-OPT132Z-SAMPLE-GATE-PER-CALL-DIAGNOSTIC | 2026-06-03 | Local diagnostic patch after OPT-132Z r1/r2 | OPT-132Z localized the instability to measured sample-gate cadence steps, but total sample-gate and learner-batch-build sums were still too coarse to say whether most sample calls slowed or only a few calls spiked. | Added diagnostic-only per-call sample-gate distribution stats for sample-gate total, candidate, RNG, and residual. Summary/compact payload, Modal reports, and comparison artifacts expose flattened count/sum/min/max/p50/p95 fields. Validation passed: ruff; focused comparator, smoke, and source-profile pytest. |
| F-OPT132Z-RUNTIME-STEP-R1R2 | 2026-06-03 | `opt132z-h100-runtime-step-envelope-diagnostic-r1r2-comparison-20260603` | Two exact `1084/270` H100 rows with runtime-step envelope fields completed. Identity matched exactly, accepted-fast-path violations and sync diagnostic violations were `[]`, runtime count was `1084`, and sync counts matched sample `12`, builder `11977`, learner `28`. Timing failed hard: wall `197.292s -> 273.865s`, runtime-step sum `197.288s -> 273.860s`, sample gate `118.072s -> 182.127s`, learner-batch build `68.491s -> 107.283s`. Runtime p50 barely moved (`0.063s -> 0.073s`), while p95/max jumped (`1.150s -> 1.784s`, `1.786s -> 2.717s`), and the slowest step was sample-gate dominated in both rows. | Do not patch speed code. The swing is inside measured loop iterations and concentrated on sample-gate cadence steps. Add/read per-call sample-gate child distribution stats before learner-batch builder optimization. |
| F-OPT132Y-RUNTIME-STEP-ENVELOPE | 2026-06-03 | Local diagnostic patch after OPT-132Y r1/r2/r3 broad slowdown | The CUDA-sync diagnostic explained only a small part of the r3 wall swing. Sidecar artifact and instrumentation audits both pointed to broad host/runtime variance and recommended a small opt-in measured-step envelope before speed work. | Added diagnostic-only `compact_profile_runtime_step_*` fields under the existing CUDA-sync diagnostic flag. The source profile records measured-step wall stats and slowest-step phase context; speed-row summary/compact payload, Modal reports, and the comparator project them. Validation passed: ruff; focused comparator, smoke, and source-profile pytest. Next H100 row should use these fields to decide whether the slowdown is uniform or spike-driven before learner-batch builder optimization. |
| F-OPT132Y-H100-CUDA-SYNC-DIAGNOSTIC-R1R2R3 | 2026-06-02 | `opt132y-h100-cuda-sync-timing-diagnostic-r1r2r3-comparison-20260602` | Three exact `1084/270` H100 rows with CUDA sync timing completed. Identity matched exactly, including sync flags and counts; accepted-fast-path violations and sync diagnostic violations were `[]`. R3 was a broad slow repeat: wall spread `30.99%`, sample gate `30.47%`, learner-batch build `28.01%`, actor wall `31.66%`, observation `62.14%`, observation-other `75.95%`, and builder cuda_sync `39.85%`. Builder sync wait ranged `5.520s -> 8.274s`, visible but too small to explain the `60.959s` wall range by itself. | Do not patch speed code yet. Treat OPT-132Y as a measurement-stability failure and isolate the broad same-work runtime slowdown before learner-batch builder optimization. |
| F-OPT132X-H100-LEARNER-BATCH-DIAGNOSTIC-R1R2R3 | 2026-06-02 | `opt132x-h100-learner-batch-timing-diagnostic-r1r2r3-comparison-20260602` | Three exact `1084/270` H100 rows with learner-batch sub-timers completed with exact identity and accepted-fast-path violations `[]`, but r3 broke timing stability. The three-row spread was wall `15.55%`, sample gate `19.49%`, learner-batch build `18.88%`. Learner-batch build ranged `46.919s -> 57.573s`; the largest internals remain builder group loop, unroll fields, and terminal metadata. Metadata sync/readback is tiny. | Do not patch speed code yet. Add diagnostic-only CUDA sync/event timing around sample gate, learner-batch builder children, and learner phases. If that stabilizes, target learner-batch builder group-loop work; if it does not, host/runtime variance remains the blocker. |
| F-OPT132W-LEARNER-BATCH-TIMING-DIAGNOSTIC | 2026-06-02 | Local patch after exact `1084/270` r1/r2 timing failure | The exact r1/r2 work fields matched, and sidecar/artifact analysis found no counter, group-size, terminal-path, replay-memory, seed, or checksum drift explaining the `12.449s` learner-batch-build swing. The missing evidence was inside the coarse learner-batch-build timer and across the 135 individual sample-gate calls. | Added resident grouped learner-batch builder sub-timers and bounded per-call learner-batch-build stats. This is measurement work, not speed evidence. Superseded by F-OPT132X: the first diagnostic row passed and exposed the fields; the active step is repeating exact `1084/270` under the same field set. |
| F-OPT132W-LATEST-FRAME-H100-STABILITY | 2026-06-02 | H100 bounded `724/180` bridge plus exact `1084/270` r1/r2 repeat | Latest-frame resident replay snapshots solved the immediate H100 long-window memory-fit problem. The `724/180` bridge passed with retained resident bytes `6243287040`; `1084/270` r1/r2 both passed with retained resident bytes `9346744320`, accepted-fast-path violations `[]`, host fallback `0`, render-state copy `0`, and exact work identity. Timing still failed the stability bar: `1084/270` wall spread `8.28%`, sample gate spread `14.04%`, learner-batch build spread `15.57%`. | Do not claim speedup. Treat memory fit as solved and timing stability as open. Next work is to isolate why sample-gate learner-batch-build timing moves by `12.449s` on exact `1084/270` work before making another speed patch. |
| F-OPT132-LATEST-FRAME-REPLAY-SNAPSHOT | 2026-06-02 | Local memory-stability patch after OPT-132-U OOM | The bounded diagnostic patch removed payload/history growth but `1084/270` still OOMed. A full resident replay snapshot clones the whole 4-frame stack every step; at B1024/A1 that is the right scale to explain step-scaled live GPU memory. | Added a bounded-diagnostic-only latest-frame resident replay snapshot mode. Accepted `180/45` rows still default to full-stack snapshots. Normal bounded steps retain one latest frame; reset steps rebuild frame history from the live stack; terminal final rows stay sparse. Local validation passed: ruff, source-profile tests `110 passed`, speed-row smoke plus compact-owned replay-store slice `48 passed`. Superseded by OPT-132W H100 rows: `1084/270` now fits; timing stability is the active blocker. |
| F-OPT132-BOUNDED-LONG-DIAGNOSTIC | 2026-06-02 | Bounded diagnostic patch after 724/180 instability and 1084/270 OOM | The correct next experiment is longer warmup and longer measured profiles, but the old harness kept step-scaled committed-row history and embedded the full source profile payload. That made `1084/270` and `1444/360` H100 diagnostics vulnerable to profiler memory growth. | Added bounded diagnostic mode: `CompactRolloutSlab` can count committed rows without retaining full history; source profile summary uses counters instead of copying `committed_index_rows`; long diagnostic speed rows can omit nested `source_profile_payload`; accepted stability windows enable `--compact-profile-bounded-diagnostics` automatically. Superseded by OPT-132-U: bounded `1084/270` still OOMed. Next action is memory attribution and cutting. |
| F-OPT132-LONG-WINDOW | 2026-06-02 | Launcher/comparator patch after OPT-132-G/H/I/J instability | The accepted-fast-path preset was hardcoded to `180` measured / `45` warmup in both `main()` and `_launch_remote()`, so longer profile requests would be silently overwritten. The comparator also assumed every accepted-fast-path row used the 180/45 denominator. Attempts with `720` measured steps failed because they did not satisfy the final learner-refresh proof; changing warmup did not fix it. | Added explicit step-window support: `accepted_180_45`, `stability_724_180`, and `stability_1444_360`. Long windows keep the fast-path flags, leave a measured post-refresh actor/search step, are labeled `long_window_stability_diagnostic`, relax only length-dependent proof fields, and cannot set `stable_speed_claim_allowed=true` against OPT-104. Superseded by K/L/Q/R/S: five exact `724/180` rows failed stability. Do not repeat unchanged. |
| F-OPT132-GHIJ-COMPARE | 2026-06-02 | `scripts/compare_compact_coach_speed_rows.py` and `opt132-ghij-runtime-comparison-20260602/comparison.json` | Row comparison is now machine-built instead of hand-written. The report marks G/H as `partial_legacy` because older summaries lack the new top-level identity fields. It marks I -> J as `exact`, with `stable_speed_claim_allowed=false`. Largest exact regressions are sample gate `+1.852s`, actor `+1.397s`, actor autoreset `+1.104s`, learner-batch build `+1.038s`, learner `+0.654s`, learner backward `+0.445s`, and sample RNG `+0.416s`. | Use the comparator for future repeat rows. Do not accept a speed claim without an exact same-work comparison and all exact rows beating OPT-104. Next implementation target stays actor/autoreset, sample build/RNG, or learner backward. |
| F-OPT132-J | 2026-06-02 | OPT-132-J H100 repeat | The corrected guard works, but the speed win is not stable. OPT-132-J passed accepted-fast-path validation and preserved the same fixed work shape, yet ran slow: `10057.98` env steps/sec, `18.3258s` wall. Its trajectory and sample-order checksums match OPT-132-I, which ran `13308.59` env steps/sec and `13.8497s` wall. The I -> J swing is wall `+4.476s`, actor `+1.397s`, sample `+1.852s`, learner `+0.654s`, while observation was slightly faster. | Treat the blocker as same-work runtime instability, not hidden flag/data drift. Do not open owner/GPU/hardware lanes. Compare G/H/I/J child timers and then target actor/autoreset, sample build/RNG, or learner backward. |
| F-OPT132-I | 2026-06-02 | OPT-132-I H100 artifact and accepted-preset guard audit | The latest accepted-preset H100 artifact ran fast: `13308.59` env steps/sec, `13.8497s` wall, beating OPT-104 by about `0.676s`. It was wrongly rejected by the launcher because the repeatability guard required `env_trajectory_ordered_checksum_total` to be positive even though signed checksums can be negative. The artifact has no accepted-preset violations under the corrected rule. | Patched the guard: required checksums must be present and nonzero; counters must be positive. Keep OPT-132-I as a useful fast row, not final closure. Run one more same-preset H100 repeat with no new feature flags. |
| F-OPT132-EVIDENCE | 2026-06-02 | OPT-132-G/H repeatability audit and local patch | The accepted-fast-path report surface was still too thin for repeatability. It exposed enough proof fields to reject flag drift, but not enough top-level seed/work-shape, actor trajectory, terminal/autoreset/death, sample-order, and sample/learner identity fields to quickly explain why OPT-132-G was fast and OPT-132-H was slow. | Patched hash-bound speed-row evidence to carry and validate repeatability fields already present in summary/report plumbing. Validation passed: focused ruff and focused pytest `30 passed, 2 warnings`. No speed claim. Next H100 row must compare these fields before any architecture change. |
| F-OPT132-REPEATABILITY | 2026-06-02 | OPT-132-G/H H100 rows | OPT-132-G beat OPT-104 once, but OPT-132-H did not repeat it. G ran at `13649.29` env steps/sec and `13.504s` wall; H ran at `11366.84` env steps/sec and `16.216s` wall. The wall swing is `+2.712s`. Resident stack shift is tiny (`0.012s` to `0.015s`), so the next target is not the ring-buffer stack. | Keep OPT-104 as the accepted baseline. Stay on the accepted direct compact trainer path, prove repeatability with top-level identity fields, then change the largest stable bucket: actor/env, observation-other, sample, or learner. |
| F-148 | 2026-06-02 | OPT-130 same-work H100 row | Direct compact Torch with `direct_core` initial inference on the accepted OPT-104 shape did not beat the baseline. Row `opt130-h100-direct-compacttorch-directcore-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `11038.67` env steps/sec and `16.698s` wall. It preserved normal death, terminal sample/target rows `167/167`, truncations `0`, and resident host fallback `0`. Buckets: actor `7.363s`, sample gate `2.735s`, slab/search `2.826s`, observation `1.839s`, learner `1.397s`, GPU `12.27%/36%`. | Historical direct-path bucket map, superseded by OPT-132-G/H repeatability. Reject direct-core alone as a speed win; do not use this row as current next-work guidance. |
| F-147 | 2026-06-02 | OPT-129 same-work H100 row | Threaded owner search preserved correctness and direct-root proof, but lost badly. Row `opt129-h100-threaded-owner-search-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `6051.61` env steps/sec and `30.458s` wall. Direct root resolve was tiny (`0.051s`), but learner train rose to `23.778s`, search to `4.555s`, actor to `17.914s`, and H100 utilization stayed low (`7.72%/27%`). | Reject threaded owner work. Park owner process/inline/threaded boundary variants unless a later same-work row proves owner ownership is required. |
| F-146 | 2026-06-02 | OPT-128 same-work H100 row | Serial inline owner search preserved correctness and direct-root proof, but lost full-loop speed. Row `opt128-h100-inline-owner-search-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `9516.73` env steps/sec and `19.368s` wall. It cut parent wait to `0.001s` and root resolve to `0.025s`, with direct root publish/resolve `225/225`, owner appends/train/updates/refresh `224/28/28/28`, parent sample/learner `0/0`, terminal sample/target rows `42/42`, normal death true, final pending/policy lag `0/0`. But wall regressed versus OPT-127 `17.771s`, with actor/slab rising to `8.404s`/`8.589s`. | Reject OPT-128 as a speed path. Keep the useful part, direct in-process root handoff. Drop the bad part, serial inline execution. Start OPT-129: asynchronous colocated owner worker with direct root handoff and restored background overlap. |
| F-144 | 2026-06-02 | OPT-127 same-work H100 row | Removing large resident owner root/replay host observation copies was a real owner-route win but not enough to beat OPT-104. Wall improved from OPT-123 `24.001s` to `17.771s`; root resolve fell `5.299s -> 0.740s`; replay append fell `8.983s -> 0.301s`; parent wait fell `9.219s -> 4.616s`. The row preserved owner appends/train/updates/refresh `224/28/28/28`, parent sample/learner `0/0`, terminal sample/target rows `42/42`, normal death, final pending/policy lag `0/0`, and action-only result shape. It still lost to OPT-104 `14.5255s`. | Keep OPT-127 as the best owner-search bucket map but not as the accepted speed baseline. Start OPT-128: remove or collapse the per-action owner process handoff; stop tiny owner-process copy/proof edits unless a row shows a wall win large enough to close the remaining gap. |
| F-145 | 2026-06-02 | OPT-127 report audit | The new resident host-copy boolean did not appear in the H100 top-level report, even though root/replay timing showed the copy cut landed and existing bridge fields were present. The missing field was a projection/list issue, not a speed-decision blocker. | Added the field to the Modal/local speed-row report lists so future rows can require it explicitly. |
| F-OPT085 | 2026-05-31 | OPT-081 local reorientation, subagent audits, and code inspection | OPT-080 proved the decoded direct-core cleanup safe but did not explain the remaining initial CUDA bucket. The existing representation/prediction fields were host enqueue timers, not GPU elapsed time, so choosing representation work, prediction work, compile, or deferral would still be guesswork. | Landed measurement-first OPT-081 instrumentation: direct-core initial inference records representation and prediction CUDA events, direct-core split total, and residual against the outer initial CUDA event. The split is projected through slab/source-profile/floor-bundle/decision surfaces, and direct-core CUDA-event evidence fails closed if split timings are missing or zero-filled. Local validation passed: ruff; compact Torch service `33 passed, 2 warnings`; floor-bundle suite `40 passed`; slab telemetry projection `1 passed, 2 warnings`. No speed claim; next step is fixed-root/H100 row/floor/decision evidence. |
| F-OPT084 | 2026-05-31 | OPT-080 fixed-root/H100/floor/decision evidence | The decoded direct-core output cleanup is semantically safe on the checked roots but not a speed win. Fixed-root public-method vs decoded direct-core gate `opt080-fixed-root-tape-decoded-directcore-smoke-20260531` passed exactly over 4 records / 1024 active roots: action match `1.0`, visit L1 `0.0`, root-value diff `0.0`, profile-only true, no live/training touch. H100 B1024/A16/sim1 rows preserved direct requested/used `180/180`, fallback `0`, action D2H `737280`, replay/committed replay D2H `0`, no host fallback, decoded direct-output true, and direct reward materialized false, but speeds were `12369.7` and `10740.5` steps/sec, below the OPT-079 direct-core repeats. | Close OPT-080 as safe cleanup/guard evidence only. The decision packet `opt080-compact-torch-service-bucket-decision-decoded-directcore-output-host-phase-cudaevent-sim1-20260531` keeps `speed_claim_allowed=false`, parks GPU mechanics/default compile, and still selects `initial_model_forward_compile_safe_path`. Continue guarded service/model work; do not infer a stock speedup, promotion, default compile, or GPU-mechanics mandate. |
| F-OPT083 | 2026-05-31 | OPT-080 local decoded direct-core output cleanup | After OPT-079, the direct-core path still shaped `_prediction` output back into a public initial-inference-like object, including a synthetic root reward list that tree search never used, and then decoded it through the generic output-field path. | Landed the small local cleanup only: direct-core now returns a slim private decoded output, `run()` and `run_action_step()` extract root policy/value/latent through one helper, tree search no longer receives the unused root-output object, and telemetry records direct decoded-output plus direct reward-materialization status. Validation: ruff passed; compact Torch service `33 passed, 2 warnings`; compact Torch plus floor-bundle `69 passed, 2 warnings`; focused replay telemetry `3 passed, 2 warnings`. Treat this as local host/model-path cleanup only. Fixed-root/action-trajectory and B1024/A16/sim1 H100 row/floor/decision evidence are still required before any speed read. |
| F-OPT082 | 2026-05-31 | OPT-079 direct-core fixed-root/H100 evidence | Direct-core initial inference is now real target-selection evidence, not just a local probe. A dedicated fixed-root sidecar compared public `initial_inference` primary against `initial_inference_direct_core` and passed exactly: 4 records / 1024 active roots, action match `1.0`, visit L1 `0.0`, root-value diff `0.0`, profile-only true, no live/training touch. Repeated H100 B1024/A16/sim1 rows then passed with direct requested/used `180/180`, fallback `0`, CUDA events recorded, action D2H `737280`, replay/committed replay D2H `0`, no host fallback, and speeds `15056.1` / `14874.6` steps/sec. During inspection, the runtime telemetry was clean but slab totals initially left direct requested/used at `0`; main patched the slab projection so the counters now aggregate to `180`. | Accept direct-core as guarded evidence for the compact Torch service/model lane, but not as a stock speedup or promotion. The decision packet selects `initial_model_forward_compile_safe_path`, keeps `speed_claim_allowed=false`, parks model compile default, and parks GPU mechanics. Next artifact is another small initial-forward/compile-safe optimization guarded by fixed-root or action-trajectory evidence. |
| F-OPT081 | 2026-05-31 | OPT-079 direct-core local probe | The installed CurvyTron visual-survival policy is plain LightZero `MuZeroModel`, whose public `initial_inference` is `_representation(obs)` followed by `_prediction(latent)` and a small `MZNetworkOutput` wrapper. Direct core matches policy/value/latent locally on the checked model shape, but the initial CUDA bucket may still be real CNN/prediction compute rather than wrapper overhead. | Added a default-off `initial_inference_mode=direct_core` path in compact Torch search service. It keeps the existing eval/inference guard, forbids model compile, fails closed without `_representation/_prediction`, emits mode and split timing telemetry through speed-row/profile/floor/decision surfaces, and has local parity/guard tests. Treat this as an attack-or-explain probe only; require fixed-root/action parity and H100 floor/decision evidence before any speed claim. |
| F-OPT080 | 2026-05-31 | OPT-079 local decision/floor-bundle hardening | The next speed row could have been overtrusted because current floor-bundle timing projection zero-filled missing CUDA-event fields, and the service-bucket decision only treated `cuda_event_timing_enabled_count > 0` as timing presence. Repeat bundles also needed the same transfer and call-count guards as canonical evidence. | Landed fail-closed local guards before another H100 service/model row: enabled CUDA-event rows require positive finite initial/recurrent/tree event timings; resident host-fallback count is projected and rejected; service-bucket decisions guard canonical and repeat evidence for recurrent calls, selected-action D2H, replay/committed replay D2H, host fallback, and timing completeness; source/payload non-claim flips have explicit negatives. Validation: ruff passed; floor-bundle suite `32 passed`; adjacent compact Torch/floor-bundle slice `59 passed, 2 warnings`. No speed claim. |
| F-OPT079 | 2026-05-31 | OPT-078 recurrent guard cleanup row, floor bundle, and decision | Removing the recurrent nested `torch.no_grad()` under the existing eval + `torch.inference_mode()` guard is safe and now measured. The service/slab/profile/bundle path exposes guard timing, and the decision gate now requires eval/inference guard telemetry. H100 row `opt078-h100-recurrent-guard-timing-host-phase-cudaevent-sim1-20260531` passed with `10740.8` steps/sec, wall `17.161s`, initial CUDA event `1.546s`, recurrent `0.336s`, guard total `0.208s`, action D2H `737280`, replay/committed replay D2H `0`, no host fallback. Floor bundle ratio is `1.281` versus fixed floor. | Close OPT-078 as safe support evidence and better accounting, not a speed win. The refreshed decision still selects `initial_model_forward_compile_safe_path`; next work must attack/explain the actual service/model bucket, not default compile, GPU mechanics, or promotion. |
| F-OPT078 | 2026-05-31 | OPT-077b H100 row, floor bundle, and decision | Packed scalar replay-index rows alone were not a speed win and revealed the actual child: metadata list materialization. OPT-077 row reported `10781.5` steps/sec, wall `17.096s`, replay-index build `2.259s`, metadata `1.651s`, scalar device transfer `0.067s`. OPT-077b changed terminal/death metadata from full lists to counts/checksums and passed H100 with `11735.2` steps/sec, wall `15.707s`, replay-index build `0.145s`, commit `0.185s`, metadata `0.013s`, action D2H `737280`, replay/committed replay D2H `0`, no host fallback. | Close replay-index/commit/sample as a reduced bucket but not as a top-level speed win. The refreshed decision selects `initial_model_forward_compile_safe_path`; compile remains optional/off by default and GPU mechanics remains parked until actor/env dominance repeats. |
| F-OPT077-local | 2026-05-31 | OPT-077 replay-index/commit/sample audit and local patch | `slab_commit_previous_sec` is a parent envelope around `_commit_previous`; `slab_replay_index_rows_build_sec` is a child and should not be added to it. The first safe replay-index reduction is to reduce scalar replay field device-transfer launch count and decompose the builder. The local patch packs scalar sidecars into 5 grouped device tensors, records `device_packed_scalar_v1`, and exposes identity/terminal/target/scalar-pack/scalar-device-transfer/metadata plus commit child/residual timing buckets. Sample-gate timing remains suspect because telemetry/checksum/readback can absorb sync debt. | Treat the local patch as instrumentation plus possible launch reduction only. Next evidence must be a same-denominator H100 row/bundle with raw new fields present before making any speed claim or moving on to model-forward/GPU-mechanics work. |
| F-OPT077 | 2026-05-31 | OPT-076 refreshed service-bucket decision | The target changed after reading the new H100 buckets. The refreshed decision packet binds OPT-073b canonical, OPT-074 repeat, OPT-076 repeat, OPT-073 support, and OPT-066 compile decision; it selects `replay_index_or_sample_gate`. In the OPT-076 bundle, dispatch residual is only `0.005s`, action residual `0.004s`, action readback `0.011s`, tree residual `0.008s`, and core residual `0.152s`, while replay-index build delta is `2.457s` and commit-previous delta is `2.491s`. | Supersede the OPT-074 current target. Next work is replay-index/commit/sample-gate decomposition, not GPU mechanics, recurrent deferral, default compile, or another blind initial-forward patch. |
| F-OPT076 | 2026-05-31 | OPT-076 H100 dispatch/service-envelope row and bundle | After OPT-075, the next H100 row needed finer buckets before another optimization attempt. Local instrumentation emitted action accounted/residual/overaccounted, initial/root-output decode, tree root-prior build, tree total/accounted/residual/overaccounted, action readback, and core accounted/residual/unaccounted/overaccounted through service/slab/profile/bundle telemetry. The H100 row `opt076-h100-dispatch-envelope-host-phase-cudaevent-sim1-20260531` passed with `12846.8` steps/sec, wall `14.348s`, fast-path/softmax-skip `180/180`, `masked_logits_argmax`, action D2H `737280`, replay D2H `0`, committed replay D2H `0`, no host fallback, loaded-checkpoint identity, guard telemetry, and raw OPT-076 fields present in `row_001_result.json`. The floor bundle is same-denominator H100 shape-matched and reports compact `12846.8` vs fixed floor `13759.0`, wall ratio `1.071`. | Treat OPT-076 as a completed measurement/target-selection gate, not a speed or promotion claim. It is shape-matched only because fixed-floor trajectory checksums differ. |
| F-OPT075 | 2026-05-31 | OPT-075 guard-only compact Torch service row and floor bundle | Removing the redundant nested `no_grad` around initial inference is safe under the existing eval/inference guard, but it is not a model-forward speed proof. H100 row `opt075-h100-guardonly-host-phase-cudaevent-sim1-20260531` reports `12563.1` steps/sec, wall `14.672s`, service `2.164s`, action wall `2.217s`, model `1.571s`, search `0.381s`, initial CUDA event `1.562s`, action D2H `737280`, replay D2H `0`, no hidden resident fallback, and guard telemetry restored. The floor bundle narrows compact-vs-fixed wall ratio to `1.095` but still reads dispatch/service-envelope dominant. | Treat OPT-075 as a safe cleanup with support evidence only. Next target is `decompose_compact_torch_search_dispatch_envelope`; do not claim initial-forward speed, stock speedup, promotion, GPU mechanics, or default compile. |
| F-OPT074 | 2026-05-31 | OPT-074 service-bucket decision packet | OPT-073b and the current CUDA-event repeat agree on slow wall, but the current event buckets point at initial model forward/service work: initial CUDA event `1.536s`, recurrent `0.333s`, tree `0.402s`, search delta `2.198s`, learner/sample `1.177s`, replay-index `0.984s`, actor wall `0.138s`. Guard checks pass and non-claims remain false. | Select `initial_model_forward_compile_safe_path` as the next implementation target; keep GPU mechanics and recurrent deferral parked. |
| F-001 | 2026-05-28 | `goal.md` re-read | The active goal is not a GPU mechanics rewrite; it is an honest compact-owned training candidate with fixed currencies and promotion gates. | Keep GPU mechanics staged until same-denominator evidence says actor/runtime remains the wall. |
| F-002 | 2026-05-28 | Wegener docs audit | Active optimizer folder had no doc index or durable finding/task/run/fallback ledgers. | Added skeleton docs and linked them from the operating memory. |
| F-003 | 2026-05-28 | Zeno and Chandrasekhar audits | Compact Torch rows can lose semantic labels in summaries despite backend telemetry. | Fixed source metadata, slab fallback, and manifest summary fallback; added regression tests. |
| F-004 | 2026-05-28 | Zeno audit | Hybrid summaries trusted manifest labels over payload labels. | Added manifest/payload triad mismatch failure in the hybrid runner. |
| F-005 | 2026-05-28 | Zeno audit | Older stock profile runner could mark compact `ok=false` payloads as `complete`. | Added `profile_failed` status for compact `ok=false`. |
| F-006 | 2026-05-28 | Russell test audit | Mixed terminal/live stock env-manager adapter behavior was the highest-value missing local fidelity gate. | Added CPU-only regression for one terminal row plus one live row through the stock-shaped adapter. |
| F-007 | 2026-05-28 | Euclid RND telemetry audit | RND reward-model stock-profile rows can miss raw collector env-step deltas because the profiler only patched `train_muzero.__globals__["Collector"]`. | The phase profiler now falls back to `lzero.worker.MuZeroCollector`; fallback-root speed currency is explicit and Gate-A-ineligible. |
| F-008 | 2026-05-28 | Kant root-tape audit | The fixed-root comparator core is solid; the weak spot is long-row durable capture, timeout handling, and detached Modal result collection. | Next remote/orchestration artifact is timeout/detach/captured-result support, not a comparator rewrite. |
| F-009 | 2026-05-28 | Plato resident parity audit | Existing tests prove resident pieces separately but not that persistent resident mode feeds the same normalized model-facing tensor as stateless host mode. | Next local fidelity artifact is a model-facing tensor parity smoke. |
| F-010 | 2026-05-28 | Hubble docs audit | Active docs contradicted each other about the current artifact and still listed completed work as next tasks. | Updated `goal.md`, `TASK_BOARD.md`, `TODO.md`, `FOLLOWUPS.md`, `CURRENT_STATE.md`, and orchestration docs around the new current state. |
| F-011 | 2026-05-28 | Main implementation | The resident persistent path feeds the same normalized model-facing tensor as the stateless host sentinel path over two nonterminal steps. | Closed the local parity gate; promote fixed-root durable capture to the active artifact. |
| F-012 | 2026-05-28 | Mill/Raman/Ampere audits plus main implementation | Larger root-tape rows should not rely on blocking stdout; durable evidence needs explicit detached launch, FunctionCall collection, exact safety labels, and root-tape invariant checks. | Added builder large preset, boundary spawn-result launch JSON, hybrid runner FunctionCall collection, command-timeout records, label-triad enforcement, root-tape invariant failures, flattened root-tape summaries, and focused tests. |
| F-013 | 2026-05-28 | Detached H100 root-tape row | The larger B512/A16/sim8 fixed-root row passed with 16 records and validated detached FunctionCall capture; compact Torch and fixed-shape floor match actions exactly but keep the same visit L1 gap as the smaller smoke. | Treat detached capture as validated for this gate; next work is adding a real additional backend comparator, not repeating compact Torch vs floor. |
| F-014 | 2026-05-28 | Main code read after larger row | The current root-tape service map only registers `primary` and optional `fixed_shape_floor`; the existing MCTX/direct-CTree comparison path wraps the primary service and does not expose direct CTree as its own tape service label. | Next implementation should add explicit additional root-tape service registration rather than relying on the wrapper comparison telemetry. |
| F-015 | 2026-05-28 | Pauli MCTX audit + main implementation | `run_compact_root_tape_comparison_v1` already supports arbitrary service labels; the missing piece was boundary/builder/runner registration for MCTX as an independent sidecar. | Added `--hybrid-compact-root-tape-compare-mctx`, `--next-fixed-root-tape-mctx-preset`, dynamic runner invariants/summaries, and three-service comparator tests. |
| F-016 | 2026-05-28 | Mencius direct-CTree audit | The current checkpoint-backed direct CTree builder cannot be compared cleanly against in-memory compact Torch without mixing backend effects with model identity. | Defer direct CTree root-tape sidecar until it can share the same in-memory policy/model or both sides are explicitly checkpoint-backed. |
| F-017 | 2026-05-28 | Boyle GPU-mechanics audit | The sync issue is real, but selected-action D2H is tiny; the bigger wall is CPU runtime plus mask/state/render-state ownership. Full GPU mechanics is high-risk because source-order collision, borderless, bonus, print-manager, and lifecycle semantics are fidelity-hard. | Do not start a full GPU mechanics rewrite now. If actor/runtime remains top wall, start with a no-bonus CPU-oracle lockstep contract behind `step_compact_profile`. |
| F-018 | 2026-05-28 | Euler fidelity/testing audit | The codebase has strong contract gates, but the missing evidence is promotion-level: remote RND raw env-step proof, durable MCTX/direct-CTree root-tape results, parity thresholds, compact trainer candidate, and matched stock-vs-candidate rows. | Keep run order explicit: local contract suites, local boundary/profile suites, manifest/currency suites, remote RND proof, remote fixed-root backend proof, confounder grid, then matched promotion rows. |
| F-019 | 2026-05-28 | MCTX first remote attempt | The MCTX root-tape sidecar flag selected the non-MCTX Modal image, causing `ModuleNotFoundError: No module named 'mctx'`; exception payloads also omitted two safety labels. | Select the MCTX image whenever `hybrid_compact_root_tape_compare_mctx=true`; include `calls_train_muzero=false` and `touches_live_runs=false` in exception payloads. |
| F-020 | 2026-05-28 | Corrected MCTX fixed-root row | MCTX ran as an independent fixed-root sidecar, but same-root semantic deltas are large: action match vs compact Torch primary `0.2829`, visit L1 mean `1.0119`, root-value diff mean `0.0506`. | Park MCTX as a drop-in parity backend; keep it only as a ceiling or explicit algorithm-change lane unless a future parity bridge is intentionally built. |
| F-021 | 2026-05-28 | Harvey/Mendel external/code-pattern refresh | External efficient-RL patterns reinforce static slabs, batched roots, two-phase search/replay, coarse sync points, and role separation. They do not justify immediate Ray/distributed architecture, CUDA graphs, or GPU mechanics before trainer honesty. | Next compact-owned step should use existing compact Torch/resident/device-replay/compact-learner pieces with explicit metadata, not promote MCTX or start a full GPU mechanics rewrite. |
| F-022 | 2026-05-28 | First compact-owned candidate attempt | The builder allowed `hybrid_resident_observation_search=true` without `hybrid_device_only_stack=true`, but the Modal boundary rejects that combo. | Added builder validation and regression tests so invalid resident-search manifests fail before Modal. |
| F-023 | 2026-05-28 | Corrected compact-owned candidate row | H100 B1024/A16/sim8 compact Torch row passed with resident observation, device replay, sample gate, and compact MuZero learner edge: `20113.8` steps/sec, resident fallback `0`, obs H2D `0`, replay D2H `0`, real MuZero update `true`. | Treat this as the current compact-owned profile denominator; next decision is trainer-honesty gap, not another backend micro-optimization. |
| F-024 | 2026-05-28 | Hume trainer-honesty audit | The compact MuZero learner edge is still one-step/profile-local, the compact replay ring is bounded/profile-only, and durable multi-step sample windows plus terminal/final-observation handling are not proven. | Next artifact is durable replay/full MuZero unroll semantics before matched stock-vs-candidate speed rows. |
| F-025 | 2026-05-28 | Sagan runner audit + main implementation | Compact-owned candidate rows could be summarized as complete even if they silently lost resident/no-scalar/replay-D2H/sample/real-learner guarantees. | Added compact-owned candidate preflight and result invariants with `compact_owned_candidate_invariant_failed` status and regression coverage. |
| F-026 | 2026-05-28 | Nash builder-boundary audit + main implementation | Builder could emit LightZero/MCTX rows with `device_latest`, non-`uint8` stacks, wrong render surface/backend, resident search without CUDA, or CLI-only invalid combos that the Modal boundary would reject. | Moved those checks into `build_manifest()` and added builder regressions. |
| F-027 | 2026-05-28 | Dalton/Noether/Lorentz audits + main implementation | The honest next slice was nonterminal N-step replay windows feeding a real recurrent compact MuZero learner; terminal/final-observation resident replay remains a distinct unsupported case. | Added contiguous same-env/player unroll windows, explicit resident unroll tensors, `num_unroll_steps > 1` learner updates, Modal/builder/runner unroll wiring, and focused tests. |
| F-028 | 2026-05-28 | Fermat/Kepler/Copernicus terminal audit wave + main implementation | Terminal compact replay-index rows could silently use immediate reward as final reward when `next_final_reward_map` was omitted; compact MuZero had no way to mask post-terminal N-step losses; resident replay lacked a device-resident final-observation tensor. | Added host/device final-reward fail-closed checks and learner validity masks with terminal provenance; the later resident terminal sampling slice closed the local final-observation gap. |
| F-029 | 2026-05-28 | Bacon/Confucius/Tesla audit wave + main implementation | Resident terminal replay can stay compact-owned if final observations are explicit resident tensors with matching row masks, and if terminal N-step windows mask post-terminal slots instead of inventing fake targets. | Added resident final observation contract fields, source-aware root validation, replay-snapshot cloning, resident terminal sample builders, N-step mask propagation, and local terminal/learner regressions. |
| F-030 | 2026-05-28 | Local validation | Resident terminal final-observation sampling and compact MuZero N-step mask propagation pass locally, but this is still not stock target parity or durable remote evidence. | Closed by later terminal stock target-alignment and durable remote terminal-safe compact-owned N-step row evidence. |
| F-031 | 2026-05-28 | Volta/Halley audits + main implementation | Terminal compact N-step value targets were still raw search roots where stock MuZero terminal tails use no-bootstrap reward returns. | Compact terminal unroll value targets now become stock-style reward returns for terminal rows, and the learner rejects terminal N-step values that do not match those returns. |
| F-032 | 2026-05-28 | Darwin remote-row audit + main implementation | The remote compact-owned candidate invariant did not prove that terminal windows were sampled or consumed by compact MuZero. | Added a terminal-forcing preset, `max_ticks` manifest wiring, terminal sample/target telemetry summaries, and runner invariants for terminal sample rows, final observations, stock target mode, and learner `done_count`. |
| F-033 | 2026-05-28 | Remote row `optimizer-compact-owned-terminal-nstep-next-20260528` | `max_ticks=1` forced every post-step root terminal, so compact active roots, committed replay rows, sample rows, and learner updates were all zero. | Corrected the preset to `steps=6`, `max_ticks=3`, and added runner/builder preflight checks for active, terminal, and reset-successor replay rows. |
| F-034 | 2026-05-28 | Remote row `optimizer-compact-owned-terminal-nstep-next-20260528b` | Real terminal rows do not have searched post-terminal successor roots, so requiring a full successor chain excluded every N-step sample despite 1024 raw stored rows. | Added terminal-tail padding index rows for all-terminal chains and a local regression that samples terminal two-step targets without a reset successor. |
| F-035 | 2026-05-28 | Remote row `optimizer-compact-owned-terminal-nstep-next-20260528c` | The compact learner support scale was mismatched: the profile LightZero model has support scale `9`, while the preset used `300`. | Terminal preset now uses support scale `9`; builder test asserts the model-matched value. |
| F-036 | 2026-05-28 | Remote row `optimizer-compact-owned-terminal-nstep-next-20260528d` | The durable terminal-safe compact-owned N-step row passed with terminal samples, resident final observations, no-bootstrap terminal value targets, and real compact MuZero updates. | Closed the terminal N-step fidelity row only; later death/terminal audit clarified this is not a promotable normal-death contract. |
| F-037 | 2026-05-28 | Kuhn/Nietzsche/Schrodinger audit wave | The next highest-value trainer-honesty gate is a stock-vs-candidate public sample diff, not another compact-only target assertion or speed row. | Implement the diff against real LightZero `GameSegment -> MuZeroGameBuffer.sample(...)`, then move to durable compact replay-store ownership. |
| F-038 | 2026-05-28 | Main implementation | Compact multi-record terminal/nonterminal rows now match real LightZero public sample observations, valid actions, masks, rewards, values, and policies for the checked N-step shape. | Closed the local stock-vs-candidate sample diff gate; next artifact is durable compact replay-store fidelity around the same fields. |
| F-039 | 2026-05-28 | Carver/Hooke/Laplace audit wave | The smallest honest durable replay-store gate is a snapshot/restore contract around the existing compact replay ring, while the stock claim boundary stays at sample-visible transition batches rather than native LightZero storage. | Implement ring durable state and tests; stamp policy/model lineage, strict load, eviction counts, profile-only non-claims, and no stock-storage/speed claims. |
| F-040 | 2026-05-28 | Main implementation | The profile-local compact replay ring can now round-trip durable state for host/resident samples, preserve FIFO eviction and restart/load semantics, and keep public-sample index rows stable through pickle before materialization. | Close the local durable replay-store fidelity gate; next artifact is the split compact-owned loop entrypoint with replay-store ownership and policy-version handoff. |
| F-041 | 2026-05-28 | Einstein/Cicero/Lagrange audit wave | The next honest boundary is not another profile-harness counter; it is an explicit compact-owned loop module with policy-version lineage, replay-store ownership, and non-claim metadata. | Added `compact_owned_loop.py` and wired the hybrid profile to route sample/learner cadence through it when `compact_owned_loop_entrypoint=true`. |
| F-042 | 2026-05-28 | Main implementation | The split entrypoint can be proven locally by snapshotting phase-A replay state, restoring it through strict durable load, and sampling phase B from the loaded store with policy lineage intact. | Added direct split durable-load tests, hybrid profile integration tests, builder/Modal/runner flags, and fail-closed runner invariants; next evidence is a remote split-entrypoint row. |
| F-043 | 2026-05-28 | Remote row `optimizer-compact-owned-split-entrypoint-next-20260528` | A zero-warmup split-entrypoint row starts with no previous step, so replay append/sample must not run on the first measured step. | `CompactOwnedLoopV1.record_step(...)` now primes the first previous step without appending replay; a regression locks this behavior. |
| F-044 | 2026-05-28 | Remote row `optimizer-compact-owned-split-entrypoint-next-20260528b` | The remote profile computed the split-entrypoint path, but compact output projection dropped every `compact_owned_loop_*` field before the runner invariants saw it. | `_compact_hybrid_observation_profile_result(...)` now preserves compact-owned loop fields, telemetry, and replay-store state metadata; a compact-output mapping regression covers the projection. |
| F-045 | 2026-05-28 | Remote row `optimizer-compact-owned-split-entrypoint-next-20260528c` | The split compact-owned loop entrypoint survives builder, Modal, compact output projection, and runner invariants with replay-store state capture and policy/model/source lineage intact. | Close the remote split-entrypoint follow-up; next artifact is matched stock-vs-candidate denominator selection, not another split-loop proof row. |
| F-046 | 2026-05-28 | Dirac/Herschel/Arendt matched-denominator audit wave | The denominator pair must be explicit cross-currency evidence: stock `train_muzero` env steps/sec versus compact split-loop active roots/sec. The terminal B128 row is fidelity evidence, not the speed denominator. | Selected a no-RND H100 pair and added `MATCHED_DENOMINATOR_2026-05-28.md`, manifest metadata, counterpart refs, speed currencies, and non-claim fields. |
| F-047 | 2026-05-28 | Main implementation | Matched-denominator manifests can now fail closed before and after launch: stock rows must prove raw collector env-step currency, and compact rows must preserve split-entrypoint lineage plus nonterminal compact Torch shape. | Added stock and compact matched-denominator presets, runner preflight/result invariants, compact `max_ticks` projection, focused tests, and materialized/dry-ran both manifests. |
| F-048 | 2026-05-28 | Remote compact matched denominator first attempt | The compact result carried `max_ticks` only under config, while the compact projection omitted top-level `max_ticks`; the new matched speed-row invariant correctly failed closed. | Preserve the failed result and emit top-level `max_ticks` from the boundary profile result/projection. |
| F-049 | 2026-05-28 | Remote matched denominator pair | The selected pair completed: stock raw env-step profile speed is `642.9` steps/sec, compact split-loop profile speed is `23105.0` compact active-root steps/sec with lineage/no-scalar/resident invariants intact. | Close denominator run; next artifact is Coach-compatibility selection, not another profile-only speed row. |
| F-050 | 2026-05-28 | Curie measurement audit | The matched pair is captured, but stricter reciprocal verification and exact denominator/cadence arithmetic are not yet encoded. Some older docs still phrased captured rows as "to run next" or treated historical stock rows as latest. | Reconciled active docs and opened a P1 matched-pair verifier follow-up. |
| F-051 | 2026-05-28 | Hopper GPU-mechanics audit | Mechanics remain CPU-owned in `vector_runtime.step_many()` / `step_compact_profile`; observation/search/replay are the resident parts. GPU mechanics should start only with a narrow CPU-oracle lockstep contract if actor/runtime remains hot. | Keep GPU mechanics staged behind Coach/trainer lifecycle gates; record no-bonus lockstep as the eventual smallest parity gate. |
| F-052 | 2026-05-28 | Poincare/Feynman Coach-compatibility audits + main implementation | The next honest artifact is compact-owned, not stock `train_muzero` injection. The immediate missing gate is checkpoint/resume-facing trainer envelope, because eval/tournament/assignment have nothing honest to load yet. Boolean promotion gates alone would also be too easy to overclaim. | Added `compact_coach_compatibility.py`, wired fail-closed compatibility metadata into `CompactOwnedLoopV1`, required evidence refs for every promotion gate, and promoted compact checkpoint/resume envelope to OPT-028. |
| F-053 | 2026-05-28 | Maxwell/Lovelace/Shannon checkpoint audits + main implementation | A useful compact checkpoint must save actual replay-store state and loop runtime state, not only metadata; it must also stay compact-native because stock eval/tournament loaders need a separate mapping payload and policy-observation sidecar. | Added `compact_owned_trainer.py`, `compact_trainer_checkpoint.py`, runtime capture/restore helpers, stale record-index guard, real compact MuZero checkpoint test, and `adapter_missing` stock-loader non-claim. |
| F-054 | 2026-05-28 | Ada/Tarski stock-loader audits | Existing stock eval/tournament/frozen-opponent code already understands normal mapping checkpoints with model state under `model` and sidecar metadata at `<checkpoint>.metadata.json`; a compact eval adapter would be larger and still not prove strict stock model compatibility. | Implemented derived stock-shaped compact export and kept the compact checkpoint native. |
| F-055 | 2026-05-28 | Main implementation | The derived export can now pass state-dict discovery, provider policy-observation metadata checks, and tournament payload metadata extraction, but strict stock LightZero model load is still unproven. | Export metadata records `stock_model_contract_verified=false`, `stock_eval_tournament_loadable=false`, and `strict_stock_model_load_not_run`; next gate is a real strict-load verifier run. |
| F-056 | 2026-05-28 | Main implementation | The strict-load verifier harness can now exercise the existing stock LightZero loader path and report success/failure, but unit-level mocked success is not proof that current compact models are stock-loadable. | Added `verify_compact_stock_export_model_contract_v1(...)`; the later stock-model smoke supplied real verifier evidence for the stock-shaped export path. |
| F-057 | 2026-05-28 | Local verifier smoke | A compact checkpoint wrapping a stock LightZero `MuZeroPolicy._model` can be exported through the compact stock-export path and strict-loaded by the stock loader under `model`. | Preserved durable verifier report at `artifacts/local/curvytron_compact_stock_export_verifier_results/optimizer-compact-stock-export-verifier-smoke-20260528/verification_report.json`; the next gate became tournament policy-loader consumption, not more model-load harness work. |
| F-058 | 2026-05-28 | Carson loader audit + local loader smoke | The existing tournament policy loader can consume the stock-shaped compact export when given `state_key=model`, and loader runtime metadata must be passed into policy construction rather than merely recorded. | Updated `_checkpoint_runtime_settings_from_payload(...)` / `_load_policy_from_checkpoint(...)`, added compact-export loader regression, and preserved `tournament_loader_report.json` with strict `candidate=as_is`; scope excludes game rollout/GIF render. |
| F-059 | 2026-05-28 | Huygens evidence-bundle audit + main implementation | Verified loadability should be evidence-scoped: the base stock-shaped compact export must stay non-promotion/non-loadability by default, while a sibling bundle can bind export, sidecar, verifier, and tournament-loader reports by hash. | Added `curvyzero_compact_stock_export_evidence_bundle/v1`, validation for stale hashes, failed verifier reports, loader runtime drift, and promotion-claim leaks; durable evidence bundle passed. Superseded by F-060 as the current eval/load evidence frontier. |
| F-060 | 2026-05-28 | Local one-game/GIF smoke | The bundled stock-shaped compact export can drive the existing tournament eval action path and GIF renderer for a tiny local self-play smoke. | Preserved `one_game_gif_smoke_report.json`, game summary, GIF, and frames NPZ; next task is to refresh the compatibility report and select the next missing lifecycle gate, not to claim promotion. |
| F-061 | 2026-05-28 | Lovelace/Ptolemy eval-smoke audits + main implementation | The tournament smoke should be paired with the standalone visual-survival eval core before closing eval/GIF/tournament load. `_run_eval(...)` can consume the stock-shaped export via state-dict discovery and run local `commit=false` evidence. | Added standalone eval result into the smoke report: strict load ok, env reset ok, policy acted in real eval env, and 8 steps survived. |
| F-062 | 2026-05-28 | Main compatibility refresh | After gameplay/eval evidence, the compact Coach report can honestly mark trainer entrypoint, checkpoint save/load, resume metadata, and eval/GIF/tournament load as passed, but reward/RND, death/terminal, policy refresh, and metrics lineage remain missing. | Preserved `compatibility_report.json` and selected `reward_rnd_contract` as the next P0 promotion gate. |
| F-063 | 2026-05-28 | Anscombe/Faraday/Leibniz reward/RND audits + main implementation | Reward contracts exist for source-state variants, and RND exists as stock reward-model/exploration plumbing, but compact-owned training has no RND update/checkpoint/restore lifecycle. The first honest compact-owned gate is therefore explicit no-RND extrinsic reward semantics. | Added `compact_reward_rnd_contract.py`, attached the contract to compact trainer/checkpoint metadata, rejected RND-enabled configs/state, refreshed Coach compatibility with `reward_rnd_contract=true`, and selected `death_terminal_contract` next. |
| F-064 | 2026-05-28 | Godel/Hegel/Heisenberg death-terminal audit + main correction | Compact terminal N-step handling is terminal-safe but not yet source-faithful death-safe. The existing durable row is profile/no-death max-tick evidence, not normal collision death evidence. | Added a partial `compact_death_terminal_contract.py`, attached it to trainer/checkpoint metadata as promotion-gate false, refreshed compatibility with `death_terminal_contract=false`, and kept `normal_collision_death_not_proven` as the active blocker. |
| F-065 | 2026-05-28 | Sartre normal-death audit + main implementation | The compact path was dropping source terminal facts: config hardcoded `profile_no_death`, native payloads lacked death fields, and replay commits reconstructed `terminated=done`, `truncated=false`. | Added `death_mode` to the hybrid config, propagated terminated/truncated/death metadata through actor payloads and compact batches, stamped replay-index metadata, and kept the Coach gate blocked until normal-death proof exists. |
| F-066 | 2026-05-28 | Hilbert/McClintock policy-refresh and metrics audits | Policy/model refs and metrics are scaffolding, not proof. Search does not yet prove it consumed learner-updated weights, and `bool(metrics)` was an overclaim for metrics lineage. | Recorded policy-refresh and metrics-lineage contracts as follow-ups; changed checkpoint compatibility so arbitrary metrics produce only partial metrics-presence evidence and keep `training_metrics_lineage=false`. |
| F-067 | 2026-05-28 | Main validation after terminal sidecar propagation | Adding terminal/death sidecars to `HybridCompactBatch` broke older no-death synthetic constructors until defaults were made explicit. | Added backward-compatible no-death defaults in `HybridCompactBatch.__post_init__`; focused ruff passed and the compact/source-hybrid validation slice passed `189 passed, 2 warnings`. |
| F-068 | 2026-05-28 | Erdos/Banach/Turing audits + main implementation | The shortest local normal-death proof is the 2P head/head opponent-trail fixture, and durable profile rows were still unable to request real normal death because the Modal/grid surface was hardwired to no-death. | Added a native compact-path fixture test for `source_collision_head_head_reverse_order_single_death_step.json`, exposed `death_mode` through the Modal boundary/grid/runner, added row/command/payload checks, and validated the compact/profile slice with `434 passed, 2 warnings`. The death/terminal Coach gate remains blocked until a structured normal-death contract and durable artifact exist. |
| F-069 | 2026-05-30 | Main contract hardening | The normal-death gate needed a structured contract mode and metadata spoof protection before any durable row could honestly close it. Otherwise a caller could overclaim by passing vague evidence or overriding checkpoint compatibility keys. | Added guarded `normal_collision_death_terminal_nstep_v1` with required evidence fields, made trainer/checkpoint metadata derive from the contract, rejected protected `extra_metadata` overrides, and validated with ruff plus `22 passed` focused tests and `442 passed` compact Coach/death/profile tests. Gate remains blocked until a compact-owned lifecycle artifact emits the evidence. |
| F-070 | 2026-05-30 | Descartes/Beauvoir audits + main implementation | Aggregates alone were still too handbuilt for the normal-death gate; the emitted profile payload needed bounded row-level death evidence and the runner needed to validate that payload-derived evidence under an explicit requirement. | Added `normal_collision_death_evidence_rows`, death-cause/final-observation/final-reward proof fields, boundary/runner pass-through, profile-payload evidence builders, and runner invariant validation. Validated with ruff, `202 passed` focused tests, and `449 passed` broader compact Coach/death/profile tests. |
| F-071 | 2026-05-30 | First durable normal-death attempt | The B512/sample-all normal-death row exhausted mixed JAX/Torch GPU memory before producing the proof; the failure was allocator/sample pressure, not a semantic death-contract failure. | Added mixed-image allocator guards (`XLA_PYTHON_CLIENT_PREALLOCATE=false`, PyTorch expandable segments) and made the normal-death preset use bounded B256 learner sampling. |
| F-072 | 2026-05-30 | Bounded normal-death retry | The first B256 retry found normal terminal/death rows in the profile payload but sampled zero terminal learner rows because finite N-step sampling excluded partial terminal entries without searched post-terminal successors. | Added terminal-aware finite sampling and terminal-only partial-terminal candidates so bounded samples can prove no-bootstrap terminal semantics without sample-all or fake reset successors. |
| F-073 | 2026-05-30 | Durable normal-death row + checkpoint smoke | The compact-owned normal-death lifecycle proof now passes with real normal deaths, terminal samples, final observations, no-bootstrap targets, learner done rows, and checkpoint compatibility metadata derived from the real profile payload. | Close `death_terminal_contract` as the active blocker. The next P0 gates at the time were policy-refresh handoff and training-metrics lineage; F-074 closes the local metrics contract while promotion remains false. |
| F-074 | 2026-05-30 | Policy/metrics/docs audit wave + main implementation | Existing metrics and refs were scaffolding, not proof. Metrics lineage needed finite learner telemetry, trainer counters, replay/sample/search provenance, checkpoint resume refs, loop counters, and explicit non-claims; at that moment policy refresh still needed a distinct search-worker refresh proof. | Added `curvyzero_compact_training_metrics_lineage/v1`, checkpoint/trainer wiring, search provenance propagation, and tests. Close the local metrics-lineage contract. F-075 subsequently closes local policy-refresh; promotion remains false until current-chain eval/GIF/tournament-load is refreshed or bound. |
| F-075 | 2026-05-30 | Policy-refresh implementation wave | A policy-refresh proof needs more than refs in checkpoint metadata: it must prove a distinct search worker consumed learner-updated weights, cleared caches, and produced stamped root/action/replay/sample evidence. Subagent audit caught that root batches were still unstamped. | Added `curvyzero_compact_policy_refresh_handoff/v1`, concrete search-worker refresh state, root/action/replay/sample propagation including `CompactRolloutSlab.step(...)`, checkpoint attachment, fail-closed stale-worker tests, and Coach evidence tests. Close local policy-refresh handoff; F-076 then adds the local current-chain eval/GIF/tournament evidence contract. |
| F-076 | 2026-05-30 | Archimedes/Mendel/Sagan current-chain audits + main implementation | Strict stock model load and construct-only tournament-loader evidence were too easy to overread as full eval/GIF/tournament-load proof. The 2026-05-28 gameplay smoke is valid only for its exact old bundled export, and the 2026-05-30 normal-death checkpoint smoke uses a tiny `torch.nn.Linear(3,2)` model that cannot prove stock LightZero eval compatibility. | Added `curvyzero_compact_current_chain_eval_gif_tournament_load_evidence/v1`, hash-bound validation for compact checkpoint/export/sidecar/verifier/loader/bundle/gameplay/GIF/frames/eval files, protected stock export metadata overrides, downgraded strict-load/loader-only loadability claims, and required current-chain evidence refs for Coach `eval_gif_tournament_load=true`. Local structural gate passed `41 passed, 2 warnings`; F-077 closes the real local current-chain smoke. |
| F-077 | 2026-05-30 | Main current-chain smoke implementation | A real current-chain eval/GIF/tournament proof is feasible locally when the compact checkpoint wraps a stock LightZero `MuZeroPolicy._model`, not the tiny Linear normal-death checkpoint. The old 2026-05-28 compatibility reports remain historical/export-scoped. | Added `scripts/build_compact_current_chain_eval_gif_tournament_smoke.py`; generated `optimizer-compact-current-chain-eval-gif-tournament-smoke-20260530` with compact checkpoint, stock export, sidecar, strict verifier, tournament loader, stock evidence bundle, one-game GIF/frames, standalone eval, current-chain evidence bundle, and compatibility report using a `compact_current_chain_eval_gif_tournament_load:` ref. Next blocker is a unified compact checkpoint lifecycle, not eval/GIF loadability alone. |
| F-078 | 2026-05-30 | Nietzsche/Bohr/Einstein audits + main unified smoke | The previous compatibility schema would have made a local all-lifecycle-gates artifact `promotion_eligible=true` without a real Coach speed/training row. The lifecycle gates can now be bound locally to one LightZero-shaped compact checkpoint/report chain, but promotion eligibility must stay blocked on explicit speed-row evidence. | Added `coach_speed_row` as a required promotion gate, kept local lifecycle artifacts promotion-ineligible without `compact_coach_speed_row:` evidence, extended the current-chain smoke with normal-death/profile, policy-refresh, metrics-lineage, and current-chain evidence on one LightZero-shaped checkpoint, generated `optimizer-compact-unified-lifecycle-smoke-20260530`, reconciled active docs, and promoted the next P0 to a real Coach-comparable compact speed row. |
| F-079 | 2026-05-30 | Hooke/Confucius/Peirce speed-row audits + main implementation | A `compact_coach_speed_row:` prefix alone is too easy to fake, and a profile-only active-root row tied to the lifecycle candidate would still not prove Coach-comparable speed/training. | Added `curvyzero_compact_coach_speed_row_evidence/v1`, made Coach compatibility require structured hash-bound evidence for `coach_speed_row=true`, rejected profile-only rows/currencies and fallback denominators, and validated with `21 passed`, broader `59 passed, 2 warnings`, and ruff. Superseded by F-085 for the first real non-profile H100 row. |
| F-080 | 2026-05-30 | Feynman/Popper/Heisenberg/Halley/Herschel speed-row hardening audits + main implementation | The first structured speed-row contract still had spoof surfaces: arbitrary schemas, weak producer proof, evidence denominator override, candidate/route drift, stock-profile currency leakage, unchecked loaded-manifest claims, and NaN/Inf acceptance. Existing hybrid profile machinery also could not honestly emit the non-profile row. | Hardened `compact_coach_speed_row.py` around speed-row-specific manifest/result/producer schemas, route allowlists, candidate/route/result identity, row snapshot hashes, loaded-manifest nonclaims, producer provenance, result `ok`/`returncode`/`run_invocation_id`, denominator binding to loaded result-summary fields, and finite positive numbers. |
| F-081 | 2026-05-30 | Local compact Coach speed-row producer smoke | A tiny local CPU sibling producer can now emit and validate a non-profile compact-owned speed row for the unified lifecycle candidate while preserving the raw support profile as support-only evidence. | Added `scripts/build_compact_coach_speed_row_smoke.py` and generated `optimizer-compact-coach-speed-row-smoke-20260530`: `env_steps_collected=8.0`, `training_wall_sec=0.8652770410408266`, `compact_trainer_env_steps_per_sec=9.245593746919415`, real compact-owned local work, and all non-claims false. Superseded for final identity by the H100 loaded-checkpoint row in F-085. |
| F-082 | 2026-05-30 | Aristotle/Volta/Mencius speed-row identity audit + main hardening | Candidate id alone is not enough for the final Coach speed gate: a stale or unrelated H100 row could name the unified lifecycle candidate without actually loading that checkpoint/model. Existing H100 hybrid profile rows are useful support payloads only, and the smallest honest remote path is a dedicated compact Coach speed-row producer. | Added model-identity scope to `curvyzero_compact_coach_speed_row_evidence/v1`. Local support evidence records lifecycle checkpoint path/hash/model digest as `candidate_named_support_only`; final compatibility validation now requires `candidate_loaded_checkpoint` result identity matching checkpoint id, trainer id, policy/model refs, policy source, checkpoint hash/path when available, and model-state digest. The dedicated remote row now exists in F-085. |
| F-083 | 2026-05-30 | First H100 compact Coach speed-row attempts | The dedicated Modal producer/collector path worked, but two honest remote failures found real packaging/device-residency issues: first the H100 image did not include the top-level `scripts` module, then the CUDA learner rejected CPU resident observations. | Added the smoke producer script to the Modal image with `/repo` on `PYTHONPATH`, and made the speed-row renderer place resident `device_frames` on the learner device. These failures are preserved as diagnostics under `optimizer-compact-coach-speed-row-h100-tiny-20260530` and `optimizer-compact-coach-speed-row-h100-tiny2-20260530`. |
| F-084 | 2026-05-30 | H100 tiny loaded-checkpoint speed-row smoke | The dedicated H100 producer can load the unified lifecycle checkpoint and pass the structured speed-row evidence contract with `candidate_loaded_checkpoint` identity. | Generated `optimizer-compact-coach-speed-row-h100-tiny3-20260530`: `env_steps_collected=8.0`, `training_wall_sec=2.1551747569999993`, `compact_trainer_env_steps_per_sec=3.7119959641398133`, CUDA learner, resident sample true, device replay true, learner observation/input H2D bytes `0`. This is contract evidence, not the useful throughput row. |
| F-085 | 2026-05-30 | Durable H100 B128/A8 compact Coach speed row | A moderate H100 non-profile compact Coach speed row now validates under `curvyzero_compact_coach_speed_row_evidence/v1` with loaded-checkpoint identity for the unified lifecycle candidate. | Generated `optimizer-compact-coach-speed-row-h100-b128a8-20260530`: `env_steps_collected=2560.0`, `training_wall_sec=2.327328861`, `compact_trainer_env_steps_per_sec=1099.973468682903`, `model_identity.scope=candidate_loaded_checkpoint`, learner calls `10`, learner rows `640`, CUDA resident sample true, device replay true, observation/input H2D bytes `0`, and all stock-resume/live-run/rating/promotion-quality claims false. Next decision is threshold/compatibility refresh, not producer packaging. |
| F-086 | 2026-05-30 | B512/B1024 speed-row threshold ladder | The dedicated H100 producer scales cleanly beyond the first B128 row, but short rows can be too fast to satisfy a conservative measured-wall threshold. | Captured B512/A16 (`30720.0` env steps, `4.559999145999999s`, `6736.843366943913` env steps/sec), B1024/A16/60 (`61440.0`, `6.262233370000001s`, `9811.196161154881`), and B1024/A16/120 (`122880.0`, `8.350520967000001s`, `14715.249561746294`). Kept them as ladder evidence and required a longer B1024/A16/180 row before closing the gate. |
| F-087 | 2026-05-30 | Accepted threshold speed row and compatibility refresh | The compact-owned compatibility gates can now be locally complete with structured H100 speed-row evidence, but that still must not be overread as deployment/promotion proof. | Accepted `optimizer-compact-coach-speed-row-h100-b1024a16-threshold-20260530`: `env_steps_collected=184320.0`, `training_wall_sec=18.397932468`, `compact_trainer_env_steps_per_sec=10018.517043727199`, loaded-checkpoint identity, resident sample true, device replay true, observation/input H2D bytes `0`, materialized timesteps `0`, learner/sample rows `11264`, all non-claims false. Added `scripts/build_compact_coach_compatibility_speed_row_refresh.py` and generated `optimizer-compact-coach-compatibility-after-speed-row-h100-threshold-20260530`, which sets `coach_speed_row=true`, has no missing current local compatibility gates/evidence, computes `promotion_eligible=true`, and keeps `promotion_claim=false`. |
| F-088 | 2026-05-30 | Post-compatibility promotion-readiness audit | The refresh path keeps `promotion_claim=false`, but the generic compatibility builder and exported report dataclass needed a second seal so local `promotion_eligible=true` could not be converted into a claim by changing one boolean. | Added `COMPACT_PROMOTION_READINESS_GATE_2026-05-30.md`; the builder now rejects completed local compatibility claims with `post_compatibility_promotion_readiness_required`, direct report construction with `promotion_claim=true` fails, and the next P0 is a real readiness evidence producer rather than another compatibility refresh. Focused ruff passed; `tests/test_compact_coach_compatibility.py` passed with `18 passed`. |
| F-089 | 2026-05-30 | Stock resume/load canary readiness lane | A compact checkpoint can be reloaded, re-exported into the LightZero iteration-checkpoint shape, selected by stock checkpoint discovery, patched into `load_ckpt_before_run`, strict-loaded under `model`, and consumed by the tournament loader without flipping broader claims. | Added `curvyzero_compact_promotion_stock_resume_load_canary/v1`, generated `optimizer-compact-promotion-stock-resume-load-canary-20260530`, and kept `promotion_claim=false`, `stock_resume_claim=false`, `calls_train_muzero=false`, `touches_live_runs=false`, and live/rating/speed claims false. Focused readiness/compatibility/speed-row slice passed `47 passed, 2 warnings`. |
| F-090 | 2026-05-30 | Matched learning-quality audit | The old matched denominator pair is not learning-quality proof; it is profile/speed evidence with different currencies and no matched eval movement. Counting it as quality would recreate the original denominator confusion. | Required a separate two-arm matched learning-quality canary with fixed eval seeds, matched settings, visible denominators, at least two eval points per arm, and no promotion/speedup/live/resume/rating claim. That later passed in the 1024x8 env16/train2 matched canary. |
| F-091 | 2026-05-30 | Matched learning-quality contract implementation | A contract-only validator is the right next slice before a real producer, because it blocks old speed/profile rows, route mislabels, support-only compact identity, seed/settings drift, fallback denominators, and non-claim flips from becoming fake quality proof. | Added `curvyzero_compact_promotion_matched_learning_quality_canary/v1` plus focused tests. Later 1024x8 stock/compact captures validate under this schema; promotion readiness, live-run safety, stock resume claim, rating quality, leaderboard, and speedup claims remain false. Ruff passed; focused suite `14 passed`. |
| F-092 | 2026-05-30 | Matched learning-quality builder audit | A public path that accepts finished arm JSON as the source of truth would preserve the fabrication risk; the public tooling should consume raw stock/compact captures and derive validator arms. | Added `curvyzero_compact_matched_learning_quality_capture/v1` and a capture-derived builder CLI. The builder writes derived `*_arm.json` files and a matched report; later it built the 1024x8 matched canary from real stock/compact captures. Ruff passed; builder+contract suite `17 passed`. |
| F-093 | 2026-05-30 | Matched learning-quality capture authenticity audit | A no-training helper that emits `stock_reference_capture.json` or `compact_candidate_capture.json` would launder support/source inventory into real quality evidence. The authenticity boundary must live in the capture schema before the producer exists. | Added `curvyzero_compact_matched_learning_quality_capture_provenance/v1` requirements for builder-eligible captures and a separate preview schema/helper with `usable_as_quality_capture=false`, `feeds_builder=false`, and forbidden builder input filenames. Ruff passed; focused builder/preview/contract suite `22 passed`; broader readiness slice `69 passed, 2 warnings`. |
| F-094 | 2026-05-30 | Matched learning-quality capture hash-binding audit | Boole found that provenance refs and direct arm payloads were still too self-attested: report arms could be accepted without raw capture file hashes, and provenance could name artifacts without binding to hashed refs. | Builder-eligible captures now require hashed training/pre-eval/post-eval/initial-checkpoint/final-checkpoint artifact kinds, provenance refs must match those artifacts, and final reports hash raw input capture files then re-derive arms from them. Focused ruff passed; builder/preview/contract suite `25 passed`; broader readiness slice `72 passed, 2 warnings`. Later 1024x8 stock/compact captures use this binding in the final matched canary. |
| F-095 | 2026-05-30 | Rawls/Godel/Sartre producer-map audit + main implementation | A capture-from-artifacts CLI is useful only if framed as a strict packager. It cannot be allowed to become "the producer" because it does not run stock `train_muzero`, compact-owned updates, or matched evals. | Added `scripts/build_compact_matched_learning_quality_capture_from_artifacts.py` with output-role boundaries and focused fail-closed tests. Documented the upstream stock and compact producer maps: stock runs real `train_muzero` plus immutable pre/post eval; compact loads the unified lifecycle checkpoint, runs real compact-owned updates, exports/evals pre/post checkpoints, and computes compact denominators before packaging. Ruff passed; packager tests `24 passed`; packager+builder+contract tests `51 passed`; broader readiness slice `98 passed, 2 warnings`. |
| F-096 | 2026-05-30 | Kant/Plato/Dirac producer audit + main implementation | The compact side can have a local producer scaffold now, but the stock side remains a Modal `train_muzero` orchestration problem. The compact scaffold must not launder a resident-sample no-movement run into quality evidence. | Added `scripts/build_compact_matched_learning_quality_compact_candidate_producer.py` with focused tests. It loads the unified lifecycle checkpoint, runs a real compact-owned resident-sample update, exports/evals pre/post stock-shaped checkpoints, writes packager inputs, and calls the strict packager. Real local eval200 attempt failed closed because both pre/post evals capped at 200 with no mean-survival movement. Later env/search/replay compact captures and the 1024x8 matched canary superseded this first scaffold. |
| F-097 | 2026-05-30 | Zeno/Arendt/Anscombe stock/compact producer reorientation + main implementation | The stock arm needed an actual Modal orchestration/materialization scaffold, while the compact scaffold needed stronger eval binding and a clear next upgrade to env/search/replay-owned rows. | Added `scripts/build_compact_matched_learning_quality_stock_reference_producer.py` and `tests/test_compact_matched_learning_quality_stock_reference_producer.py`; tightened compact producer eval binding against same-basename wrong-directory refs. The stock scaffold launches real stock `mode=train`, selects immutable checkpoints, downloads them, runs matched evals, derives denominators from learner metrics and target audit counts, and calls the strict packager. Ruff passed; stock+compact producer tests `18 passed, 2 warnings`; broader readiness-adjacent slice `116 passed, 2 warnings`; CLI `--help` passed. Later 1024x8 stock diagnostic produced the builder-eligible stock capture. |
| F-098 | 2026-05-30 | Real stock producer Modal attempts | The first real stock-reference producer attempts exposed operational truth rather than fake proof: imported `.remote()` functions were unhydrated, deployed trainer/status apps were stale, evaluator episode count could be invalid, eval seeds were not deterministic by default, v2 volume kwargs mattered, and immediate Modal train returns could be too slim for denominator extraction. | Deployed the current trainer/status apps and hardened the stock producer with `modal.Function.from_name`, v2 volume kwargs, deterministic `eval_seed_rng_seed`, fixed stock-train surface guards, evaluator episode-count validation, and durable summary-ref loading. Focused ruff passed; stock producer tests now pass `23 passed`; CLI `--help` passed. |
| F-099 | 2026-05-30 | Stock producer `smoke5` failed closed | A real stock train/eval/download/package attempt can reach strict packager inputs, but the tiny eval shape is saturated: pre `iteration_0` and post `iteration_124` both capped at 64 steps on two fixed seeds, producing zero mean-survival movement. | Preserve `artifacts/local/curvytron_compact_matched_learning_quality_results/optimizer-stock-reference-quality-producer-modal-current-smoke5-20260530/` as failed producer evidence. The later 1024x8 diagnostic is the stock capture that feeds the matched canary; do not hand-edit old failed artifacts into captures. |
| F-100 | 2026-05-30 | Carson the 2nd capture-boundary red team | The final matched-quality builder can still fail or be gamed if stock and compact arms have incompatible source fingerprints, if packager training artifacts are only hashed but not content-validated, if eval summaries are not strongly bound to checkpoint/settings, or if one tiny saturated seed set is accepted as quality. | Implemented added hardening requirements: source-fingerprint compatibility policy, training-artifact content validation, stronger eval/checkpoint/settings binding, and minimum seed/horizon evidence before final matched report closure. The 1024x8 canary now validates through that hardened path. |
| F-101 | 2026-05-30 | OPT-057 isolated live-run producer and real canary | The isolated live-run safety lane needed a real-run producer around the strict packager, and the default assignment should be a frozen checkpoint opponent against the uploaded stock-resume seed so assignment-provider proof can actually become true. The first real attempts also showed two useful wrinkles: generated assignment ids must satisfy deployed `clean_id`, and initial assignment application plus env telemetry is the right proof for this canary because the refresh hook can collide with open TensorBoard files. | Added `scripts/build_compact_promotion_isolated_live_run_safety_canary_producer.py` with upload, generated frozen-initial sandbox assignment/audit, deployed assignment writer call, tiny stock `train_muzero` launch, status/proof fetch, final checkpoint download, canary input derivation, and packager invocation. Produced `optimizer-compact-promotion-isolated-live-run-safety-canary-20260530d`: provider-ok rows `509`, learner calls `62`, collector env-step delta `250`, final `iteration_62.pth.tar`, fresh optimizer preserved, and all production-touch flags false. Ruff passed; adjacent readiness/assignment/status slice `63 passed, 2 warnings`; CLI `--help` passed. |
| F-102 | 2026-05-30 | OPT-058 sandbox assignment/rating proof | The assignment/rating readiness lane should prove local plumbing from rating snapshot to leaderboard snapshot to trainer assignment, not publish a public leaderboard or claim rating quality. The isolated live-run canary proves assignment load/apply, but it does not prove the rating-to-leaderboard-to-stable-assignment materialization path. | Added `curvyzero_compact_promotion_sandbox_assignment_rating_proof/v1`, a strict packager, local producer, and fail-closed tests. Produced `optimizer-compact-promotion-sandbox-assignment-rating-proof-20260530a`: one local tiny rated pair with five valid games, two active local leaderboard rows, unpublished pointer, `stable_slots_v1` assignment/audit with the candidate selected, parser/audit ok, compact checkpoint/export hashes bound, and all production live-run/intake/rating/leaderboard/control-pointer/public publish flags false. Ruff passed; readiness/assignment slice `69 passed, 2 warnings`. |
| F-103 | 2026-05-30 | OPT-059 longer-horizon compact metrics | The matched compact candidate producer was honest but pre/post-shaped; it could not prove a multi-checkpoint compact learning trace by itself. Another speed row would not close this gap. | Added `curvyzero_compact_longer_horizon_learning_metrics/v1` plus a compact-only producer that snapshots real compact env/search/replay training after learner updates, saves compact-native checkpoints and stock-shaped eval exports, runs per-checkpoint evals, validates strict checkpoint/eval binding, monotonic denominators, distinct model digests, trained-point metrics lineage, and non-claims. Produced `optimizer-compact-longer-horizon-learning-metrics-local-20260530a`: three checkpoints, two learner updates, `compact_rollout_rows=12`, `compact_sample_rows=4`, final mean survival `107.5 -> 117.5`, and all promotion/speed/live/rating/leaderboard/`train_muzero` claims false. Ruff passed; longer-horizon plus adjacent producer slice `15 passed, 2 warnings`. |
| F-104 | 2026-05-30 | OPT-060 final readiness-bundle review | The next honest artifact after the five lanes was a reviewer, not a promoter. It needed to bind real lane reports by hash/candidate id and make the matched-quality sufficiency limit explicit instead of laundering canary evidence into a promotion claim. | Added `curvyzero_compact_promotion_readiness_bundle_review/v1`, CLI, and fail-closed tests. Produced `optimizer-compact-promotion-readiness-bundle-review-20260530a`: compatibility, lifecycle, matched canary, pair verifier, stock resume/load, isolated live-run safety, sandbox assignment/rating, and longer-horizon metrics all validate under candidate `optimizer-compact-unified-lifecycle-smoke-20260530`; `ready_for_manual_review=true`, `promotion_claim=false`, `automatic_promotion_allowed=false`, and `matched_quality_sufficiency_decision=canary_scale_manual_acceptance_or_larger_study_required_before_promotion`. Ruff passed; bundle-review suite `6 passed, 2 warnings`; real report validated. |
| F-105 | 2026-05-30 | OPT-061 matched-quality sufficiency review | The OPT-060 packet left a real decision boundary: either explicit human acceptance of canary-scale evidence for a named non-production step, or a stronger same-surface study before promotion. Jumping straight to producers would hide that decision; claiming promotion would overstate 8x1024 mixed-hardware/local-CPU compact evidence. | Added `curvyzero_compact_matched_quality_sufficiency_review/v1`, CLI, and fail-closed tests. Produced `optimizer-compact-matched-quality-sufficiency-review-20260530a`: it hash-binds OPT-060 plus matched canary, pair verifier, longer-horizon metrics, and capture inputs; records current evidence limits; chooses `require_larger_same_surface_study`; attaches a 2048x32 same-surface plan; and keeps `promotion_claim=false`, `automatic_promotion_allowed=false`, `current_evidence_sufficient_for_promotion=false`. Ruff passed; focused suite `7 passed, 2 warnings`; real report validated. |
| F-107 | 2026-05-30 | OPT-063 residual-aware speed target | The initial OPT-062 `real_search_dominant` label was only true among a narrow set of named deltas. The real compact-vs-floor wall gap was `23.431s`, and raw `search_service_total_sec` explained only `3.027s`; the missing wall was already visible as `compact_rollout_slab_sec`. | Rebuilt the bundle with wall-aligned slab accounting. New read: `compact_rollout_slab_non_service_dominant`, slab delta `22.590s`, slab-minus-search delta `19.563s`, search gap share `12.9%`, `search_dominance_claim=false`, next target `decompose_compact_rollout_slab_commit_flush_materialization`. |
| F-108 | 2026-05-30 | OPT-064 local slab timer instrumentation | Once the residual target was named, the smallest honest next code change was not deep PUCT profiling; it was additive slab-envelope timers around the wall that dominates the same-denominator gap. | Added compact rollout slab internal timing telemetry for commit/flush, replay payload validation/materialization, replay-index row build/store, root-batch build, action dispatch/validation/assembly, pending store, and telemetry build. Hybrid profile aggregates those totals and the floor bundle will compare their deltas on the next H100 rerun. Ruff passed; `test_compact_search_replay_contract.py` passed `43 passed, 2 warnings`; targeted source-state slab tests passed `3 passed, 2 warnings`. |
| F-109 | 2026-05-30 | OPT-064 H100 dispatch-envelope decomposition | The slab non-service hypothesis was too broad. The H100 slab-timer rerun showed commit/flush/replay-index materialization were not dominant; the missing wall lived in `compact_rollout_slab_search_dispatch_wall_sec`. Service-envelope timers then proved the dispatch wall was inside `CompactTorchSearchServiceV1.run_action_step`, and `metadata_build_sec=9.769s` dwarfed actual `service_total_sec=3.086s`. | Added compact Torch action-envelope timers, promoted them through slab/profile/floor-bundle telemetry, and ran `optimizer-speed-row-compact-torch-h100-b1024a16-threshold-dispatchenv-20260530`. The row reported `4797.9` steps/sec, dispatch `12.913s`, service action wall `12.907s`, service total `3.086s`, metadata build `9.769s`. |
| F-110 | 2026-05-30 | OPT-064 policy-refresh metadata cache fix | `CompactTorchSearchServiceV1._policy_refresh_metadata()` recomputed `compact_model_state_digest_v1(self._model)` for every action step after refresh. That was a pure metadata tax in the hot path, not search, game mechanics, or replay. | Cached policy-refresh model digest and metadata at `refresh_model_state`; rebuilt the bundle dominance logic so nested `slab_internal_accounted_sec` cannot choose the wall target. Cached H100 row `optimizer-speed-row-compact-torch-h100-b1024a16-threshold-dispatchenv-cache-20260530` improved compact Torch to `9608.9` steps/sec, wall `19.182s`, metadata build `0.0034s`, dispatch `3.152s`. Cached bundle `optimizer-compact-speed-row-floor-bundle-dispatchenv-cache-20260530` reads `compact_rollout_slab_search_service_dominant`, ratio `1.270`, wall delta `4.079s`, search delta `3.042s`; next target is compact Torch model/search wall. Ruff passed; targeted suite `148 passed, 2 warnings`. |
| F-111 | 2026-05-30 | OPT-065 one-simulation fast path | The cached row was `num_simulations=1`, so the generic multi-simulation tree loop was doing unnecessary PUCT bookkeeping for a one-visit search. A fixed-shape one-simulation result can be computed by masked root-prior argmax plus one recurrent inference while preserving the same one-visit visit-policy/root-value semantics. | Added a sim1 fast path to `CompactTorchSearchServiceV1`, promoted fast-path/recurrent-call counters through slab/profile/bundle telemetry, and validated locally with ruff plus targeted tests `148 passed, 2 warnings`. Clean H100 row `optimizer-speed-row-compact-torch-h100-b1024a16-threshold-fastpath1sim-20260530` improved compact Torch to `10326.2` steps/sec, wall `17.850s`, service total `2.825s`, tree search `0.529s`, model `2.237s`; bundle `optimizer-compact-speed-row-floor-bundle-fastpath1sim-20260530` reads search-service dominant with ratio `1.182`. Promoted-counter row proved `fast_path_count=180`/`recurrent_calls=180` but was top-level noisy. F-112 supersedes the broad "model/recurrent" target with initial model sync/forward and phase-sync timing. |
| F-112 | 2026-05-31 | OPT-065 timing split and compile probe | The sim1 fast path still looked like model/search wall, but "model/recurrent" was too broad. Additive timers show the dominant measured bucket is initial inference sync/forward, not recurrent search or final tree sync. Warm default model compile currently fails closed with a Torch CUDAGraph overwritten-output error. | Added timing split telemetry through compact Torch search service, slab/profile promotion, floor-bundle buckets, and Modal array-ceiling telemetry. Canonical H100 row `opt065-h100-timing-eager-sim1-canonical-20260531` reported `12335.7` steps/sec, wall `14.942s`, service `2.808s`, model `2.249s`, tree `0.506s`, initial sync `1.905s`, recurrent enqueue `0.186s`, final tree sync `0.0048s`, `fast_path_count=180`, `recurrent_calls=180`; bundle `opt065-compact-speed-row-floor-bundle-timing-eager-sim1-canonical-20260531` keeps `engineering_read=compact_rollout_slab_search_service_dominant`. Next target is gated phase-sync/CUDA-event timing or safer compile mode, not GPU game mechanics. Ruff passed; targeted suite `289 passed, 2 warnings`. |
| F-113 | 2026-05-31 | OPT-065 timing-mode and CUDA-event baseline | The timing split still mixed host synchronization policy with GPU execution time. The safest next step was not to remove syncs first, but to add an explicit timing-mode gate and record CUDA events while preserving default host-phase sync behavior. | Added compact Torch timing modes `host_phase_sync`, `host_phase_sync_cuda_event`, `host_final_sync_only`, and `cuda_event_final_sync`; wired CLI/Modal/profile/floor-bundle telemetry; and ran H100 row `opt065-h100-timing-host-phase-cudaevent-sim1-20260531`. It records initial CUDA event `2.232s`, initial host sync `1.751s`, recurrent CUDA event `0.471s`, tree CUDA event `0.536s`, and tree host sync `0.0049s`, with `initial_sync_enabled=true`. Bundle `opt065-compact-speed-row-floor-bundle-host-phase-cudaevent-sim1-20260531` stays search-service dominant. At that point the next probe was a final-sync timing mode with hidden implicit readback syncs and fidelity watched; F-114 records the result. Ruff passed; targeted suite `249 passed, 2 warnings`; CLI help passed. |
| F-114 | 2026-05-31 | OPT-065 final-sync timing-mode probe | Removing the explicit initial host sync does not buy wall speed. In `cuda_event_final_sync` mode the initial host sync falls to `0.0s`, but the initial CUDA event stays `2.249s` and wall regresses to `20.387s`; host bucket accounting moves into downstream queued work/search timing. | Treat the old `1.905s` initial sync as mostly real initial model GPU execution rather than an avoidable CPU phase barrier. Row `opt065-h100-timing-cudaevent-finalsync-sim1-20260531` passed trap checks: event statuses recorded, fast-path/recurrent calls `180/180`, action override drops `0`, replay D2H `0`, action D2H `737280`. Bundle `opt065-compact-speed-row-floor-bundle-cudaevent-finalsync-sim1-20260531` still reads `compact_rollout_slab_search_service_dominant` with compact-vs-floor ratio `1.350`. Next target is model forward/compile-safe optimization, not more phase-sync deletion and not GPU mechanics. |
| F-115 | 2026-05-31 | OPT-065 safe model-compile-mode probe | The old model compile failure was mode-specific enough to isolate. Model inference compile needs its own mode knob so we can avoid CUDAGraph modes while leaving helper-kernel compile behavior unchanged. | Added `CompactTorchCompileConfig.model_compile_mode`, validated choices, wired speed-row CLI, Modal speed-row, hybrid profile, and grid builder, and covered propagation/caching in focused tests. H100 event row `opt065-h100-modelcompile-default-sim1-20260531` avoided the CUDAGraph overwrite and reduced initial CUDA event `2.232s -> 1.391s`, service `2.862s -> 1.916s`, recurrent CUDA event `0.471s -> 0.333s`; replay D2H stayed `0`, action D2H `737280`, fast-path/recurrent `180/180`. Canonical compile row `opt065-h100-modelcompile-default-canonical-sim1-20260531` passed too with service `1.806s`, model `1.324s`, search `0.424s`, but wall `17.967s` / `10258.6` steps/sec is not a win versus eager canonical `14.942s` / `12335.7`. Bundles now read `compact_rollout_slab_search_dispatch_wall_dominant` with residual accounting required. Compile default is safe and improves the model bucket; it is not yet a promoted end-to-end speed win. Ruff passed; targeted suite `227 passed, 2 warnings`; CLI help passed. |
| F-116 | 2026-05-31 | OPT-065 paired compile/eager rows and dispatch split | The first canonical compile row lost wall time, but fresh current-source same-denominator pairs favor compile. r1 compile `12450.3` vs eager `9840.1`; r2 compile `12563.9` vs eager `11520.7`, with replay D2H `0`, selected-action D2H `737280`, fast-path/recurrent `180/180`, and compile cache-hit. The remaining `search_dispatch_wall` headline needed a true residual split. | Added derived dispatch-envelope accounting to the compact slab/profile/floor bundle: dispatch residual, non-dispatch slab wall, non-action-service slab wall, and action-wall-over-total. Rebuilt four `-dispatchsplit-20260531` bundles; signed dispatch residual deltas are only `0.0032s` to `0.0038s`, so the dispatch headline is the compact Torch action-wall envelope, not unmeasured slab wrapper overhead. F-117 then added the comparator and blocked compile approval on action/trajectory mismatch. Ruff passed; changed/focused pytest passed; broad replay contract passed on rerun after one unrelated RND assertion failed once. |
| F-117 | 2026-05-31 | OPT-065 compile/eager pair comparator | The fresh compile rows are faster and pass same-denominator/safety gates, but they do not preserve the aggregate action/trajectory checksums. Treating that as a speed win would repeat the action-trajectory confounder. | Added `curvyzero_compact_compile_eager_speed_pair/v1` and `scripts/build_compact_compile_eager_speed_pair.py`. Real review `opt065-compile-eager-speed-pair-review-r1r2-20260531` reports `all_same_denominator=true`, `all_safety_checks_passed=true`, `all_compile_faster=true`, min wall win `0.083`, but `decision=not_approved_action_trajectory_mismatch` and `speed_claim_allowed=false`. Historical next target was compile/eager action drift; superseded by F-119/F-120, where the guard fixed current checked drift but speed stayed unapproved. Ruff passed; comparator/floor tests `7 passed`; broader changed slice `131 passed, 2 warnings`. |
| F-118 | 2026-05-31 | OPT-066 fixed-root model-compile gate | The same-root comparator no longer supports the theory that `model_compile_mode=default` inherently changes checked root outputs. On the H100 fixed-root smoke, eager primary and `model_compile_default` matched actions, visit policy, and root values exactly over 4 records / 1024 active roots. | Keep model compile as a candidate, but do not default it from fixed-root parity. The remaining question is closed-loop behavior and repeatable wall speed. Artifact: `artifacts/local/curvytron_hybrid_observation_profile_results/opt066-fixed-root-modelcompile-gate-smoke-20260531/row_001_result.json`. |
| F-119 | 2026-05-31 | OPT-066 post-guard closed-loop pair r1/r2 | Forcing compact Torch search into eval/inference mode during search appears to remove the scary closed-loop trajectory drift: eager and compile action/trajectory checksums match exactly in both post-guard pairs. The compile service/model buckets are lower in both pairs, but top-level wall is not repeatably better: r1 compile is much slower, r2 is essentially tied. | Treat the eval/inference guard as an important fidelity fix. Do not approve compile as a speed default. Compile is now a fidelity-safe service/model optimization candidate whose end-to-end speed value is unproven after the guard. Artifact: `artifacts/local/curvytron_compact_compile_eager_speed_pair_results/opt066-compile-eager-speed-pair-evalguard-r1r2-20260531/compile_eager_speed_pair_report.json`. |
| F-120 | 2026-05-31 | OPT-066 model-compile default decision artifact | The right closure was a policy decision artifact, not another reflexive H100 row. Fixed-root parity and post-guard closed-loop fidelity are both positive, but the post-guard pair review is `not_approved_compile_not_faster`; service/model buckets improve without a repeatable end-to-end wall win. | Added `curvyzero_compact_model_compile_decision/v1`, CLI builder, tests, and real report `artifacts/local/curvytron_compact_model_compile_decision_results/opt066-model-compile-default-decision-20260531/model_compile_decision_report.json`. Decision: `park_model_compile_default_speed_unapproved`; `model_compile_default_speed_default_allowed=false`; all promotion/speedup/live/`train_muzero` claims false. |
| F-121 | 2026-05-31 | OPT-067 larger matched-quality study plan artifacts | After OPT-061, the next useful step was not a promotion claim and not another speed row. The larger study needed an executable, hash-bound plan that keeps the current evidence boundary explicit: plan artifacts are not quality evidence, and the real gate still requires fresh stock and compact captures plus refreshed reviews. | Added `curvyzero_compact_matched_quality_larger_study_plan/v1` and `curvyzero_compact_matched_quality_larger_study_bundle/v1`, CLIs, and tests. Built `optimizer-compact-matched-quality-larger-study-plan-20260531` and refreshed the canonical bundle as `optimizer-compact-matched-quality-larger-study-bundle-canonical-20260531`. The standalone plan emits fresh `20260531` future run ids; the canonical bundle binds the refreshed sufficiency review's embedded six-step plan and post-step validators. Both keep `promotion_claim=false`, `automatic_promotion_allowed=false`, `fresh_outputs_produced=false`, and `stock_train_muzero_speedup_claim=false`. Ruff passed; focused matched-quality plan/bundle/preflight/readiness slice `26 passed, 2 warnings`; real reports validated. |
| F-122 | 2026-05-31 | OPT-069 larger matched-quality execution preflight | The first bundle was structurally useful but too easy to misread as execution evidence, and it still carried old embedded-plan drift until the sufficiency defaults were refreshed. The planned readiness refresh also needed explicit source report inputs so it could not fall through to stale builder defaults. | Added `curvyzero_compact_matched_quality_larger_study_preflight/v1`, CLI, and tests. Built `optimizer-compact-matched-quality-larger-study-preflight-20260531` bound to the canonical bundle. It reports recommended first wave `stock_reference_capture_producer` plus `compact_candidate_capture_producer`, rejects implicit readiness-refresh defaults, blocks canary/pair/readiness/sufficiency refresh until prior outputs exist, and keeps `commands_run_by_preflight=false`, `quality_evidence_produced=false`, `promotion_claim=false`, `automatic_promotion_allowed=false`, and `stock_train_muzero_speedup_claim=false`. |
| F-106 | 2026-05-30 | OPT-062 speed-row floor bundle | The accepted Coach speed row was honest speed evidence but still used the device-target service, so it could not answer whether the next wall was real search, game mechanics, observation refresh, or sample/learner gates. The right next artifact was a same-denominator sibling comparison, not GPU game-mechanics speculation. | Added backend selection to the speed-row producer, device replay payload support for `FixedShapeBatchedSearchOwnerV0`, `curvyzero_compact_speed_row_floor_bundle/v1`, CLI, and fail-closed tests. Produced H100 siblings and bundle `optimizer-compact-speed-row-floor-bundle-20260530`: accepted device-target `10018.5`, compact Torch `5005.0`, fixed-shape floor `13759.0` compact trainer env steps/sec under the same B1024/A16/180 denominator. Trajectory checksums differ; all promotion/speedup/live/rating/`train_muzero` claims false. Ruff passed; focused suite `31 passed, 2 warnings`; real bundle validated. |
| F-123 | 2026-05-31 | OPT-068 larger matched-quality execution | The larger study is no longer just plan/preflight evidence. The compact capture succeeded, the canonical stock producer failed closed before packaging on a LightZero evaluator empty-ready-set edge, and the explicit `evalenv32` stock rerun succeeded on the same 32-episode eval surface. | Built refreshed larger canary, pair verifier, readiness bundle, and sufficiency review. Larger 32x2048 movement: stock `147.21875 -> 152.28125` (`+5.0625`), compact `151.90625 -> 157.34375` (`+5.4375`), compact-minus-stock `+0.375`. The sufficiency review accepts this only for `larger_32x2048_matched_quality_manual_review_not_promotion`; promotion, automatic promotion, speedup, live-run, rating, and leaderboard claims remain false. |
| F-124 | 2026-05-31 | Compact denominator verifier contract | The pair verifier's old compact denominator rule confused replay/sample rows with learner gradient updates. In compact-owned training, `learner_update_count_delta` can exceed `compact_sample_rows` when `train_steps>1`, so requiring sample rows to cover learner updates was the wrong invariant. | Patched `curvyzero_compact_matched_learning_quality_pair_verification/v1` to require rollout rows cover sample rows, sample rows cover sample batches, and learner updates cover sample batches. Added `learner_updates_per_sample_batch` plus contract text. Focused verifier tests passed `23 passed`; broader matched-quality/readiness slice passed `49 passed, 2 warnings`; ruff and `git diff --check` passed at validation time. |
| F-125 | 2026-05-31 | OPT-070 larger matched-quality manual review | The right closure for the 32x2048 packet was a manual decision artifact, not a promotion. The evidence is positive but small, mixed-hardware, compact-local-CPU on the compact side, and dependent on an explicit `evalenv32` stock workaround after the canonical stock producer failed closed. | Added `curvyzero_compact_matched_quality_manual_review/v1`, CLI `scripts/build_compact_matched_quality_manual_review.py`, and tests. Produced `artifacts/local/curvytron_compact_promotion_readiness_results/optimizer-compact-matched-quality-manual-review-larger-2048x32-env64train8-20260531/manual_review_decision_report.json`: `manual_review_decision=keep_compact_candidate_only`, `next_allowed_step=return_to_engineering_speed_and_stock_evaluator_hardening`, promotion/automatic-promotion/live/rating/leaderboard/stock-speedup/compact-superiority claims false. Focused ruff passed; manual-review tests passed `9 passed, 2 warnings`; real report validated. |
| F-126 | 2026-05-31 | OPT-071 stock evaluator workaround hardening | The canonical larger stock producer failed because the LightZero internal evaluator ran 32 episodes with `evaluator_env_num=2`; the successful substitute used explicit `evalenv32`. Leaving future plans to rely on producer defaults would recreate the same failure. | Added local fail-closed stock evaluator requirements: the stock producer rejects hidden evaluator-width drift, the standalone larger study plan and sufficiency embedded plan emit and validate `--evaluator-env-num == --n-evaluator-episode == eval_seed_count`, generated larger stock run ids include `-evalenvN`, and the larger-study bundle/preflight surfaces expose and validate `stock_evaluator_requirement`. Focused ruff passed; focused matched-quality suite `56 passed, 2 warnings`. This formalizes the workaround only; no promotion, speedup, or upstream LightZero evaluator-fix claim. |
| F-127 | 2026-05-31 | OPT-072 compile/eager eval-inference guard | The post-guard compile/eager pair is fidelity-positive, but old pre-guard rows are still dangerous if a later reviewer forgets why they were rejected. The speed-pair contract needed to require the actual eval/inference-mode guard telemetry, not just same denominator, D2H budgets, and trajectory checks. | Added `inference_guard_telemetry` to `curvyzero_compact_compile_eager_speed_pair/v1` row entries and made row safety require observed/restored model training state, `model.eval()` application, and `torch.inference_mode()` usage. Added a regression proving missing pre-guard telemetry yields `not_approved_safety_failed`. Focused ruff passed; `tests/test_compact_speed_row_floor_bundle.py` passed `12 passed`. |
| F-128 | 2026-05-31 | OPT-073 compact Torch sim1 action-path audit | In one-simulation, zero-root-noise compact Torch search, the selected action is invariant under root-prior softmax because the visit policy is one-hot and action selection is argmax over legal actions. The action readback also reported `int16` bytes after host casting while the transferred tensor was still `argmax` long, and `run_action_step()` rebuilt fixed-shape masks for compile eligibility after already building them. | Added a zero-noise sim1 fast path that selects directly from masked logits, records `compact_torch_search_one_simulation_root_prior_softmax_skipped` and `selection_mode`, promotes those fields through slab telemetry, casts selected actions to `int16` before readback, and reuses already-built masks for compile eligibility. Local ruff passed; focused compact Torch/slab tests passed `26 passed, 2 warnings`. F-129 records the H100 telemetry result. |
| F-129 | 2026-05-31 | OPT-073b H100 telemetry bundle | The local OPT-073 action-path patch is real on H100, but the canonical same-denominator telemetry bundle does not support a wall-speed claim. Row `opt073-h100-eager-sim1-canonical-20260531b` validates fast-path and softmax-skip counts `180/180`, selection mode `masked_logits_argmax`, `int16` action-readback byte match, action D2H `737280`, replay D2H `0`, committed replay D2H `0`, resident CUDA observation with no host fallback, and no compile/model-compile request. It ran at `9394.0` steps/sec / `19.621s`; the bound bundle compares that to fixed floor `12203.9` steps/sec / `15.103s`, wall ratio `1.299`, with trajectory checksums still different. The first OPT-073 row was faster (`12428.4` steps/sec) but lacked aggregate softmax-skip projection. | Treat OPT-073b as telemetry validation only. Keep compact Torch service/action/model/search as the lane, but do not claim speed. The next step is a post-OPT-073 service-bucket decision packet: repeat or current CUDA-event timing rows only if they separate noise from real model/recurrent/replay/sample buckets. GPU mechanics and recurrent deferral stay parked until those buckets justify them. |
| F-130 | 2026-06-02 | OPT-119/120 owner-search totals row | The index-only replay append payload fix is real, but the owner-search route still loses badly. The totals row `opt120-h100-owner-search-totals-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `3888.48` env steps/sec with `33.52s` compact rollout slab. That slab is mostly `31.34s` owner parent wait; worker time inside includes root resolve `7.15s`, replay append `6.57s`, learner train `3.20s`, search `2.36s`, and refresh `0.88s`. Parent replay-row build/store is tiny. | Surface slab totals in summary/compact/report and select the next implementation target: split action-critical search from owner replay append/train/refresh maintenance, with fail-closed final drain, replay-order checks, policy-lag tracking, and exception propagation. Bigger GPUs, GPU mechanics, and PufferLib ports remain parked because H100 utilization is still about `5%`. |
| F-131 | 2026-06-02 | OPT-121 deferred owner maintenance proof | Deferred owner maintenance works and improves the route, but does not solve the owner wait. H100 row `opt121-h100-owner-search-deferred-maintenance-samework-b1024a1-normal-unroll2-speed-20260602-r2` ran at `4658.42` env steps/sec with `39.567s` wall, preserved owner appends/train/updates `224/28/28`, parent sample/learner `0/0`, final pending owner work `0`, final policy lag `0`, and normal-death proof. Owner parent wait still totaled `22.52s`; `51` actor steps saw maintenance pending. | The next implementation is not more payload trimming. Replace the single FIFO process-worker handoff with a persistent priority owner loop that serves search before maintenance while keeping maintenance ordered, fail-closed, drained to zero, and counted in the denominator. |
| F-132 | 2026-06-02 | OPT-121 local priority owner loop proof | The FIFO process-worker handoff has a local replacement. Row `opt121-local-owner-search-priority-loop-smoke-20260602-r1` passed with loop kind `persistent_priority_owner_loop_v1`, action priority enabled, `16` action requests, `5` maintenance requests, `11` actions while maintenance was pending, `11` actions served before maintenance completed, FIFO-blocked actions `0`, owner append/train/update completion, final pending work `0`, and final policy lag `0`. | This is local correctness and proof-surface evidence only. The required next evidence is the same-work H100 priority-loop row against OPT-104. |
| F-133 | 2026-06-02 | OPT-121 H100 priority owner loop row | The persistent priority owner loop works on H100 and improves the route again, but it still does not beat OPT-104. Row `opt121-h100-owner-search-priority-loop-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `5853.08` env steps/sec with `31.491s` wall. It preserved owner appends/train/updates `224/28/28`, parent sample/learner `0/0`, request/result CUDA tensors `0/0`, model-state bytes `0`, terminal sample/target rows `42/42`, normal-death gate true, final pending work `0`, and final policy lag `0`. It proved priority with `65` actions served while maintenance was pending and FIFO-blocked actions `0`. | Priority scheduling is a real improvement (`4658.42 -> 5853.08`) but not the finish. The next target is the remaining measured owner parent wait `17.476s` and owner worker work: root resolve `4.418s`, replay append `4.890s`, learner train `7.725s`, search `2.515s`, and refresh `1.215s`. |
| F-134 | 2026-06-02 | OPT-122 local owner-search bucket cuts | Three targeted local fixes now pass: owner search-result payloads use NumPy ndarray IPC instead of nested Python tuples, the owner resident bridge stores terminal final observations as sparse env rows instead of dense full-batch clones, and deferred owner maintenance coalesces to train boundaries or final drain. Local row `opt122-local-owner-search-ipc-sparse-coalesce-smoke-20260602-r1` preserved owner appends/train/update/refresh `15/3/3/3`, parent sample/learner `0/0`, final pending work `0`, final policy lag `0`, action-priority proof, and FIFO-blocked actions `0`; maintenance requests fell `5 -> 2` versus the previous local priority-loop shape. | This is local wiring proof only. The next evidence is a same-work H100 owner-search row against OPT-104 preserving `224/28/28`, normal-death terminal proof, final pending `0`, final policy lag `0`, and reporting parent wait/result bytes/root-resolve/replay/train/refresh buckets. |
| F-135 | 2026-06-02 | OPT-122 H100 bucket cuts row | The IPC/sparse/coalescing fixes are H100-proven and improved the route, but still did not beat OPT-104. Row `opt122-h100-owner-search-ipc-sparse-coalesce-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `6639.26` env steps/sec with `27.762s` wall. It preserved owner appends/train/updates `224/28/28`, parent sample/learner `0/0`, request bytes `138733`, result bytes `115213`, model-state bytes `0`, request/result CUDA tensors `0/0`, terminal sample/target rows `42/42`, final pending owner maintenance `0`, owner policy lag `0`, and normal-death gate true. | OPT-122 reduced parent wait `17.476s -> 10.872s`, maintenance requests `173 -> 25`, and result bytes `187414 -> 115213`, but the full loop still loses. Do not keep polishing this lane. The next target is to remove full search arrays from the parent action response and let the owner materialize replay. |
| F-136 | 2026-06-02 | OPT-123 local action-only owner result proof | The parent action response can be action-only while the owner still appends replay and trains. Local row `opt123-local-owner-action-only-materialized-replay-smoke-20260602-r2` passed with `search_result_payload_bytes=0`, `visit_policy_bytes=0`, `root_value_bytes=0`, `parent_reconstructed_search_result=false`, `owner_materializes_replay=true`, result bytes `334`, owner appends/train/update/refresh `15/3/3/3`, parent committed index rows `0`, final pending work `0`, and final policy lag `0`. | The first OPT-123 H100 attempt failed before speed reporting because the normal-death gate could not see `terminal_rows_verified`. Code now keeps owner-materialized resident replay as device replay rows and direct deferred `run()` fails closed instead of returning a synthetic full search result. The next proof is the patched same-work H100 row. |
| F-137 | 2026-06-02 | OPT-123 H100 action-only owner result row | The action-only owner result path is H100-proven and improved the route, but still did not beat OPT-104. Row `opt123-h100-owner-action-only-materialized-device-replay-samework-b1024a1-normal-unroll2-speed-20260602-r3` ran at `7679.53` env steps/sec with `24.001s` wall. It preserved owner appends/train/updates/refresh `224/28/28/28`, parent sample/learner `0/0`, request/result CUDA tensors `0/0`, model-state bytes `0`, terminal sample/target rows `42/42`, final pending owner work `0`, owner policy lag `0`, and normal-death gate true. Result bytes fell `115213 -> 4444`, request bytes fell `138733 -> 32779`, and search payload/visit/root bytes fell to `0/0/0`. | OPT-123 is progress, not the win. The next target is not parent result payloads; it is the remaining owner path: parent wait `9.219s`, root resolve `5.299s`, replay append `8.983s`, learner train `8.593s`, and refresh `1.333s`. |
| F-138 | 2026-06-02 | OPT-124 local inner two-phase owner proof | The owner can use the inner compact Torch two-phase action path for resident roots locally. Local row `opt124-local-owner-inner-two-phase-device-replay-smoke-20260602-r2` passed with action-only result true, owner materializes replay true, inner two-phase action step true, deferred inner device replay payload true, parent reconstruction false, request/result bytes `2513/334`, payload/visit/root bytes `0/0/0`, owner appends/train/update/refresh `15/3/3/3`, final pending `0`, and final policy lag `0`. | Local proof did not predict speed. F-139 rejects this path on H100, so it stays default-off. |
| F-139 | 2026-06-02 | OPT-124 H100 inner two-phase rejection | The inner two-phase owner replay path passed correctness and regressed badly. Row `opt124-h100-owner-inner-two-phase-device-replay-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `3884.97` env steps/sec with `47.444s` wall. It preserved action-only result shape, owner materialized replay, owner appends/train/updates/refresh `224/28/28/28`, parent sample/learner `0/0`, terminal sample/target rows `42/42`, normal-death gate true, final pending `0`, and policy lag `0`, but parent wait rose to `21.455s`, root resolve to `15.801s`, replay append to `28.825s`, learner train to `14.267s`, and H100 utilization fell to `4.54%/25%`. | Reject OPT-124 as a speed path. Keep it default-off. Use OPT-123 as the current bucket map and attack owner root/replay construction or train scheduling next. |
| F-140 | 2026-06-02 | OPT-125 local resident replay step cleanup | Owner-search resident replay steps were still copying full host observations into the step object even when resident-device observations were available for sampling. That copy is unnecessary for resident replay and makes slow fallback easier to hide. | Patched `_OwnerSearchReplayStoreFactorySidecarV1._step_from_root_batch` so resident roots keep `resident_observation_replay_snapshot` and set `observation=None`; added a unit test proving resident steps omit the host observation copy. Ruff passed; the new test passed; `tests/test_compact_owner_search_service.py` passed `22 passed, 2 warnings`. This is not speed evidence; the next decision is a same-work H100 row. |
| F-141 | 2026-06-02 | OPT-125 H100 row | The resident replay step host-copy elision was not a full-loop speed win. Row `opt125-h100-resident-owner-replay-step-no-host-copy-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `6593.40` env steps/sec with `27.955s` wall. Correctness held: owner appends/train/updates/refresh `224/28/28/28`, parent sample/learner `0/0`, terminal sample/target `42/42`, normal-death true, final pending/policy lag `0/0`. Replay append improved `8.983s -> 2.306s`, but learner train rose to `13.628s` and wall regressed. | Reject OPT-125 as the speed win. Keep the clue that replay materialization had real copy waste, but do not keep polishing that exact cut. |
| F-142 | 2026-06-02 | OPT-126 H100 row | The replay successor-window cache also failed as a full-loop speed row. Row `opt126-h100-replay-successor-window-cache-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `6591.11` env steps/sec with `27.965s` wall. Correctness held, but parent wait rose to `11.310s`, root resolve to `6.662s`, replay append was `4.215s`, learner train `10.564s`, and GPU mean/max was `7.36%/30%`. | Reject OPT-126 and stop small replay-sampling cache work unless a future row makes sampling setup the measured blocker again. The current issue is still the owner action/root/replay/train boundary. |
| F-143 | 2026-06-02 | OPT-127 local root/replay host-copy cut | The owner resident path still copied the full host observation during root resolution and again during replay materialization. Action-only results also hid resident bridge metadata in summaries because full search payload metadata was omitted. | Patched resident root resolution to skip the full host observation copy, patched CPU resident tests to clone snapshots, patched resident replay materialization to avoid the extra host observation copy, and carried `search_result_metadata` separately so action-only rows can surface resident bridge and host-copy proof fields. Ruff passed; `tests/test_compact_owner_search_service.py` passed `22 passed, 2 warnings`; focused speed-row smoke passed. This is local correctness only; OPT-127 H100 is the next decision. |
| F-144 | 2026-06-02 | OPT-132 long-window stability pair | The accepted `180/45` window was too short to trust by itself, so the launcher/comparator now support diagnostic `724/180`, `1084/270`, and `1444/360` windows. The first valid `724/180` pair matched exact work identity but still swung hard: K `7365.79` steps/sec / `100.651s`, L `8297.18` / `89.353s`, wall delta `11.298s`. | Treat this as a measurement-stability failure, not a speed-code target. Do not claim a stable speedup. Later evidence superseded the old next action: five `724/180` exact rows failed stability, and bounded `1084/270` still OOMed. Next target is memory-stable diagnostics. |
| F-145 | 2026-06-02 | Stable-claim comparator tightening | The comparison script could have treated a single fast baseline row as enough for `stable_speed_claim_allowed` if all candidates were mismatched, and it did not block stable claims on accepted-fast-path violations. That is the same class of evidence bug that caused earlier confusion. | Patched `scripts/compare_compact_coach_speed_rows.py` so stable claims require at least one exact candidate repeat and no accepted-fast-path violations across exact rows. Added regression tests. Focused ruff passed; comparator tests passed `6 passed`; launcher/comparator slice passed `51 passed, 2 warnings`. |
| F-146 | 2026-06-02 | OPT-132 1444/360 and 1084/270 H100 memory failures | Running `stability_1444_360` diagnostics on H100 failed before valid rows. A smaller `stability_1084_270` pair also failed. Modal logs showed CUDA OOM, with PyTorch around `78.34-78.36 GiB` allocated on an H100. This is a memory-budget failure for one-shot longer diagnostic shapes, not a speed result. | Keep `stability_1084_270` support for larger-memory diagnostics, but do not rerun it unchanged on H100. Added an `env_steps_collected == batch_size * measured_steps` guard so longer windows cannot silently change the speed denominator. Later evidence superseded repeated `724/180`: the next measurement needs memory-stable diagnostics. |
| F-147 | 2026-06-02 | OPT-132 five-row 724/180 A/A failure | Repeating the memory-safe `stability_724_180` row did not bound measurement noise. K/L/Q/R/S all matched exact work identity and had no accepted-fast-path violations, but wall ranged `89.353s -> 137.297s`; sample gate ranged `44.170s -> 77.233s`; learner-batch build ranged `25.023s -> 43.698s`. | Stop repeating `724/180` unchanged and do not patch normal speed code yet. Superseded by OPT-132W: `1084/270` now fits, but timing stability still fails. The active target is direct sample-gate learner-batch-build isolation. Comparator artifacts now include exact-repeat spread summaries. |
| F-OPT132AV-LOCAL-BUILDER-CHILD-CPUTIME | 2026-06-03 | OPT-132AV local in-process builder-child CPU-time diagnostic | External perf counters are unavailable in Modal, so the remaining user-CPU-backed swing needs in-process attribution. | Added process/thread CPU-time delta fields for learner-batch builder group-loop/accounted/residual, terminal metadata children, unroll fields, write-output, order-restore, finalize-outputs, metadata-sync, and metadata-build. Projected through source profile trace records, speed-row summary/compact payload, local/remote Modal reports, and comparator timings. Ruff passed; focused profile/smoke/comparator tests passed; broader smoke/compare pytest passed `78 passed, 2 warnings`. Diagnostic only. |
| F-OPT132AV-H100-BUILDER-CHILD-CPUTIME | 2026-06-03 | OPT-132AV H100 builder-child CPU-time r1/r2 read | Exact generic no-sampler `1084/270` still does not repeat. R1/R2 kept exact identity and report violations `[]`, but wall spread was `15.03%`: `190.101s / 220.991s`; sample gate `114.928s -> 135.240s`; learner-batch build `66.229s -> 82.661s`; builder group-loop `65.505s -> 81.792s`. | The builder group-loop wall delta is mostly process CPU: group-loop process CPU rose `+14.88s` against `+16.287s` wall. The largest moving child CPU bucket is terminal metadata `+6.37s`, with terminal final-observation `+2.67s`; residual CPU also moved `+4.28s`; unroll fields moved less at `+2.76s`. No speed claim. Next target: explain terminal/final-observation and residual CPU under exact replay/sample identity. |
| F-OPT132AW-LOCAL-FINALOBS-RESIDUAL-SPLIT | 2026-06-04 | OPT-132AW local final-observation/residual split diagnostic | OPT-132AV left two ambiguous buckets: terminal final-observation CPU and group-loop residual CPU. | Added group-loop prepare and terminal-value bookkeeping wall/CPU fields; split terminal final-observation into presence, selected-current-observation, and final-gather wall/CPU fields; added low-overhead branch/storage proof counters for terminal final-observation work. Projected through trace records, speed-row summary/compact payload, Modal reports, and comparator timing fields. Ruff passed; focused source-profile/smoke/comparator pytest passed `189 passed, 2 warnings`. Diagnostic only; superseded by the AW H100 read; no speed claim. |
| F-OPT132AW-H100-FINALOBS-RESIDUAL-SPLIT | 2026-06-04 | OPT-132AW H100 final-observation/residual split r1/r2 read | Exact generic no-sampler `1084/270` still does not repeat. R1/R2 kept exact identity and report violations `[]`, but wall spread was `7.318%`: `250.183s / 232.521s`; sample gate `157.724s / 146.441s`; learner-batch build `94.924s / 88.854s`; builder group-loop `94.002s / 87.987s`. | Branch/storage proof was identical: `399` terminal-final-observation groups, `0` index-fast-path, `399` fallback, final-row sum/max `512/4`, sparse storage `399`, sparse-row sum/max `3102/16`. New accounting reduced unexplained residual CPU movement (`14.45s / 14.07s`), while unroll fields (`38.18s / 34.98s`), terminal metadata (`24.01s / 21.75s`), terminal final-observation (`14.78s / 13.35s`), and final gather (`6.84s / 5.95s`) still moved. No speed claim. Historical target superseded by AX/AZ; live target is the guarded learner-ready resident replay / unroll-2 cache path. |
| F-OPT132AX-LOCAL-FINALOBS-VALIDATE-ONLY | 2026-06-04 | OPT-132AX local validate-only terminal-final-observation cleanup | AW proved grouped learner terminal-final-observation fallback work is identical across repeats, and the materialized tensor was discarded in the grouped learner path. | Added `_validate_resident_final_observation_for_rows` and used it only in the grouped learner fallback path; sample paths still materialize because they write `next_observation`. Added `terminal_metadata_final_observation_validate` wall/per-call/process/thread CPU fields, validate-only/materialized proof counters, and optional comparator identity for terminal-final-observation proof counters. Added a regression that monkeypatches the old materializer to raise while grouped learner still succeeds. Ruff passed; focused source-profile/smoke/comparator pytest passed `190 passed, 2 warnings`. Superseded by the AX H100 read; no speed claim. |
| F-OPT132AX-H100-FINALOBS-VALIDATE-ONLY | 2026-06-04 | OPT-132AX H100 validate-only terminal-final-observation r1/r2 read | The cleanup worked semantically but did not solve measurement stability. R1/R2 exact generic no-sampler `1084/270` kept exact identity, optional terminal-final-observation proof fields were present, and report violations were `[]`, but wall spread was `43.864%`: `168.280s / 262.830s`. | Proof was exact: groups/fallback/validate-only `399/399/399`, materialized `0`, final-row sum/max `512/4`, sparse storage `399`, sparse-row sum/max `3102/16`; select-current/gather wall and CPU were `0`. The slow row moved builder group-loop process CPU `+42.83s`, unroll-fields CPU `+20.29s`, terminal metadata `+9.75s`, and residual CPU `+6.60s`. No speed claim. Next target: split unroll-fields CPU and broader builder group-loop CPU; final-observation materialization is no longer the blocker. |
| F-OPT132AY-LOCAL-UNROLL-SUBCPU | 2026-06-04 | OPT-132AY local unroll sub-CPU diagnostic | AX removed the discarded final-observation materialization but left unroll-fields CPU opaque. | Added process/thread CPU-time deltas for existing unroll wall sub-timers: identity, stack fields, mask build, terminal value, mask apply, and action stack. These are nested attribution fields and are not added to group-loop accounted CPU. Projection covers source profile trace records, speed-row summary/compact payload, local/remote Modal reports, and comparator timing fields. Validation passed: ruff; focused source-profile/smoke/comparator pytest `190 passed, 2 warnings`; local fused unroll-2 smoke emitted nonzero nested CPU fields. Diagnostic only. |
| F-OPT132AY-H100-UNROLL-SUBCPU | 2026-06-04 | OPT-132AY H100 unroll sub-CPU r1/r2 read | The unroll split worked but exact same-work timing remains unstable. R1/R2 exact generic no-sampler `1084/270` kept exact identity and stable speed claim false; wall was `237.752s / 183.386s` (`25.819%` spread), sample gate `148.271s / 110.841s`, learner-batch build `91.035s / 65.376s`, and builder group-loop `89.834s / 64.694s`. | Group-loop process CPU was `85.30s / 62.04s`; accounted/residual CPU `76.22s / 55.78s` and `9.08s / 6.26s`; terminal metadata CPU `17.77s / 12.02s`; unroll-fields CPU `43.86s / 34.14s`. Unroll sub-CPU split: identity `0.39s / 0.23s`, stack fields `7.76s / 6.29s`, mask build `10.08s / 8.47s`, terminal value `8.51s / 6.55s`, mask apply `3.25s / 2.29s`, action stack `1.97s / 1.31s`; sub-sum `31.96s / 25.14s`. No speed claim. Next target: remaining unroll residual plus group-loop prepare, terminal metadata, and broad sample-gate CPU. |
| F-OPT132AZ-LOCAL-REMAINING-BUILDER-CPU | 2026-06-04 | OPT-132AZ local remaining builder-CPU diagnostic | AY split unroll subchildren but left remaining unroll residual, group-loop prepare, and terminal metadata opaque enough that another targeted read was needed before changing strategy. | Added nested timing/CPU fields for group-loop prepare snapshot/index/observation and accounted/residual, terminal metadata accounted/residual plus final-observation storage, and unroll accounted/residual plus builder-select and row-index prep. These are diagnostic/projection fields and not new group-loop accounted children. Validation passed: ruff; focused source-profile/smoke/comparator pytest `190 passed, 2 warnings`; local smoke emitted the new fields. Diagnostic only. |
| F-OPT132AZ-H100-REMAINING-BUILDER-CPU | 2026-06-04 | OPT-132AZ H100 remaining builder-CPU r1/r2 read plus sidecar step-back audit | Exact same-work H100 timing remains unstable after the final attribution split. R1/R2 exact generic no-sampler `1084/270` kept identity exact and stable speed claim false, but wall was `217.391s / 180.892s` (`18.329%` spread), sample gate `136.622s / 109.260s`, learner-batch build `85.001s / 65.049s`, and builder group-loop `84.177s / 64.278s`. | Group-loop process CPU was `80.37s / 61.70s`; prepare CPU `10.44s / 8.33s`; terminal metadata CPU `15.58s / 11.44s`; unroll-fields CPU `42.13s / 32.24s`; unroll accounted/residual CPU `30.29s / 24.21s` and `11.84s / 8.03s`. Runtime-step sum still explains the wall delta, and the main movement is broad user-CPU builder work. Sidecars agreed pure attribution has diminishing returns. Decision: stop adding pure timers and implement a guarded learner-ready resident replay path or batch-level vectorized unroll-2 builder before the next exact `1084/270` repeat. |
| F-OPT132-R29-R34-VECTORRNG-THREADED | 2026-06-05 | Vectorized reset RNG plus refresh-4 threaded/background owner maintenance | The project now has a modest, explained H100 repeat speedup over OPT-104, but not the near-target `2x`. Vectorized reset RNG fixed a real actor/autoreset hot path; matching OPT-104 refresh interval `4` fixed the comparison shape; threaded/background owner maintenance stabilized the remaining slab cadence better than inline. | Code patch: `src/curvyzero/env/vector_source_random.py` vectorizes `seeded_source_math_random_history` with exact scalar parity. r30 inline refresh4 won once (`14230.53 env/s`) but r31 failed repeat (`11794.65`) while reset stayed fixed, pointing at owner/slab dispatch. r32/r33 threaded refresh4 repeated above OPT-104 (`14002.12`, `13640.60`), and r34 2x-long stayed positive (`13145.34`, wall `112.797s` vs baseline-equivalent `116.850s`). Proof stayed clean: tensor fallback `none`, terminal sample/target present and equal, accepted/unroll2 violations `[]`. Next work is headroom from `1.04x-1.10x` toward `2x`, not another proof-only repeat. |

## 2026-06-07 Addendum

| Id | Date | Topic | Finding | Decision |
| --- | --- | --- | --- | --- |
| F-OPT132-OWNER-DEFERRED-ONE-SIM-PROOF | 2026-06-07 | Owner deferred one-simulation replay proof | The missing `2x` is not explained by random variance alone. The fastest same-work H100 row already has action-only owner transport and zero root/visit payload bytes; replay/sample removal projects just under `2x`, so the remaining target must include non-replay owner/root/search/parent-wait/learner-publication surfaces. | Implemented a default-off local proof lane for compact Torch one-simulation replay deferral through the owner boundary. The owner service and direct transition-batch replay store now aggregate flush-time proof fields, fail closed on identity drift or refresh crossing, wire the flag through local/Modal launchers and evidence summaries, and validate with focused pycompile/ruff/pytest. This is a local proof closure only; next speed-currency action is an end-to-end flagged local speed-row smoke before any H100 spend. |
| F-OPT132-LOCAL-DEFERRED-SMOKE-R7 | 2026-06-07 | Local flagged owner/deferred smoke | The r1-r6 local smoke failures were productive proof/dataflow failures: inference tensors entered learner replay, digest strings entered numeric telemetry totals, unflushed deferred handles crossed refresh, and the validator confused direct current-action staging with owner-final pending replay. None of these were speed evidence or variance conclusions. | r7 passed locally with the default-off deferral flag, direct transition-batch replay, owner search, and compact Torch inner search. Direct proof counts were `12/12/12/12`, refresh-crossed `0`, replay D2H `0.0`, and owner-inner pending final `0`. This makes one same-work H100 probe permissible, but the architecture priority remains fixed owner buffers/root/search/readout because this local proof alone cannot reach `2x` or `10x`. |
| F-OPT132-DIGEST-DEFERRAL-MODAL-PLUMBING | 2026-06-07 | Owner-ref digest deferral Modal launch surface | The owner-ref digest-deferral mechanism was locally proof-clean, but the H100 Modal app entrypoint did not yet accept or persist the `owner_search_defer_model_state_digest_to_refresh` flag. That is a launch-surface bug, not speed evidence. | Patched the Modal producer signature/config/projection and launcher test fixture so remote result bundles report `owner_search_defer_model_state_digest_to_refresh_requested`. Validation passed: ruff on touched launcher/producer/test files, focused launcher/remote projection tests `5 passed`, and full speed-row smoke suite `90 passed`. Any H100 digest-deferral row can now be read as testing the requested mechanism instead of a silently incomplete config. |
| F-OPT132-FIXED-SOA-SLOTCANDIDATE-H100 | 2026-06-07 | Fixed-SoA slot-candidate support row | The slot-candidate path is H100 proof-clean but not a speed path. Row `opt132-h100-slotcandidate-support-b1024a1-normal-unroll2-m724-w180-r1-20260607` ran `10348.87 env/s` with `71.638s` wall, only `0.816x` OPT-104 and `0.653x` columnar r2. Proof was clean: normal-death gate true, accepted/tensor-native/cache/unroll violations `[]`, fixed SoA requested/used true, fallback `0/none`, object/table counts `0`, direct replay `724 -> 181 -> 181`, columnar append false, search payload bytes `0`. | Close fixed-SoA exact/locality/slot-candidate as standalone H100 speed lanes. The row spent `4.083s` in fixed-SoA work, mostly successor-index `4.080s`, and still left broad owner/sample/train surfaces. Keep this only as support inside a broader fixed owner-buffer/root/search/learner-publication patch. |
| F-OPT132-OWNER-ROOT-CACHE-SLICE-LOCAL | 2026-06-07 | Deferred owner-maintenance root-cache snapshot slicing | Owner-search deferred maintenance was still copying the whole recent root-batch cache when staging work, and `_append_replay` copied that cache again before direct transition-batch append. That is exactly the kind of Python-object fanout the owner-boundary plan is trying to shrink. | Patched `CompactOwnerSearchServiceV1.run_action` to stage only root batches referenced by replay `record_indices`, `next_record_indices`, and the current actor step when the replay entries are introspectable; opaque entries fall back to a full snapshot and increment a proof counter. New metadata/owner-sample telemetry exposes snapshot count, full entries, retained entries, required entries, dropped entries, and full-fallback count. Local validation passed: ruff on `compact_owner_search_service.py` plus `test_compact_owner_search_service.py`, focused direct-transition pytest `1 passed`, and full owner-search service suite `47 passed`. This is a support patch, not speed proof; next implementation should target owner-local transition derivation or fixed action-result buffers. |
| F-OPT132-FIXED-ACTION-RESULT-SLOT-LOCAL | 2026-06-07 | Same-process fixed action-result slot proof | The remaining threaded/root-build action path still waited for a Python action-result envelope from the owner each actor step. The smaller sidecar-recommended RPC-boundary cut is to write the full owner action result into a same-process slot table and return a tiny slot stub through the worker result queue. | Implemented default-off same-process fixed action-result slot support for inline/threaded direct-root owner proxies. Process owner rejects this mode unless a real shared-memory result table is added. Root-build action requests carry `action_result_slot_id`; the owner writes the full result to `CompactOwnerActionResultSlotTableV1`, the priority-loop result carries the slot stub, and the parent proxy reconstructs the existing action step from the slot payload. Proof metadata exposes requested/used, slot count, acquire/write/read counts, pending slots, wire result bytes, and full result bytes. Focused root-build/direct-transition tests passed (`5 passed`), ruff passed, and the full owner-search service suite passed (`47 passed`). This is local proof only; next work is launcher plumbing/local speed-row smoke before any H100 row. |

## 2026-06-08 Addendum

| Id | Date | Topic | Finding | Decision |
| --- | --- | --- | --- | --- |
| F-OPT132-FIXED-ACTION-RESULT-SLOT-SPEEDROW-PROOF | 2026-06-08 | Fixed action-result slot speed-row proof closure | The fixed-slot path is now wired through local speed-row/launcher/Modal proof, but it is not a speed claim. The local proof row `opt-fixed-action-result-slot-local-smoke-proof5-20260608` passed with fixed-slot requested/used `true/true`, slot count `4`, acquire/write/read `5/5/5`, pending `0`, wire/full result bytes `413/712`, action-only `true`, direct-root `true`, owner-owned train `1/1`, and tensor-native replay `true`. The tiny CPU speed was `548.91 env/s`, which is proof-only and not comparable to OPT-104. | The route now fails closed unless deferred owner maintenance is requested/proved; owner-owned fused learner-batch proof no longer requires the parent prebuilt learner-batch bit. Validation passed: ruff on touched files, focused speed-row slice `7 passed`, owner-search service `47 passed`, full speed-row smoke `96 passed`. The same-work H100 decision row is recorded in `F-OPT132-FIXED-ACTION-RESULT-SLOT-H100`; use that row to decide the lane. |
| F-OPT132-FIXED-ACTION-RESULT-SLOT-H100 | 2026-06-08 | Fixed action-result slot H100 decision row | The fixed-slot mechanism worked but did not close the speed goal. H100 row `opt132-h100-fixed-action-result-slot-columnar-directtable-b1024a1-normal-unroll2-m724-w180-r1-20260608` passed proof with fixed-slot requested/used `true/true`, acquire/write/read `904/904/904`, pending `0`, wire/full result bytes `414/4837`, parent builder calls/root objects `0/0`, parent replay rows `0/0`, action-only true, terminal sample/target `512/512`, normal-death gate true, and accepted/tensor-native violations `[]`. Speed was `12794.42 env/s`, `57.9453s`: only `1.008x` OPT-104 and `0.807x` columnar r2. | Keep fixed-slot as support because it improved root-build-request parent wait (`26.151s -> 19.035s`) and speed (`11327.75 -> 12794.42`), but close it as the main lane. Do not repeat unchanged. Superseded by the 2026-06-09 owner-local derivation rejection and production fixed-SoA handle-consumption proof; current next gate is corrected local whole-loop timing for the production handle path. |

## 2026-06-09 Addendum

| Id | Date | Topic | Finding | Decision |
| --- | --- | --- | --- | --- |
| F-OPT132-PRODUCTION-FIXED-SOA-HANDLE-CONSUMPTION-LOCAL | 2026-06-09 | Production fixed-SoA handle consumption proof | The fixed-SoA row/window path already had ring-level handle metadata, but production speed-row metadata did not request the handle ring and the compact-owned learner boundary did not report whether the learner actually consumed it. | Added speed-row metadata flags so `owner_search_fixed_soa_replay` requests the fixed-SoA learner-batch handle ring. Added compact-owned loop learner-boundary counters for resident-handle requested/consumed, schema/handle/snapshot/checksum/row counts, and materialized-parent fallback count/reason. Green-path proof consumes the resident handle with fallback `0`; fallback-path proof trains but marks consumed `false` and fallback count `1`. Validation passed: ruff plus combined speed-row/owned-loop/real-ring packet `28 passed`. Local proof only; next gate is corrected local whole-loop timing before any H100. |
| F-OPT132-PRODUCTION-FIXED-SOA-HANDLE-WHOLELOOP-LOCAL | 2026-06-09 | Production owner-search fixed-SoA handle whole-loop local gate | The latest local failures were not proof that the speed idea failed and not whole-loop variance. They were two concrete local contract bugs: expected train requests used raw step count instead of submitted replay entries, and terminal forcing checked terminal-window presence instead of actual terminal-row presence. | Patched expected train-count derivation and terminal sample forcing. Local production owner-search fixed-SoA handle rows now pass normal-death gates: `normaldeath-b128s64-r2` has replay/train `60/15`, resident consumed true, fallback `0/0`, death gate true; `opt107-envelope-r2` has replay/train `64/8`, refresh `2/6`, terminal sample/target `1/2`, resident consumed true, fallback `0/0`, death rows `8`. This unblocks local contract proof only. It does not justify treating 1.25x as success; next speed work must remove/overlap a broad measured wall surface. |
| F-OPT132-OWNER-MANAGER-REPLAY-TRAIN-BOUNDARY | 2026-06-09 | Production manager owner replay/train boundary | The owner action/mechanics manager path can prove parent materialization is gone while still reaching owner replay append and train, but fixed transition batching matters. A three-step proof with transition-batch capacity four closes only two transitions and correctly submits no replay batch; that was a test-shape bug, not a runtime speed failure. | Added a fail-closed manager-boundary guard with parent compact/root/request/dense-action/transition-closure builders poisoned. With transition-batch capacity two, replay/train ownership proves positive and parent materialization remains zero. This is support evidence only: the next speed patch must move broader scheduling/ownership surfaces because replay-only work cannot clear the `2x` gap by Amdahl. |
