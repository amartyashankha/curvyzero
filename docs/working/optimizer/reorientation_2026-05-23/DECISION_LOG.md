# Optimizer Decision Log

Date: 2026-06-08

Purpose: record accepted/rejected decisions and reversal criteria.

| ID | Date | Decision | Evidence | Reversal Criteria |
| --- | --- | --- | --- | --- |
| D-OPT132-ACTION-DISPATCH-OVERLAP-LOCAL | 2026-06-08 | Accept manager-level owner action dispatch overlap as local support and as the next one-row H100 question, not as proven speed. The direct-root action handle is now used across real profile-loop parent work instead of only through the sync wrapper. | `CompactOwnerSearchDirectStepperV1` now exposes `submit_step()` and `resolve_step()`. `HybridBatchedObservationProfileManager.step()` can submit the compact owner action request, run existing parent post-slab work, then resolve before returning the next joint action. The overlap window covers real work already present in the loop: optional batched probe, scalar/no-scalar materialization, payload pickle, resident replay snapshot, render-state snapshot, and minimal/full step payload snapshot. The local proof verifies resolve happens after parent payload/snapshot work, submit/resolve counts equal measured iterations, pending `0`, max pending `1`, submit-no-wait true, sync-wrapper false, and overlapped parent work positive. Speed-row producer, local Modal launcher, and remote Modal producer now carry `--compact-owner-action-dispatch-step-overlap` and require enabled/proof_passed/supported/used, no sync wrapper, no wait-in-submit, counts matched, pending `0`, and positive parent work. Validation passed: focused source-state overlap tests, full owner-search service `55 passed`, replay-contract suite `47 passed`, full speed-row smoke `99 passed`, and ruff. | Reopen if overlap mode falls back to the sync wrapper, submit waits on `worker.result()`, fixed-slot reads happen before resolve, pending handles leak, submit/resolve counts drift from measured iterations, parent work between submit/resolve disappears, or launcher proof accepts a row without the overlap proof. If the first same-work H100 overlap row is flat or negative versus columnar r2, do not repeat unchanged; move to fixed owner mechanics-frame handles or learner publication/update tickets/refs. |
| D-OPT132-ACTION-DISPATCH-HANDLE-LOCAL | 2026-06-08 | Accept owner action dispatch handles as local support for the owner wait/search boundary. The direct-root threaded proxy can submit an owner action request and resolve it later; the old synchronous root-build-request action path is a compatibility wrapper over the handle path. This was not H100 speed evidence by itself; the separate overlap decision records the later profile-loop use across real parent work. | `CompactOwnerSearchSlabProxyV1` now exposes `submit_action_step_from_root_build_request()` and `resolve_action_step_handle()` around `CompactOwnerActionDispatchHandleV1`. Submit publishes the root-build request, snapshots replay/train cadence, acquires the fixed action-result slot, and submits the owner command without calling `worker.result()` or reading the slot. Resolve waits, reads the slot, updates owner replay/train/maintenance counters, builds `CompactSearchActionStepV1`, and records proxy action frames when proxy closure is active. `CompactLazyThreadedOwnerSearchSlabProxyV1` forwards submit/resolve. Proxy-closure mode also avoids storing the parent action-step identity handle in direct-stepper `_pending`; proxy cached action frames own previous-action identity. Focused proof monkeypatches `worker.result()` to raise during submit, verifies fixed-slot read count stays `0` until resolve, then resolves once with submit/resolve/pending/wait-in-submit counters clean. Pending dispatch handles now fail closed on proxy close. Validation passed: action-dispatch/direct-root/proxy packet `5 passed`, profile boundary packet `5 passed`, full owner-search service suite `54 passed`, replay-contract suite `47 passed`, and ruff passed. | Reopen if submit calls `worker.result()`, reads the fixed action-result slot, permits duplicate pending dispatches without proof, loses replay/train/maintenance cadence accounting, or lets sync-wrapper rows masquerade as async/overlap speed evidence. Do not launch H100 for this rung alone; H100 requires the later profile-loop overlap proof or a different fixed mechanics/learner-publication owner-boundary move. |
| D-OPT132-OWNER-PROXY-TRANSITION-CLOSURE-LOCAL | 2026-06-08 | Accept owner/proxy previous-transition closure as local support. The guarded direct-root path now closes previous transitions from proxy cached action state with applied-action verification instead of parent `CompactOwnerSearchDirectStepperV1` staging, but this is not H100 speed evidence and not the full owner-owned step frame. | `CompactRootBuildRequestV1` now carries validated `joint_action`. Default-off `owner_proxy_transition_closure=True` makes the direct stepper call `stage_owner_proxy_transition_from_root_build_request()` and fail closed if the proxy hook is missing. `CompactOwnerSearchSlabProxyV1` caches previous action frames, validates applied actions from the current root-build request, builds the existing derived transition-batch schema, and stages it before owner request/train scheduling. Focused proof monkeypatches parent `_stage_previous_derived_transition`, `_flush_derived_transition_batch`, and `_commit_previous_transition` to raise while the real direct-root threaded proxy still appends a derived transition batch; mismatch proof corrupts applied actions and fails closed before replay append. Proof fields show parent previous-transition closure count `0`, proxy closure closed count `2`, batch/transition count `1/2`, applied-action verification `2`, mismatch `0`, fallback `0/none`, and parent applied-action validation count `0`. Validation passed: focused proxy-closure packet `4 passed`, full owner-search service suite `52 passed`, replay-contract suite `47 passed`, and ruff passed. | Reopen if parent previous-transition staging/flush/commit reappears in the guarded path, if the proxy accepts stale or mismatched applied actions, if `joint_action` disappears from root-build request proof, if fallback/pending/drop/mismatch becomes nonzero, if final flush silently uses parent root sidecars, or if a legitimate direct-root owner path cannot provide owner dense actions without weakening proof. Do not launch H100 for this rung alone; next code must move owner wait/search dispatch, learner publication/update cadence, mechanics/observation ownership, or fixed owner-buffer handles. |
| D-OPT132-DIRECT-ROOTLESS-RETURN-LOCAL | 2026-06-08 | Accept direct-root returned root-sidecar avoidance and request-derived transition outcome validation as local support. This fixes a proof/speed mismatch in the direct-root path, but it is not H100 speed evidence and not the full owner-side previous-transition closure. | In `direct_root_build_request=True`, `CompactRolloutSlabStepV1.root_batch` is now `None`; `_slab_telemetry()` derives root counts and resident host-stub metadata from `CompactRootBuildRequestV1`; and owner-local transition derivation validates mechanics outcome sidecars with `compact_transition_outcome_v1_from_root_build_request()` instead of building a parent request-derived root sidecar. Proof fields show returned root-batch sidecar stored `false`, storage avoided `true`, build count `0`, and root batch build sec `0.0`. Focused direct-root owner-local test monkeypatches both parent `build_compact_root_batch_v1` and `_root_batch_sidecar_from_build_request()` to raise while still staging a derived transition batch. Validation passed: request-outcome contract packet `2 passed`, direct-root owner packet `3 passed`, combined owner/profile packet `6 passed`, full owner-search service suite `50 passed`, and ruff passed. | Reopen if a legitimate direct-root consumer requires a materialized returned `CompactRootBatchV1`, if request-derived mechanics outcome validation misses terminal/final sidecar drift, or if optional `root_batch` breaks a non-direct slab caller. Do not launch H100 for this rung alone; owner/proxy transition closure is now also closed-local support, so next code must move broader owner wait/search, learner-publication, mechanics/observation, or fixed-buffer surfaces. |
| D-OPT132-DIRECT-PENDING-IDENTITY-HANDLE-LOCAL | 2026-06-08 | Accept direct owner pending compact/root sidecar avoidance as local support. The direct stepper pending hot state now stores action-step identity handles instead of parent compact-batch/mechanics-step-view or root-batch sidecars, but this is not H100 speed evidence and not the full owner step frame. | `_PendingCompactSearchV1.compact_batch` and `root_batch` are optional. `CompactOwnerSearchDirectStepperV1` stores both as `None` in `_pending`, increments proof counters, and projects metadata through slab/profile telemetry. Proof fields show pending compact-batch sidecar stored `false`, storage avoided `true`, store count `0`, avoided count positive; pending root-batch sidecar stored `false`, storage avoided `true`, store count `0`, avoided count positive; action-step identity handle stored `true`, identity-handle count positive; and pending root-build request stored `false`. Validation passed: focused direct-root tests `2 passed`, owner-step-frame packet `8 passed`, full owner-search service suite `50 passed`, and ruff passed for touched modules. | Reopen if a legitimate direct-owner replay/transition path requires pending compact-batch or root-batch sidecars, if optional pending sidecars break a non-direct slab path, or if pending identity handles cannot preserve action/legal/terminal proof. Do not launch H100 for this rung alone; next code must move owner wait/search dispatch, pending transition/root ownership beyond parent handles, learner publication, or fixed owner-buffer handles. |
| D-OPT132-OWNER-DENSE-ACTION-LOCAL | 2026-06-08 | Accept owner-published dense next actions as a default-off direct-root fixed-slot support rung. It removes parent dense joint-action reconstruction from that guarded path, but it is not H100 speed evidence and not the full owner-owned step frame. | `CompactSearchActionStepV1` now carries optional `dense_joint_action`. Owner direct-root fixed-slot results derive the dense `(batch, player)` action from validated owner root/search sidecars, payload shape/checksum/digest are fail-closed, and `CompactOwnerSearchDirectStepperV1` validates legality plus active selected-action identity before applying the dense action. The direct-root boundary test monkeypatches `_selected_joint_action_from_compact_sidecars` to raise and still passes, proving owner dense action present/used, parent assembly avoided, fallback `0/none`, mismatch `0`, bytes/checksum/digest set, parent dense-action reconstruction count `0`, parent compact-batch/root builders `0`, and action-feedback checksums preserved. Validation passed: owner-boundary dense-action packet `5 passed`, direct-root owner-service packet `4 passed`, ruff passed for touched modules. | Reopen if a legitimate direct-root fixed-slot owner path cannot publish dense actions without weakening legal/action identity proof, if payload shape/checksum/digest validation blocks correct rows, or if a non-fixed-slot path needs a different handle-based action publication contract. Do not launch H100 for this rung alone; next code must move pending transition/root state, owner wait/search dispatch, learner publication, or fixed owner-buffer handles deeper into owner ownership. |
| D-OPT132-OWNER-MECHANICS-STEP-VIEW-LOCAL | 2026-06-08 | Accept the default-off owner mechanics step-view boundary as the first local upstream step-frame rung. It removes parent `HybridCompactBatch` materialization from the resident direct-root owner-action path, but it is not the full owner-owned step-frame ring and not H100 speed evidence. | `HybridObservationProfileConfig.compact_owner_mechanics_step_boundary` now requires owner action feedback, resident observations, direct root-build request, and resident host-observation stub. `HybridBatchedObservationProfileManager.step()` builds `CompactOwnerMechanicsStepViewV1` after mechanics/resident observation update and before `_make_compact_batch`, with `observation=None` and borrowed root/mechanics sidecars. `CompactOwnerSearchDirectStepperV1` propagates the view metadata through root/action telemetry. The direct-root boundary test monkeypatches both parent `_make_compact_batch` and legacy parent `build_compact_root_batch_v1` to raise and still passes, proving parent compact-batch builder/object count `0`, parent root builder `0`, host observation/final-observation bytes `0`, parent rows `0/0`, search payload bytes `0`, resident-root/stub proof, and action-feedback checksums. Validation passed: expanded owner-boundary/guard packet `6 passed`, ruff passed for touched modules. | Reopen if any direct-root owner-action path legitimately requires a full `HybridCompactBatch`, if resident final-observation/terminal sidecars drift, or if a generic compact consumer can accidentally receive the view. Do not launch H100 for this rung alone; next code must move action assembly/pending root-transition state or a fixed owner-buffer surface deeper into owner ownership and then beat columnar r2. |
| D-OPT132-STEP-FRAME-OWNER-WORLD-MODEL | 2026-06-08 | Treat the active blocker as parent-owned step-frame architecture, not variance and not proof failure. The next real implementation target is an owner-resident mechanics/root/search step-frame boundary, with replay flat-table work only as a paired support lane. | Three bounded sidecars and main-thread code read converged. The fastest columnar row already has owner action-only transport, zero search/visit/root payload bytes, parent replay rows `0/0`, direct prebuilt sample, and `ring_entry_objects=0`, yet still leaves parent wait, worker search, replay append, learner train, and parent-built mechanics/root sidecars. The replay/sample ceiling projection for columnar r2 is `24903.25 env/s`, `1.9625x`, still about `0.558s` short of `2x`; replay-only therefore cannot be the whole answer. Anscombe/Gauss identified the missing mature-system pattern as a single owner-owned step frame passed by slot/generation/digest handles from action application through root/search/replay/sample/learner publication. | Reverse only if a same-work H100 row or local fail-closed toy proves the parent step-frame boundary is no longer a measured surface, or if replay/sample removal plus another measured non-replay patch already clears repeatable `2x`. Do not reverse into longer unchanged rows, root-build-request/fixed-slot/owner-local repeats, or replay-only polishing. |
| D-OPT132-MINIMAL-STEP-PAYLOAD-SNAPSHOT-LOCAL | 2026-06-08 | Accept default-off minimal owner-step payload snapshot as local support only. It reduces one parent mechanics/observation snapshot surface under the guarded direct-root owner boundary, but it is not the owner-resident step-frame ring and not speed evidence. | `HybridObservationProfileConfig.compact_owner_minimal_step_payload_snapshot` now requires `compact_owner_action_step_boundary`. In that guarded path, the profile step snapshot keeps only summary/action payload keys and records measured-step counts, full payload bytes elided, key count elided, retained key count, and snapshot timing. The direct-root/threaded boundary test now runs with this flag and still proves parent root builder `0`, owner build used, resident-root/stub proof, zero host/search payload transport, parent rows `0/0`, and action-feedback checksums. Validation passed: focused source-state boundary/minimal-payload tests `4 passed`; ruff passed. | Reopen if any parent replay/sample path needs omitted payload keys, if a legitimate owner-boundary row cannot use the guard, or if the next owner-resident step-frame patch supersedes this support cut. Do not launch H100 for this flag alone. |
| D-OPT132-OWNER-ACTION-STEP-BOUNDARY-LOCAL | 2026-06-08 | Accept `compact_owner_action_step_boundary` as local proof-only support and a direct-root/action-only speed-row guard. Reject any H100 or speed claim from this boundary alone. | The profile loop now owns the cached next action, verifies `manager.step()` payload action equals the applied action, requires the slab next action, and reports strict counters/checksums/failure reason. The compact Coach speed-row producer, Modal launcher, remote Modal producer, and hash-bound evidence summary now project the proof and fail closed when a direct-root/action-only owner row requests the boundary but lacks clean proof. The direct-root/threaded local proof is also closed: `test_hybrid_profile_owner_action_step_boundary_uses_direct_root_build_request` monkeypatches the legacy parent root-batch builder to raise, then proves `CompactOwnerSearchDirectStepperV1` plus `CompactLazyThreadedOwnerSearchSlabProxyV1` use root-build-request handoff, owner build, resident-root view, host-stub/no-observation transport, zero search payload bytes, zero parent committed/stored rows, and action-feedback checksums across `3` iterations. Focused validation passed: boundary direct-root/profile tests (`3 passed`), speed-row proof/launcher tests (`7 passed`), and ruff. | Reopen only if the proof rejects a legitimate direct-root/action-only row with clean action feedback, zero parent rows/search payload/root-builder fallback/host fallback, clean owner-local derivation counters, and terminal proof. Do not launch H100 for this boundary alone; it must be carried inside a broader owner-buffer/root/search/mechanics/learner-publication patch. |
| D-OPT132-RESIDENT-FINAL-HOST-ELISION-LOCAL | 2026-06-08 | Accept resident terminal dense-host final-observation elision as local mechanics/observation support. Reject any H100 or speed claim from it alone. | In resident-observation search with scalar timestep materialization disabled, `HybridBatchedObservationProfileManager.step()` skips dense host `final_observation = zeros_like(observation)` allocation when sparse resident device final rows already own terminal observations. Telemetry exposes elided count, bytes, and row count. Focused validation passed: resident terminal and boundary tests (`4 passed`) plus ruff. | Reopen if scalar timestep materialization, host final observations, resident replay final-row semantics, or terminal proof requires dense host final observations in this mode. Otherwise carry it only inside broader mechanics/observation/root-owner patches. |
| D-OPT132-FIXED-ACTION-TAPE-OWNER-BUFFER-TOY | 2026-06-08 | Accept the refreshed fixed-action-tape owner-buffer probes as local parity/proof support, but reject a naive fixed-buffer direct env-step rewrite as the main speed lane. | Mechanics-only B1024/m724/w180 local artifact `artifacts/local/vector_fixed_action_tape_results/owner_buffer_mechanics_b1024_m724_w180_20260608.json` passed proof and output/state parity, but fixed direct was slower than compact profile: `0.843x`, `1.3959s` versus `1.1774s`. Rendered search-feedback slab/replay B128/m180/w45 artifact `artifacts/local/vector_fixed_action_tape_results/owner_buffer_render_slab_b128_m180_w45_20260608.json` passed proof with slab replay proof true, closed-loop feedback true, `224` replay appends, `224` sample-gate calls, no root-observation copy, and toy `1.129x`; known limits include CPU-oracle render, profile-only first-legal owner, host replay snapshots, no learner, no GPU search, and no H100 claim. | Reopen only as a contract fixture for an owner-owned action+step/root/replay implementation. Do not run H100 or patch the hot loop around `_fixed_buffer_direct_step` alone; the next candidate must combine owner action, mechanics step, next root, replay append/sample, and learner publication boundaries. |
| D-OPT132-OWNER-LOCAL-TRANSITION-DERIVATION-H100 | 2026-06-08 | Accept owner-local next-root transition derivation as proof-clean support, but reject it as the main speed lane. Do not repeat unchanged. | r1 exposed missing CLI plumbing and r2 timed out during detached launch collection. r3 ran the mechanics but failed the row guard because stale generic transition-batch fields still reported the legacy fixed-coalesced kind; the payload proved owner-local derivation was actually used with derived batches/transitions/transport `181/724/181`, cache hits/misses `724/0`, checksum verified/mismatch `724/0`, fallback/pending/drop `0/0/0`, and parent outcome bytes/fields `0/0`. The guard now overlays generic transition-batch proof fields from owner-local proof. H100 r4 `opt132-h100-owner-local-transition-derivation-columnar-directtable-b1024a1-normal-unroll2-m724-w180-r4-20260608` passed proof with derived kind/schema `owner_local_next_root_derived_transition_batch_v1` / `curvyzero_compact_owner_search_replay_append_derived_transition_batch/v1`, generic projection corrected, digest verified true, direct transition replay/columnar append/tensor-native replay true, normal-death terminal gate true, terminal sample rows `512`, speed `13265.51 env/s`, and wall `55.8875s`. It is only `1.045x` OPT-104 and `0.837x` columnar r2. | Reopen only inside a broader fixed owner-buffer/root/search/mechanics/learner-publication design that needs this support mechanism. Do not run owner-local derivation alone again. The next main lane must remove resident mechanics/root/search/replay/learner-publication surfaces together rather than pushing another narrow proof-clean handoff. |
| D-OPT132-FIXED-ACTION-RESULT-SLOT-H100 | 2026-06-08 | Accept fixed action-result slot as proof-clean support, but reject it as the main speed lane. Do not repeat unchanged. | Local proof `opt-fixed-action-result-slot-local-smoke-proof5-20260608` passed after launcher/Modal plumbing and deferred-maintenance guards. H100 `opt132-h100-fixed-action-result-slot-columnar-directtable-b1024a1-normal-unroll2-m724-w180-r1-20260608` passed proof with fixed-slot requested/used `true/true`, slot count `4`, acquire/write/read `904/904/904`, pending `0`, wire/full result bytes `414/4837`, parent builder calls/root objects `0/0`, parent replay rows `0/0`, action-only true, terminal sample/target `512/512`, normal-death gate true, and accepted/tensor-native violations `[]`. Speed was `12794.42 env/s`, `57.9453s`: barely above OPT-104 and only `0.807x` columnar r2. | Reopen fixed-slot only if a broader owner-buffer/root/search patch needs the same support mechanism. Do not rerun it alone. The next main lane must remove owner-local transition/root/search/mechanics/learner-publication surface, not result-slot bookkeeping. |
| D-OPT132-FIXED-SOA-SLOT-CANDIDATE-LOCAL | 2026-06-07 | Accept slot-candidate fixed SoA as a local support patch, not speed evidence and not a revived replay-only P0 lane. | Local code replaces fixed-SoA candidate-record slicing/padding with slot descriptors and a slot-indexed unroll-2 gather path. Focused replay/source-profile tests pass. Local tiny wiring smoke `opt132-local-fixed-soa-slot-digestdefer-smoke-20260607-r3` passed as `profile_no_death` proof only: owner-search threaded proxy, direct transition batch `12 -> 3`, fixed SoA requested/used true, fixed-SoA slot writes `12`, columnar append false with columnar slot count `0`, fixed-SoA entry/step/learner-ready/table-entry/table-concat object counters `0`, fallback `0/none`, learner slot-candidate path true, and selected fixed-SoA records `6`. | Reverse only if parity or terminal/successor tests fail, or if a later H100 ablation shows proof fallback/object leakage. Do not reopen fixed-SoA as a standalone 2x lane without paired movement in owner-buffer/root/search/parent-wait/learner surfaces. |
| D-OPT132-OWNER-REF-DIGEST-DEFER-LOCAL | 2026-06-07 | Accept owner-ref model-state digest deferral as a default-off same-process ablation/support gate, not as the final correctness story for copied model state. | Local owner-search service tests prove deferred owner-ref digest requires a same-process `model_state_dict` and requires the search worker to return post-load digest source `search_worker_after_load`. Local smoke `opt132-local-fixed-soa-slot-digestdefer-smoke-20260607-r3` proves speed-row plumbing reports `compact_owner_search_owner_train_model_state_digest_deferred_to_refresh=true` and owner train digest time `0.0s` while preserving action-only/direct replay proof. | Reverse if copied-state refresh can bypass the post-load search digest, if model-state-dict requirement weakens, or if H100 ablation breaks proof. Promote only the true shared-model/token path as a fully safe no-learner-hash mechanism. |
| D-OPT132-ROOT-BUILD-REQUEST-LOCAL | 2026-06-07 | Accept direct root-build-request as proof-clean support, but reject it as a standalone H100 speed lane. Do not repeat unchanged. | Local smoke `opt132-local-owner-rootbuildrequest-threaded-directtransition-smoke-20260607-r3` passed with schema/kind `curvyzero_compact_root_build_request/v1` / `resident_root_view_build_request_v1`, publish/resolve/owner-build `15/15/15`, parent builder used/calls `false/0`, parent build sec `0.0`, request observation bytes `0`, resident handle/proof true, H2D/D2H `0.0/0.0`, stub materialized bytes `0`, parent rows `0/0`, search payload bytes `0`, direct replay batches/transitions/transport `3/12/3`, action mismatches `0`, maintenance/policy lag `0/0`, and owner pid/bytes reported. H100 `opt132-h100-rootbuildrequest-columnar-directtable-b1024a1-normal-unroll2-m724-w180-r1-20260607` kept proof closed at scale: publish/resolve/owner-build `904/904/904`, parent builder used/calls `false/0`, root build `0.0s`, request observation bytes `0`, parent rows/search payload bytes `0/0`, direct replay `181/724/181`, action mismatch `0`, normal-death terminal proof true, maintenance/policy lag `0/0`. Speed was rejected: `11327.75 env/s`, `65.4477s`, `0.893x` OPT-104 and `0.715x` columnar r2. | Reopen only as part of a broader owner-resident design that also attacks parent wait, replay append, search, learner train/publication, mechanics, or observation. Do not reopen as a standalone root-build repeat. |
| D-OPT132-FIXED-SOA-LOCAL | 2026-06-07 | Accept the fixed SoA/direct-gather path as locally implemented and ready for one guarded H100 decision row. Do not treat it as speed evidence yet. | Local code adds a default-off fixed SoA unroll-2 buffer to `_CompactReplayRingV1`, `append_fixed_soa_columnar_records()`, fixed learner-batch-only unroll-2 sampling with `fixed_soa_direct_gather_v1` / `fixed_soa_columns_v1`, owner-search direct transition-batch wiring via `--owner-search-fixed-soa-replay`, Modal launcher/producer propagation, and tensor-native validators that reject nonzero table concat/group-object/fixed-SoA object counters. Local parity compares all learner tensor fields against maintained replay on mixed live + partial-terminal rows. Focused ruff and pytest passed. | Reverse if H100 proof shows fallback, columnar append still used, nonzero fixed-SoA entry/step/learner-ready/table-entry/table-concat counters, tensor-native impl/source drift, table concat/group-object leakage, or terminal/action checksum drift. If proof is clean but speed regresses, keep it as a falsified speed lane and do not weaken the proof. |
| D-OPT132-COLUMNAR | 2026-06-07 | Accept columnar direct append as the current fastest single H100 support row, but not as a promotion-grade speedup. The next row is root-build-request on top of this support stack, then broader owner-boundary work if that row is killed. | Corrected no-preset H100 `opt132-h100-columnar-append-direct-table-b1024a1-normal-unroll2-m724-w180-r2-20260607` ran `15852.67 env/s`, `46.7666s` wall over `741376` env steps, about `1.25x` OPT-104 and only about `1.01x` faster than direct-table r3. Proof stayed closed with owner-search threaded proxy, slab bypass, direct transition replay, columnar append used, `724` slots, `ring_entry_objects=0`, direct prebuilt sample used, direct group objects `0`, maintained tensor-native table source, and fallback `0`. Remaining timing still includes replay append `14.219s`, direct append `14.169s`, ring append `11.523s`, search `13.295s`, learner train `9.778s`, and parent wait `17.655s`. | Reverse only if an exact repeat violates proof or falls below the direct-table/ring-batch candidate stack. Do not reverse into ring-entry-only counters, lazy selected table build, preset rows, or another proof-only repeat. |
| D-OPT132-LAZYSELECTED-REJECT | 2026-06-07 | Reject lazy selected direct table build as the next speed path and keep it default-off. | H100 `opt132-h100-lazy-selected-table-columnar-b1024a1-normal-unroll2-m724-w180-r2-20260607` passed proof but slowed to `11179.26 env/s`, `66.3171s` wall. It reduced append/ring append (`14.169s/11.523s -> 10.224s/7.101s`) but moved cost into owner train/sample (`9.778s -> 48.696s`, train sample `2.777s -> 41.640s`) and reintroduced `352` direct group objects. | Reopen only if the implementation changes to avoid sampled record-group objects and proves no learner/sample blowup locally before H100. |
| D-OPT132-FIXED-SOA-NEXT | 2026-06-07 | Superseded: fixed SoA/direct-gather replay storage is support evidence, not the active main lane. Exact/locality H100 rows showed fragmentation is real but fixed-SoA gather/locality alone is not the 2x path. | Exact fixed-SoA recovered to r8 `11616.45 env/s`, still below OPT-104. Locality r2 collapsed selected records to `62` but slowed to `10428.59 env/s`; replay append, worker search, parent wait, learner update, and owner train wall still dominated. | Reopen only as part of a broader owner-buffer design that also attacks root/search dispatch, parent wait, learner publication/update, or mechanics/observation ownership. Do not reopen as a standalone gather/locality lane. |
| D-OPT132-DIRECTTABLE | 2026-06-07 | Accept direct record-table building as H100-useful and part of the current candidate stack. Do not promote it: it is one fastest single row, not a repeat-proven `2x` speedup. | H100 `opt132-h100-direct-table-builder-b1024a1-normal-unroll2-m724-w180-r3-20260607` ran `15681.59 env/s`, `47.2768s` wall over `741376` env steps. Nested owner learner telemetry proves `direct_record_table_v1`, direct build used `true`, table build `0.637s` for `9770` rows, worker replay append `13.879s`, ring append `9.848s`, and remaining `ring_entry_objects=724`. It improves over ring-batch r3/r4 and OPT-104 but remains only about `1.24x` baseline and not repeat-proven. | Reverse if an exact repeat violates proof or falls back below the ring-batch/threaded candidate. Otherwise preserve this path and move the next implementation to fixed SoA/ring-buffer replay storage. Exact-repeat only when making a speed claim or isolating a regression. |
| D-OPT132-DIRECTPREBUILT | 2026-06-07 | Accept the direct tensor-native prebuilt sample path as a local fail-closed support patch, but do not run a standalone H100 row for it. Move directly to fixed SoA/ring-buffer owner replay. | Local code now lets `_CompactReplayRingV1.sample_from_snapshot` call the maintained unroll-2 tensor-native table gather builder without constructing per-sampled-group resident sample objects when learner-batch-only, tensor-native replay, learner-ready unroll-2 cache, resident/device replay, maintained candidate universe, and full maintained-table coverage all hold. Source-state tests prove direct path used and group-object count `0`; speed-row and Modal proof guards now fail closed on direct-prebuilt requested but not used, fallback nonzero, fallback reason not `none`, or group-object count nonzero. Focused ruff and tensor-native/source-state/speed-row/modal tests pass. r3/r4 timing shows the remaining replay append/materialization surface is about `19.420s`, while the skipped group-object/builder surface is too small to justify a standalone remote row. | Reopen standalone H100 only as a control if a later fixed-buffer row needs to isolate this patch. Otherwise keep its proof fields as support and do not reverse into owner-worker-mode polish, transition-batch-only work, or sample-wrapper polishing. |
| D-OPT132-RINGBATCH | 2026-06-07 | Accept ring-batched owner replay append/cache refresh as the current best same-window OPT-132 candidate, but not as the 2x solution. Next target is fixed SoA/ring-buffer owner storage. | Local unit proof showed a four-transition batch builds maintained tensor-native tables once per eligible record. Local smoke passed. H100 r3/r4 kept transition transport `724 -> 181`, pending `0`, action mismatches `0`, tensor-native reused/missing `719/0`, and repeated above r2. r3: `14394.77 env/s`, `51.503s`, replay append `20.204s`. r4: `14160.27 env/s`, `52.356s`, replay append `18.636s`. r3/r4 average: `14277.52 env/s`, `51.930s`, replay append `19.420s`, versus r2 `13618.91`, `54.437s`, `31.926s`. | Reverse only if an exact repeat with the same patch falls below the r32/r33/r34 threaded/background candidate or violates proof. Do not reverse into owner-worker polish, transition transport-only work, or another sample wrapper. Move larger materialization/storage surfaces to fixed buffers. |
| D-OPT132-TRANSITION-BATCH-NEUTRAL | 2026-06-07 | Treat transition-batch transport alone as useful proof but insufficient speed work. | H100 r2 with `--owner-search-slab-bypass --owner-search-transition-batch-size 4` sent `724` logical replay transitions through `181` transport entries, with parent rows `0/0`, legacy transition entries `0`, pending `0`, action mismatches `0`, and green tensor-native/unroll2 proof, but speed was `13618.91 env/s`, `54.437s`, essentially tied with r33 and below r32. | Reopen only as a support mechanism inside a larger materialization/storage patch. Do not rerun the r2 shape unchanged as the next speed experiment. |
| D-OPT132-R14-FIRST-H100-WIN | 2026-06-05 | Select owner-search inline shared-model refresh plus no inline host payload clone as the first H100 speed-win path. Treat it as a first win, not a stable/promoted baseline. The next gate is exact repeat plus promotion/cadence audit. | r14 `opt132-h100-owner-action-only-inlinelearner-sharedmodel-nopayloadclone-warmupgate-b1024a1-normal-unroll2-r14-20260605` ran `13497.30 env steps/sec`, `13.6561s` wall versus OPT-104 `12689.38`, `14.5255s`. It preserved owner-search proof: expected train requests `22`, owner learner updates `22`, refresh request/skip/update `22/0/22`, final update consumed, warmup suppressed `44`, parent committed rows `0`, search-result payload bytes `0`, final owner drain `0.000042s`, owner train/sample/update `2.864s/1.817s/1.032s`, digest/state_dict/host clone `0/0/0`. r12 cadence-only was slow (`8610.37`), and r13 shared refresh still paid host clone and was slow (`9791.12`). | Reverse if an exact r14 repeat fails below OPT-104 or violates proof fields. If the only objection is strict cadence parity, run shared/no-clone with `policy_refresh_interval=4`; do not reopen cadence-only, MPS, eager append, same-process async, or learner-resource split without new evidence. |
| D-OPT132AZ-STRUCTURAL-BUILDER-PIVOT | 2026-06-04 | Stop adding pure attribution timers for the exact `1084/270` same-work swing. The active implementation path is structural learner-batch builder replacement or reshaping, first via guarded learner-ready resident replay or a batch-level vectorized unroll-2 builder. | OPT-132AZ exact generic no-sampler r1/r2 kept identity exact and stable speed claim false, but wall still spread `217.391s / 180.892s` (`18.329%`). Sample gate moved `136.622s / 109.260s`, learner-batch build `85.001s / 65.049s`, builder group-loop `84.177s / 64.278s`, group-loop process CPU `80.37s / 61.70s`, unroll-fields CPU `42.13s / 32.24s`, terminal metadata CPU `15.58s / 11.44s`, and prepare CPU `10.44s / 8.33s`. Prior AP/AQ/AR/AS already ruled out exposed replay/sample drift, CUDA allocator drift, Python GC collections, off-CPU wait, system CPU, page faults, and context switches as the main explanation. | Reopen pure attribution only if a structural patch cannot be validated without one narrow proof field, or if exact rows after a structural patch still fail and the failure moves outside sample-gate learner-batch builder CPU. Do not reverse into generic diagnostics. |
| D-OPT132-REPEATABILITY | 2026-06-02 | Keep OPT-104 as the baseline and treat OPT-132-G as a useful fast row, not a new baseline. The active task is repeatability under the accepted-fast-path preset. | OPT-132-G passed accepted proof fields and beat OPT-104 at `13649.29` env steps/sec, `13.504s` wall. OPT-132-H used the same accepted proof fields but failed at `11366.84` env steps/sec, `16.216s` wall. The measured wall swing is `+2.712s`, while resident stack shift is only `0.012s` to `0.015s`. | Reverse only after repeated same-work H100 rows beat OPT-104 with accepted proof fields and repeatability fields intact. |
| D-OPT132-EVIDENCE | 2026-06-02 | Bind repeatability fields in reports and hash-bound evidence before another speed claim. | Local patches expose and validate row seed/work shape, actor trajectory checksums, terminal/autoreset/death checksums, sample-order checksums, and sample/learner counters. Focused ruff passed. Focused pytest passed: speed-row evidence/report slice `30 passed, 2 warnings`. | Reopen if a future row cannot surface these fields without perturbing timing; then label that row diagnostic and choose a lower-overhead identity proof. |
| D-OPT131 | 2026-06-02 | Historical decision, superseded by OPT-132 repeatability: choose a direct compact trainer hot-path optimization from the OPT-130 bucket map. | Owner variants lost: OPT-127 best owner row `10371.96` / `17.771s`, OPT-128 inline `9516.73` / `19.368s`, OPT-129 threaded `6051.61` / `30.458s`. Direct OPT-130 is better than owner variants but still loses to OPT-104: `11038.67` / `16.698s` versus `12689.38` / `14.5255s`. | Current reversal is D-OPT132-REPEATABILITY: stay on the accepted direct compact trainer path, but first prove repeatability fields before another structural change. |
| D-OPT130-REJECT | 2026-06-02 | Reject direct-core mode alone as a speed win. | H100 row `opt130-h100-direct-compacttorch-directcore-samework-b1024a1-normal-unroll2-speed-20260602-r1` ran at `11038.67` env steps/sec and `16.698s` wall on the accepted shape. Correctness held, but OPT-104 remains faster at `12689.38` and `14.5255s`. | Reopen only as part of a broader direct compact trainer optimization that beats OPT-104 on the same shape. |
| D-OPT129-REJECT | 2026-06-02 | Reject threaded owner search as a speed path. | H100 row `opt129-h100-threaded-owner-search-samework-b1024a1-normal-unroll2-speed-20260602-r1` preserved correctness but ran at `6051.61` env steps/sec and `30.458s` wall. Learner train, search, and actor wall ballooned. | Reopen only if a future design avoids Python thread contention and beats OPT-127 on a same-work H100 row. |
| D-OPT129 | 2026-06-02 | Start OPT-129: asynchronous colocated owner worker with direct in-process root handoff. | OPT-128 proved direct root handoff is correct and cheap, but serial inline execution regressed the full loop to `9516.73` env steps/sec and `19.368s` wall. OPT-127 remains the best owner route at `10371.96` and `17.771s`, largely because it kept background overlap. | Reverse if the local implementation cannot preserve owner appends/train/updates/refresh, parent sample/learner `0/0`, terminal proof, final drain, final policy lag, action-only result shape, direct-root proof, and background-owner proof; or if a same-work row shows the next measured blocker is somewhere else. |
| D-OPT128-REJECT | 2026-06-02 | Reject OPT-128 serial inline owner path as a speed path. | H100 row `opt128-h100-inline-owner-search-samework-b1024a1-normal-unroll2-speed-20260602-r1` preserved correctness, used direct root store, and cut parent wait/root resolve to `0.001s`/`0.025s`, but lost to OPT-127: `9516.73` env steps/sec and `19.368s` wall versus OPT-127 `10371.96` and `17.771s`. Actor/slab rose to `8.404s`/`8.589s`, showing lost overlap. | Reopen only if a new implementation restores background overlap and a same-work H100 row beats OPT-127 or OPT-104. Do not rerun the serial inline path unchanged. |
| D-OPT128 | 2026-06-02 | Start OPT-128: remove or collapse the per-action owner process handoff while preserving same-work owner semantics. | OPT-127 was a real improvement but still lost to OPT-104: `10371.96` env steps/sec and `17.771s` wall versus target `12689.38` and `14.5255s`. Root resolve and replay append are now small enough that another tiny copy/cache edit is unlikely to close the remaining `3.245s` gap. | Reverse only if local implementation cannot preserve owner appends/train/updates/refresh, parent sample/learner `0/0`, terminal proof, final drain, final policy lag, and action-only result shape; or if a focused benchmark proves a different measured bucket can close the full wall gap. |
| D-OPT127-H100 | 2026-06-02 | Keep OPT-127 as useful evidence, but reject it as the speed baseline. | H100 row `opt127-h100-resident-root-replay-no-host-observation-copy-samework-b1024a1-normal-unroll2-speed-20260602-r1` preserved correctness and improved OPT-123 wall `24.001s -> 17.771s`, but did not beat OPT-104 `14.5255s`. Root resolve fell to `0.740s`, replay append to `0.301s`, parent wait to `4.616s`, and H100 utilization stayed low at `11.27%/39%`. | Reopen only if a repeat same-work row beats OPT-104 or if OPT-128 regresses and this row becomes the fallback implementation while another measured bucket is attacked. |
| D-OPT127 | 2026-06-02 | Start OPT-127 as the last small owner-process cleanup before a larger structural change. | OPT-125 and OPT-126 both preserved correctness but lost full-loop speed. Local OPT-127 removes large host observation copies from resident root resolve and resident replay materialization, keeps CPU resident snapshots safe, surfaces action-only resident bridge metadata, and passes ruff plus owner-search tests. | If the same-work H100 row is flat or slower, stop patching the current owner-process boundary and implement a larger root-handoff / colocated owner path. |
| D-OPT126 | 2026-06-02 | Reject replay successor-window cache as the next speed path. | H100 row `opt126-h100-replay-successor-window-cache-samework-b1024a1-normal-unroll2-speed-20260602-r1` passed correctness but ran at `6591.11` env steps/sec and `27.965s` wall, slower than OPT-123 and far below OPT-104. | Reopen only if a future row shows replay sampling setup is again the measured blocker and the proposed change targets full-loop wall, not just sample telemetry. |
| D-OPT125-REJECT | 2026-06-02 | Reject resident replay step host-copy elision as a speed win. | H100 row `opt125-h100-resident-owner-replay-step-no-host-copy-samework-b1024a1-normal-unroll2-speed-20260602-r1` passed correctness but ran at `6593.40` env steps/sec and `27.955s` wall. Replay append improved, but learner train and action wall regressed. | Reopen only as part of a larger root/replay materialization change that beats OPT-123 on a same-work H100 row. |
| D-OPT125 | 2026-06-02 | Start OPT-125 as the active owner-path reduction after rejecting OPT-124. | OPT-123 remains the best useful bucket map at `7679.53` env steps/sec and `24.001s` wall. Local OPT-125 patch removes host observation copies from resident owner replay steps and passes ruff, the new resident-step test, and owner-search service tests. | Reverse if the same-work H100 row fails correctness or shows no wall/bucket improvement, then park this cut and attack replay/train scheduling or another measured OPT-123 bucket. |
| D-OPT124 | 2026-06-02 | Reject inner two-phase owner replay as a speed path and keep it default-off. | H100 row `opt124-h100-owner-inner-two-phase-device-replay-samework-b1024a1-normal-unroll2-speed-20260602-r1` passed correctness but regressed to `3884.97` env steps/sec and `47.444s` wall, with parent wait `21.455s`, root resolve `15.801s`, replay append `28.825s`, learner train `14.267s`, and H100 mean/max utilization `4.54%/25%`. | Reopen only if a new implementation removes the measured root/replay/train regression and a same-work H100 row beats OPT-123 while preserving all owner/death/proof counters. |
| D-OPT119 | 2026-06-01 | Reject Python `local_process` replay append as the speed path and require no-pickle replay/learner dataflow next. | Same-shape H100 r2 preserved work but lost badly: `1148.88` env steps/sec, `160.43s` wall, `57.8GB` request bytes, H100 mean utilization `3.85%`, full replay snapshot submit count `0`, worker-owned replay/model true. Local code now reports append-entry bytes, host observation bytes, resident snapshot count/bytes, compact-batch bytes, and step-payload bytes; future local-process rows fail closed if host observations or resident snapshots still cross the process boundary. | Reopen only if the path no longer sends replay observation tensors through Python multiprocessing, preserves replay/learner/death counters and final drain, and a same-shape H100 row beats OPT-104. |
| D-OPT085 | 2026-05-31 | Make OPT-081 measurement-first: decompose the direct-core initial CUDA event into representation vs prediction before attempting another compile-safe model-path optimization. | Subagent audits and artifact review agreed that OPT-080 did not speed up wall time and that the remaining initial CUDA bucket is opaque. Local patch records representation/prediction CUDA events, direct-core split total, and residual against the outer initial-inference CUDA event; propagates them through slab/source-profile/floor-bundle/decision; and requires direct-core CUDA-event split fields in floor/decision guards. Validation passed: ruff on touched files; compact Torch service `33 passed, 2 warnings`; floor-bundle suite `40 passed`; slab telemetry projection `1 passed, 2 warnings`. | Reverse if the split instrumentation adds synchronization or changes fixed-root/action outputs, if H100 rows show fields are missing or noisy enough to be unusable, or if a stronger target-selection artifact proves another bucket dominates. No speed/default-compile/GPU-mechanics/promotion claim until fixed-root/action guard plus repeated B1024/A16/sim1 H100 row/floor/decision evidence exists. |
| D-OPT084 | 2026-05-31 | Close OPT-080 as safe decoded direct-core output cleanup and continue initial-model-forward/service work; do not make a speed, promotion, default-compile, or GPU-mechanics claim. | Fixed-root public-method vs decoded direct-core gate passed at `artifacts/local/curvytron_hybrid_observation_profile_results/opt080-fixed-root-tape-decoded-directcore-smoke-20260531/row_001_result.json`: 4 records / 1024 active roots, action match `1.0`, visit L1 `0.0`, root-value diff `0.0`, profile-only/no-live/no-training. H100 rows `opt080-h100-decoded-directcore-output-host-phase-cudaevent-sim1-r1-20260531` and `opt080-h100-decoded-directcore-output-host-phase-cudaevent-sim1-r2-20260531` passed with direct requested/used `180/180`, fallback `0`, action D2H `737280`, replay/committed replay D2H `0`, no host fallback, decoded direct output true, direct reward materialized false, and speeds `12369.7` and `10740.5` steps/sec. Floor bundles completed, and decision report `artifacts/local/curvytron_compact_torch_service_bucket_decision_results/opt080-compact-torch-service-bucket-decision-decoded-directcore-output-host-phase-cudaevent-sim1-20260531/compact_torch_service_bucket_decision_report.json` reports all canonical guards passed, repeat count 2 met, `speed_claim_allowed=false`, `gpu_mechanics_selection_allowed=false`, and `selected_next_target=initial_model_forward_compile_safe_path`. | Reopen if a later same-denominator repeat shows the cleanup consistently improves wall/model buckets without actor/sample noise, or if a stronger parity/action-trajectory gate finds a semantic mismatch. Reopen GPU mechanics only after repeated actor/env dominance with a CPU-oracle mechanics contract. |
| D-OPT083 | 2026-05-31 | Accept OPT-080 only as a local direct-core host/model-path cleanup until fixed-root/action-trajectory and H100 evidence exists. | Direct-core initial inference now returns a slim private decoded output instead of repacking `_prediction` output into a public-method-shaped object with synthetic root reward materialization; `run()` and `run_action_step()` share root output extraction; tree search no longer receives the unused root-output object; and telemetry records the decoded direct-output route. Local validation passed: ruff; compact Torch service `33 passed, 2 warnings`; compact Torch plus floor-bundle `69 passed, 2 warnings`; focused replay telemetry `3 passed, 2 warnings`. | Revisit after the OPT-080 fixed-root or action-trajectory guard plus repeated same B1024/A16/sim1 H100 row/floor/decision chain lands. Revert or park if parity fails, direct mode falls back, eval/inference guard telemetry disappears, transfer budgets drift, or the measured bucket is noise. Do not use this local patch to claim stock speedup, promotion, default compile, or GPU mechanics. |
| D-OPT082 | 2026-05-31 | Accept OPT-079 direct-core as guarded target-selection evidence and continue initial-model-forward/compile-safe service work; do not make a speed, promotion, compile-default, or GPU-mechanics claim. | Fixed-root public-method vs direct-core gate passed at `artifacts/local/curvytron_hybrid_observation_profile_results/opt079-fixed-root-tape-public-vs-directcore-smoke-20260531/row_001_result.json`: 4 records, labels `initial_inference_direct_core` and `primary`, action match `1.0`, visit L1 `0.0`, root-value diff `0.0`, profile-only/no-live/no-training. H100 rows `opt079-h100-directcore-host-phase-cudaevent-sim1-r1b-20260531` and `opt079-h100-directcore-host-phase-cudaevent-sim1-r2-20260531` passed with `15056.1` and `14874.6` steps/sec, direct requested/used `180/180`, fallback `0`, CUDA events present, action D2H `737280`, replay/committed replay D2H `0`, no host fallback. Floor bundles completed and decision report `artifacts/local/curvytron_compact_torch_service_bucket_decision_results/opt079-compact-torch-service-bucket-decision-directcore-host-phase-cudaevent-sim1-20260531/compact_torch_service_bucket_decision_report.json` reports all canonical guards passed, repeat count 2 met, `speed_claim_allowed=false`, and `selected_next_target=initial_model_forward_compile_safe_path`. | Reverse or park if a stronger fixed-root/action-trajectory guard finds public-vs-direct divergence on real policy roots, if a repeat no longer has direct requested/used counts matching steps or fallback `0`, if CUDA-event/timing or transfer-budget guards fail, or if the next small service/model patch shows the direct-core speed was noise and another bucket dominates. Reopen GPU mechanics only after repeated same-denominator actor/env dominance with a CPU-oracle mechanics contract. |
| D-OPT081 | 2026-05-31 | Allow a default-off direct-core initial-inference probe, but do not treat it as a speed default or compile lane. | Local implementation uses `initial_inference_mode=direct_core` to call `_representation` plus `_prediction` under the existing eval/inference guard, emits requested/effective/fallback mode evidence and split timings, rejects unsupported models, and rejects direct core combined with model compile. Local validation passed: ruff, changed compact Torch/floor/speed-row/grid slice `134 passed, 2 warnings`, and boundary config regression `1 passed, 2 warnings`. | Revert or park the probe if fixed-root/action parity fails, if H100 row shows no useful split/timing explanation, or if real model wrappers carry semantics not captured by `_representation/_prediction`. Promote only after same-denominator H100 row/floor/decision with no mode fallback and all existing guardrails. |
| D-OPT080 | 2026-05-31 | Require OPT-079 guard-hardening evidence before another speed-default or H100 service/model claim. | Local patch makes enabled CUDA-event rows fail closed on missing/zero initial, recurrent, or tree event timings; projects and rejects resident host fallback; applies recurrent-call and transfer-budget guards to canonical and repeat bundles; and adds source/payload non-claim flip negatives. Validation: ruff passed; `tests/test_compact_speed_row_floor_bundle.py` `32 passed`; `tests/test_compact_torch_search_service.py tests/test_compact_speed_row_floor_bundle.py` `59 passed, 2 warnings`. | A future valid CUDA-event timing mode legitimately emits zero for one required event field; then document that timing mode and replace the positive-field requirement with an explicit mode-specific guard before using it for target selection. |
| D-OPT079 | 2026-05-31 | Treat OPT-078 as safe guard/service cleanup and measurement hardening, not a speed win; keep the active target on guarded initial-model-forward/service work. | OPT-078 removed recurrent nested `no_grad`, surfaced guard enter/exit/total timing, and hardened service-bucket decisions to require eval/inference guard telemetry. H100 row `opt078-h100-recurrent-guard-timing-host-phase-cudaevent-sim1-20260531` passed with `10740.8` steps/sec, wall `17.161s`, initial CUDA event `1.546s`, recurrent `0.336s`, guard total `0.208s`, action D2H `737280`, replay/committed replay D2H `0`, no host fallback, and all non-claims. Floor bundle reports fixed-floor ratio `1.281`; decision report `artifacts/local/curvytron_compact_torch_service_bucket_decision_results/opt078-compact-torch-service-bucket-decision-recurrent-guard-timing-host-phase-cudaevent-sim1-20260531/compact_torch_service_bucket_decision_report.json` selects `initial_model_forward_compile_safe_path`. | A repeat same-denominator row shows this cleanup consistently lowers wall/model/service time, or the next actual service/model patch changes the dominant bucket. Reopen GPU mechanics only after repeated same-denominator actor/env dominance with a CPU-oracle mechanics contract. |
| D-OPT078 | 2026-05-31 | Treat OPT-077b as replay-index/commit bucket reduction, not top-level speed proof; return the active compact Torch target to guarded initial-model-forward/service work. | OPT-077 packed-scalar row passed but was slower (`10781.5` steps/sec, wall `17.096s`) and exposed metadata as the replay-index child (`1.651s`). OPT-077b metadata-counts row passed with `11735.2` steps/sec, wall `15.707s`, replay-index build `0.145s`, commit `0.185s`, metadata `0.013s`, action D2H `737280`, replay/committed replay D2H `0`, no host fallback, loaded-checkpoint identity, and all non-claims. Floor bundle is not a speed win versus fixed floor (`13759.0`, ratio `1.172`) or OPT-076 (`12846.8`). Decision report `artifacts/local/curvytron_compact_torch_service_bucket_decision_results/opt077b-compact-torch-service-bucket-decision-replay-index-metadata-counts-host-phase-cudaevent-sim1-20260531/compact_torch_service_bucket_decision_report.json` selects `initial_model_forward_compile_safe_path`. | Reopen replay-index/commit/sample only if a repeat row shows the bucket regressed or sample-gate sync debt dominates after service/model work. Reopen actor/GPU mechanics only after repeated same-denominator actor/env dominance; one actor-wall-dominant floor-bundle classification is not enough. |
| D-OPT077 | 2026-05-31 | Select replay-index/commit/sample-gate decomposition as the next compact Torch speed target; do not chase GPU mechanics or another initial-forward patch from the OPT-076 evidence. | OPT-076 H100 row, floor bundle, and refreshed service-bucket decision passed. The bundle is same-denominator H100 shape-matched: compact `12846.8` vs fixed floor `13759.0`, wall ratio `1.071`, dispatch residual `0.005s`, action residual `0.004s`, action readback `0.011s`, core residual `0.152s`, initial CUDA event `1.510s`, tree total `0.368s`, replay-index build delta `2.457s`, commit-previous delta `2.491s`. Decision report `artifacts/local/curvytron_compact_torch_service_bucket_decision_results/opt076-compact-torch-service-bucket-decision-dispatch-envelope-host-phase-cudaevent-sim1-20260531/compact_torch_service_bucket_decision_report.json` selects `replay_index_or_sample_gate`. | A repeated same-denominator row shows replay-index/commit/sample was noise and another bucket dominates, or a local fidelity/test gate shows the replay/commit/sample change would perturb search/replay semantics. |
| D-OPT076 | 2026-05-31 | Treat OPT-076 dispatch/service-envelope decomposition as complete evidence for target selection, not as a speed/promotion claim. | Local instrumentation validation passed, then H100 row `opt076-h100-dispatch-envelope-host-phase-cudaevent-sim1-20260531` proved the new fields are present in raw row totals with loaded checkpoint identity, guard telemetry, fast-path/softmax-skip `180/180`, action D2H `737280`, replay D2H `0`, committed replay D2H `0`, and no host fallback. Floor bundle and decision packet preserved non-claims. | If a raw-row audit shows the new fields were missing or zero-filled by the bundle, invalidate the decision packet and rerun. If trajectory-identical claims are needed, collect a stronger fixed/root or trajectory-matched comparison; current scope is shape-matched only. |
| D-OPT075 | 2026-05-31 | Treat the OPT-075 guard-only initial-inference cleanup as safe support evidence, not a speed default or model-forward win. | OPT-075 H100 row `artifacts/local/curvytron_compact_coach_speed_row_results/opt075-h100-guardonly-host-phase-cudaevent-sim1-20260531/compact_coach_speed_row_modal_report.json` and floor bundle `artifacts/local/curvytron_compact_speed_row_floor_bundle_results/opt075-compact-speed-row-floor-bundle-guardonly-host-phase-cudaevent-sim1-20260531/compact_speed_row_floor_bundle_report.json` passed. Row reports `12563.1` steps/sec, wall `14.672s`, service `2.164s`, action wall `2.217s`, model `1.571s`, initial CUDA event `1.562s`, action D2H `737280`, replay D2H `0`, no hidden host fallback, guard telemetry restored, compact-vs-floor wall ratio `1.095`. | A repeated same-denominator bundle shows the cleanup itself consistently lowers initial CUDA/model event time and end-to-end wall under matching trajectories; otherwise the next decision remains dispatch/service-envelope decomposition. |
| D-OPT074 | 2026-05-31 | Select initial model forward/compile-safe service work as the next compact Torch optimization target. | `artifacts/local/curvytron_compact_torch_service_bucket_decision_results/opt074-compact-torch-service-bucket-decision-20260531/compact_torch_service_bucket_decision_report.json` binds OPT-073b canonical, OPT-074 current CUDA-event repeat, OPT-073 fast support, and OPT-066 compile decision. It reports `decision=select_initial_model_forward_path`, `selected_next_target=initial_model_forward_compile_safe_path`, same-denominator evidence count `2`, current CUDA-event timing present, and all speed/promotion/live/stock-speedup/`train_muzero` claims false. | A repeat same-denominator row shows actor/env, replay/sample, or recurrent buckets dominate instead; or the selected initial-model path cannot preserve eval/inference guard, action/trajectory checks, action D2H, and replay D2H budgets. |
| D-OPT074-GPU | 2026-05-31 | Keep GPU mechanics deferred. | OPT-074 current event bundle has actor wall delta only `0.138s` while search delta is `2.198s`; selected-action D2H is the expected `737280` int16 bytes and not the wall. | Same-denominator evidence after service/model work shows actor/env is repeatedly the dominant remaining wall and a CPU-oracle lockstep GPU-mechanics contract exists. |
| D-OPT074-DEFERRAL | 2026-05-31 | Do not implement recurrent deferral yet. | OPT-074 recurrent CUDA event is `0.333s`, not dominant; deferral crosses action-commit/model-refresh boundaries and the decision packet has `recurrent_deferral_implementation_allowed=false`. | A named refresh-hazard guard lands with force-drain/snapshot semantics and repeated evidence shows recurrent work is the dominant remaining bucket. |
| D-001 | 2026-05-28 | Do not start a full GPU mechanics rewrite as the next main artifact. | `goal.md` and current Amdahl read: compact candidate still lacks trainer honesty and promotion gates. | Same-denominator compact candidate shows actor/runtime remains top wall after search/observation/replay/learner gates are honest. |
| D-002 | 2026-05-28 | Fix measurement/summary honesty before another profile row. | Subagents independently found compact semantics and label-status weaknesses. | Focused telemetry gates pass and next profile has explicit labels. |
| D-003 | 2026-05-28 | Treat mixed terminal/live stock adapter gate as a local P0 fidelity proof. | Russell audit found vector mixed terminal/live and stock adapter tests were separate. | A broader stock-entrypoint gate supersedes this local adapter proof. |
| D-004 | 2026-05-28 | Fallback MCTS-root `steps/sec` is not Gate A stock-profile env-step currency. | Euclid audit found RND rows could report `mcts_search_root_sum` as env steps when raw collector deltas were zero. | Remote RND rows consistently expose raw collector env-step deltas and summaries prove the same currency. |
| D-005 | 2026-05-28 | Make persistent-vs-stateless model-facing tensor parity the next local artifact. | Plato audit found this is the smallest missing resident-observation fidelity stitch. | The parity gate passes and a stronger boundary-level compact Torch/model probe supersedes it. |
| D-006 | 2026-05-28 | Treat larger fixed-root-tape rows as an orchestration problem first. | Kant audit found blocking Modal rows can expire before JSON capture; comparator identity checks already exist. | Detached/captured-result support lands and durable larger-row JSON is captured. |
| D-007 | 2026-05-28 | Promote fixed-root durable capture to the active artifact. | RND telemetry and resident/stateless model-facing tensor parity are now locally green. | A local invariant failure shows a more basic fidelity gate must be fixed first. |
| D-008 | 2026-05-28 | Use detached FunctionCall result capture for the next larger root-tape row. | Builder/boundary/runner plumbing is locally tested; blocking B512/A16 previously expired before JSON. | The detached path fails remotely in a way that cannot be fixed without changing Modal timeout/lifecycle strategy. |
| D-009 | 2026-05-28 | Consider detached FunctionCall capture validated for larger fixed-root rows. | `optimizer-fixed-root-tape-large-detached-20260528` passed with `function_call_id=fc-01KSQP8VJR1TBVAXT3VZMVC45S` and a complete `row_001_result.json`. | A later detached row fails due to launch/result lifecycle rather than backend/profile behavior. |
| D-010 | 2026-05-28 | Do not repeat compact Torch versus fixed-shape floor as the next root-tape artifact. | The larger row reproduced the same action-match and visit-L1 pattern on 16 records. | A regression needs a same-pair repeat, or a local invariant change requires re-baselining. |
| D-011 | 2026-05-28 | Use MCTX as the next root-tape sidecar, not as the primary closed-loop policy. | MCTX can be registered independently under `run_compact_root_tape_comparison_v1`; fixed-root tape removes trajectory confounding while preserving semantic/timing telemetry. | MCTX fixed-root deltas are too noisy or the JAX backend cannot run reliably under the detached profile. |
| D-012 | 2026-05-28 | Defer direct CTree root-tape sidecar until model identity is clean. | Current direct CTree helper is checkpoint-backed; compact Torch primary is in-memory. Comparing them would mix backend and model differences. | A direct CTree service is built from the same in-memory policy, or the comparison is explicitly checkpoint-backed on both sides. |
| D-013 | 2026-05-28 | Park full GPU mechanics rewrite; start only with a narrow CPU-oracle device-mechanics contract if actor/runtime remains hot. | CPU mechanics own high-risk source semantics; selected-action D2H is tiny relative to runtime plus mask/state/render-state ownership. | Same-denominator compact candidate shows actor/runtime remains a top wall after search/resident/replay/learner gates are honest. |
| D-014 | 2026-05-28 | Park MCTX as a parity backend for the current compact Torch path. | Corrected fixed-root row `optimizer-fixed-root-tape-mctx-detached-20260528b` matched compact Torch actions on only `0.2829` of roots and had visit/value deltas too large for backend parity. | A future MCTX parity bridge intentionally aligns search semantics/model behavior and passes thresholded fixed-root gates. |
| D-015 | 2026-05-28 | Return to compact-owned trainer candidate work on compact Torch. | Fixed-root MCTX answered the immediate backend question: it is useful telemetry but not the next drop-in speed backend. External/code-pattern refresh points back to static slabs, replay ownership, and trainer honesty. | A new fixed-root backend candidate passes semantic thresholds or compact-owned trainer work hits a concrete blocker. |
| D-016 | 2026-05-28 | Treat `optimizer-compact-owned-candidate-next-20260528b` as the current compact-owned profile denominator. | Row passed with resident observation used, fallback `0`, observation H2D `0`, committed replay D2H `0`, scalar materialization `0`, compact MuZero real update `true`, and explicit `calls_train_muzero=false`. | A matched rerun fails, or a stricter trainer-honesty gate shows this denominator hides a required semantic shortcut. |
| D-017 | 2026-05-28 | Build durable replay/full MuZero unroll before matched stock-vs-candidate speed rows. | The current compact learner/replay edge is one-step and profile-local; comparing it to stock training now would mix training semantics with speed. | Durable N-step sample windows, terminal/final-observation target alignment, and `num_unroll_steps > 1` compact MuZero updates pass locally. |
| D-018 | 2026-05-28 | Harden compact-owned profile rows before more remote launches. | Runner and builder audits found ways to produce rows that would either overclaim completion or fail late at the Modal boundary. | The invariant blocks future legitimate candidate rows; then narrow the invariant with a documented reason and a replacement gate. |
| D-019 | 2026-05-28 | Treat nonterminal N-step unroll as locally proven but not terminal-complete. | Resident replay windows now feed explicit N-step tensors into a real compact MuZero recurrent update; terminal/final-observation rows are still rejected by resident replay and need a separate fidelity gate. | Terminal/final-observation target alignment and post-terminal masking pass locally and in a durable remote compact-owned N-step row. |
| D-020 | 2026-05-28 | Represent terminal N-step learner semantics with explicit validity masks, not repeated or fake post-terminal targets. | Copernicus audit found policy/value/reward losses were unconditional; terminal windows would otherwise supervise padded post-done slots. | A stock-aligned target builder proves terminal final-state value/policy semantics should be supervised differently and updates the masks with tests. |
| D-021 | 2026-05-28 | Treat resident terminal final-observation sampling as locally proven, but not promotion-ready. | Resident compact batches now own final device observations and masks, resident replay samples terminal final observations instead of autoreset/current observations, and terminal N-step masks feed compact MuZero; focused and broader local suites passed. | Stock target-alignment diff or durable remote terminal-safe N-step row fails and shows the local mask policy or resident final-observation ownership is wrong. |
| D-022 | 2026-05-28 | Treat terminal stock target alignment as a launch gate, not post-hoc analysis. | Stock MuZero terminal tails train value targets as no-bootstrap reward returns; raw search root values were the hidden mismatch. Local regression now proves `reward=[2,5]` maps to `value=[7,5,0]`, and the compact learner rejects terminal N-step raw-root values. | A fuller stock GameSegment parity artifact supersedes the local stock-semantics regression. |
| D-023 | 2026-05-28 | Require explicit terminal proof telemetry for the remote compact-owned N-step row. | A row could previously pass compact-owned invariants while sampling zero terminal windows. The corrected preset uses `steps=6`, `max_ticks=3`, support scale `9`, and the runner requires terminal sample rows, final-observation rows, resident terminal final-observation use, stock terminal value-target mode, and learner `done_count > 0`. | The invariant blocks a legitimate terminal-safe row; then narrow the check and document the replacement proof field. |
| D-024 | 2026-05-28 | Terminal N-step replay must pad terminal tails instead of requiring searched post-terminal roots. | Remote `20260528b` produced roots and committed rows but zero N-step-eligible sample rows because terminal roots are not searched. Padding all-terminal tails with zero-valued index rows preserves stock no-bootstrap masks/targets without fake search. | A future compact search service intentionally searches terminal reset roots and proves full successor windows without padding. |
| D-025 | 2026-05-28 | Use model-matched support scale `9` for the compact MuZero terminal preset. | Remote `20260528c` reached the learner and failed because the LightZero profile model value head width matched support scale `9`, not the preset's `300`. | The profile policy/model config changes and a shape probe proves a different support width. |
| D-026 | 2026-05-28 | Select stock-vs-candidate public sample diff as the next trainer-honesty artifact after the terminal row. | Kuhn/Nietzsche/Schrodinger audits converged that real `GameSegment -> MuZeroGameBuffer.sample(...)` semantics are the most authoritative next local proof before replay-store architecture. | A public sample diff cannot be made deterministic or proves the compact rows disagree; then fix the disagreement before replay-store work. |
| D-027 | 2026-05-28 | Build durable compact replay-store fidelity next, before a split compact-owned loop entrypoint. | The public sample diff is now green and defines the fields a durable store must preserve: row identity, env/player order, observations, valid actions, masks, rewards, values, policies, and terminal semantics. | Replay-store design is blocked by loop-boundary/policy-version questions that must be solved first. |
| D-028 | 2026-05-28 | Keep durable replay-store scope to snapshot/restore of the compact replay ring for this gate. | Carver/Hooke/Laplace audits converged that a new replay subsystem would be premature; the useful proof is ordered-entry persistence, lineage, eviction, strict load, and sample-visible field fidelity. | A split loop entrypoint needs a broader store API, or a local gate fails because ring-level state cannot represent required replay semantics. |
| D-029 | 2026-05-28 | Promote split compact-owned loop entrypoint with policy-version handoff to the next artifact. | Durable replay-store fidelity is locally green, but the compact path still lives in the profile harness and cannot yet be matched honestly against stock training as a loop entrypoint. | The split entrypoint is blocked by replay/checkpoint lifecycle design; then document the blocker and narrow the next store/metadata slice. |
| D-030 | 2026-05-28 | Keep the split compact-owned loop entrypoint profile-only until it actually enters stock LightZero. | The new entrypoint owns compact replay/sample/learner cadence and policy handoff, but it still calls the compact learner edge directly and does not call `train_muzero` or stock Coach. | A future implementation routes through stock `train_muzero`/Coach with checkpoint/eval/tournament compatibility and can honestly set `calls_train_muzero=true`. |
| D-031 | 2026-05-28 | Prime no-warmup split-entrypoint rows instead of failing the first measured step. | Remote `optimizer-compact-owned-split-entrypoint-next-20260528` showed the old path skipped replay append until a previous step existed; the split loop should preserve that semantics explicitly. | A future manifest requires warmup before split-loop replay append, or the loop gets a different bootstrapping contract with a local/remote proof. |
| D-032 | 2026-05-28 | Treat compact output projection as part of the remote invariant surface. | Remote `20260528b` completed the profile but failed runner invariants because projected compact JSON dropped the owned-loop fields. | Compact output is retired for these rows, or the runner consumes full profile payloads directly. |
| D-033 | 2026-05-28 | Move from split-entrypoint proof to matched denominator selection. | Remote `20260528c` passed with entrypoint schema, replay-store ownership, policy/model/source lineage, replay-state metadata, sample/learner telemetry, and safety-label triad intact. | A rerun of the same row fails, or matched denominator planning reveals a missing compact-loop field that requires another split-entrypoint gate. |
| D-034 | 2026-05-28 | Select the no-RND H100 stock-vs-compact matched denominator pair and keep currencies separate. | Dirac/Herschel/Arendt audits and local code checks converged on stock H100 C512/b64/sim8 `train_muzero` raw env-step currency versus compact H100 B1024/A16/sim8 split-loop compact active-root currency. Manifests are materialized and runner dry-runs pass. | The remote pair fails closed, RND becomes required for the selected comparison, or a real Coach-compatible compact trainer changes the compact currency. |
| D-035 | 2026-05-28 | Stop treating speed as the blocker; promotion semantics are the next blocker. | The matched pair completed and preserved currencies/metadata: compact is fast in profile-only compact-root currency, but still `calls_train_muzero=false` and lacks Coach checkpoint/eval/tournament/reward/RND/death/policy-refresh compatibility. | A compact row actually enters stock `train_muzero`/Coach, or a compact trainer candidate explicitly implements and proves those lifecycle contracts. |
| D-036 | 2026-05-28 | Keep the next route compact-owned instead of bridging into stock `train_muzero` immediately. | Local code read and Poincare/Curie audits agree that stock `train_muzero` still owns collector/search rows, `GameSegment`, `MuZeroGameBuffer.sample`, learner, and checkpoint lifecycle; forcing compact through it now would blur currencies and rebuild the scalar hot path. | A narrow stock bridge can consume compact rows without scalar materialization and proves checkpoint/eval/tournament semantics with `calls_train_muzero=true`. |
| D-037 | 2026-05-28 | Make checkpoint/resume the first compact Coach-compatibility implementation gate. | The compatibility report can record existing support gates, but eval, tournament, assignment, and live canary work need a concrete artifact to load first. | A more basic trainer entrypoint blocker prevents even local checkpoint save/load, or stock eval/tournament loader constraints force an adapter-design gate first. |
| D-038 | 2026-05-28 | Keep the compact trainer checkpoint compact-native and label stock loader gap as `adapter_missing`. | Maxwell audit showed stock eval/tournament can load mapping-style LightZero checkpoints with policy-observation sidecars, but the compact checkpoint dataclass also needs optimizer, replay, loop runtime, counters, and resume metadata that stock loaders cannot consume as-is. | A stock-compatible export payload or compact eval adapter is implemented and proven by loader tests. |
| D-039 | 2026-05-28 | Use a derived stock-compatible export payload as the first loader-facing artifact, not a compact eval adapter. | Ada/Tarski audits and local loader reads showed existing stock/eval/tournament code already discovers mapping payloads under `model` and consumes `<checkpoint>.metadata.json` sidecars. A compact eval adapter would need new model/config reconstruction machinery and still lack strict load proof. | Strict stock LightZero load cannot be made to work for compact models; then switch to an explicit compact eval adapter with its own model class/config contract and non-claims. |
| D-040 | 2026-05-28 | Do not let the stock-shaped export claim tournament loadability until strict stock model load is proven. | The compact learner accepts generic MuZero-shaped models, so a `model` mapping plus valid sidecar proves state-dict discovery and metadata only. The new export records `stock_model_contract_verified=false` and has no boolean parameter that can flip it. | A strict stock-model load verifier passes and writes an evidence report with state key, model variant, support config, and strict-load result. |
| D-041 | 2026-05-28 | Treat verifier reports as separate evidence until a verified-export attachment policy exists. | The durable stock-model smoke proves strict load for one export, but the base export payload intentionally defaults to `stock_model_contract_verified=false`. Mutating that default too early would recreate the overclaim problem. Superseded by D-043. | A verified-export artifact contract lands that atomically couples export payload, sidecar, verification report, state key, and strict-load status. Satisfied by D-043. |
| D-042 | 2026-05-28 | Propagate checkpoint runtime metadata through the tournament policy loader. | Recording `decision_source_frames`, `source_physics_step_ms`, `decision_ms`, and `source_max_steps` in the export is not enough if `_load_policy_from_checkpoint(...)` reconstructs the policy/env with defaults. | A future loader refactor proves those runtime fields are irrelevant to strict stock model construction, or a full gameplay/eval smoke replaces this local loader contract with stronger evidence. |
| D-043 | 2026-05-28 | Attach verified compact stock-export evidence with a sibling bundle, not by mutating the base export. | `curvyzero_compact_stock_export_evidence_bundle/v1` hashes the export, policy sidecar, strict-load verifier report, and tournament-loader report together; durable bundle `artifacts/local/curvytron_compact_stock_export_evidence_bundle_results/optimizer-compact-stock-export-evidence-bundle-smoke-20260528/iteration_0.pth.tar.evidence.json` records `ok=true`, `base_export_claims_mutated=false`, strict stock load, and tournament-loader construction. | Downstream eval/assignment requires a self-contained loadability flag in the checkpoint itself; then create a new promoted artifact contract with explicit mutation rules and fresh non-claim guards. |
| D-044 | 2026-05-28 | Treat the one-game/GIF compact-export smoke as eval/load evidence, not promotion evidence. | Durable local smoke `artifacts/local/curvytron_compact_stock_export_gameplay_smoke_results/optimizer-compact-stock-export-one-game-gif-smoke-20260528/one_game_gif_smoke_report.json` loaded the bundled export under `model` for both seats, ran eval actions for 4 physical steps, and wrote GIF/frames artifacts with non-claims false. | A full promotion candidate has a non-profile trainer entrypoint plus every required lifecycle gate with evidence refs; until then, gameplay smoke remains a loader/eval gate only. |
| D-045 | 2026-05-28 | Select reward/RND contract as the next compact-owned promotion gate. | The post-gameplay compatibility report passed trainer entrypoint, checkpoint save/load, resume metadata, and eval/GIF/tournament load while leaving reward/RND, death/terminal, policy refresh, and metrics lineage missing. Reward/RND is the next clean contract because it decides whether the first promotable compact candidate is explicitly no-RND or must checkpoint/update RND state. | A more basic trainer-runtime gate blocks reward/RND metadata from attaching to compact checkpoints, or death/terminal evidence fails and shows reward semantics cannot be isolated. |
| D-046 | 2026-05-28 | Make the first compact-owned reward/RND contract explicitly no-RND. | RND adapters and stock reward-model plumbing exist, but compact-owned training does not yet own RND update, reward-target mutation, checkpoint, or restore semantics. The honest promotable candidate should first attest extrinsic replay rewards with `exploration_bonus_mode=none`, `rnd_enabled=false`, and no RND checkpoint/resume claim. | A compact-owned RND lifecycle lands with update counters, predictor/target state, reward-target semantics, checkpoint/restore validation, and compatibility evidence. |
| D-047 | 2026-05-28 | Do not promote profile/no-death terminal N-step evidence as the death/terminal contract. | The durable terminal row and local compact learner checks prove useful terminal handling, but they run in `profile_no_death`/max-tick territory. A Coach-compatible death contract needs source-faithful `death_mode=normal` collision death and death facts propagated through compact replay, learner, checkpoint, and compatibility metadata. | A normal-death compact-owned artifact proves collision death, final observations/rewards before autoreset, masks, no-bootstrap targets, truncation handling or explicit truncation exclusion, and non-empty compatibility evidence. |
| D-048 | 2026-05-28 | Do not count arbitrary checkpoint metrics as `training_metrics_lineage`. | A nonempty metrics dict such as `{"unit_metric": 1.0}` or `{"loss": 1.25}` is not enough to prove coherent trainer/replay/learner/checkpoint lineage. | A dedicated metrics-lineage contract validates finite losses, counters, refs, replay/sample/search provenance, checkpoint resume state, and non-claims. |
| D-049 | 2026-05-30 | Keep normal-death learner sampling bounded; do not use sample-all as the proof default. | The first larger normal-death attempt showed allocator pressure in mixed JAX/Torch rows. The passing proof uses B256 finite sampling and still consumes terminal rows. | A future bounded sample cannot reliably hit terminal rows even with terminal-aware sampling, and a documented terminal stratification strategy replaces B256. |
| D-050 | 2026-05-30 | Treat terminal-only partial-terminal candidates as legitimate compact replay samples. | Real terminal rows often have no searched post-terminal successor root. Requiring full successor chains excluded the very deaths the contract needed to prove. | A future search/service design explicitly searches terminal reset successors and proves equivalent no-bootstrap terminal targets without terminal-tail padding. |
| D-051 | 2026-05-30 | Close death/terminal and move the active P0 lane to policy refresh plus metrics lineage. | Durable normal-death profile `optimizer-normal-death-compact-owned-profile-20260530` and checkpoint smoke `optimizer-compact-owned-normal-death-checkpoint-from-profile-20260530` pass the death contract and keep promotion false. D-052 subsequently closes the local metrics-lineage contract, leaving policy refresh as the active implementation blocker. | A regression in death metadata, terminal final observations, terminal target mode, learner done rows, or checkpoint compatibility invalidates the durable artifact. |
| D-052 | 2026-05-30 | Close training metrics locally, but do not promote. | The new `curvyzero_compact_training_metrics_lineage/v1` contract flips the checkpoint gate only with finite learner losses, trainer counters, replay/sample/search provenance, loop counters, refs, evidence, and non-claims; arbitrary metrics remain partial-only. | A current-chain checkpoint smoke or policy-refresh integration shows the lineage contract cannot bind real profile/trainer state without weakening its invariants. |
| D-053 | 2026-05-30 | Close policy-refresh locally, but do not promote. | The new `curvyzero_compact_policy_refresh_handoff/v1` contract flips the checkpoint gate only when a distinct search worker refreshes to the learner model digest, clears caches, and stamps matching policy/model/digest/update refs through root/action/replay/sample metadata with evidence and non-claims. | A current-chain checkpoint/eval smoke shows the handoff cannot bind real trainer/search state without weakening distinct-worker, cache-clear, or row-stamp invariants. |
| D-054 | 2026-05-30 | Strict-load and loader-only evidence are not full eval/GIF/tournament loadability. | Archimedes/Mendel/Sagan audits showed the old verifier/bundle language could be overread: strict stock model load proves only model load, construct-only tournament loader proves only construction, and the old 2026-05-28 one-game/GIF smoke belongs to its exact bundled export. | A real current-chain artifact must validate compact checkpoint, stock export, sidecar, verifier, loader, evidence bundle, gameplay/GIF artifacts, and standalone eval under `curvyzero_compact_current_chain_eval_gif_tournament_load_evidence/v1`. |
| D-055 | 2026-05-30 | Close current-chain eval/GIF locally, then unify lifecycle gates before promotion. | The new smoke builds a LightZero-shaped compact checkpoint and validates stock export, strict load, tournament loader, GIF/frames, standalone eval, and Coach evidence ref under the current-chain contract, while keeping all speed/promotion/live-run claims false. | The next P0 artifact is a unified compact checkpoint lifecycle that binds reward/RND, normal death, policy refresh, metrics lineage, and current-chain eval/GIF to the same candidate; do not spend more time on historical 2026-05-28 eval/GIF attachments unless auditing them. |
| D-056 | 2026-05-30 | Close the unified lifecycle locally; make `coach_speed_row` the next P0 blocker. | `optimizer-compact-unified-lifecycle-smoke-20260530/unified_lifecycle_report.json` binds reward/RND, normal death, policy-refresh handoff, training-metrics lineage, and current-chain eval/GIF/tournament evidence to one LightZero-shaped compact checkpoint/report chain. The compatibility report has `lifecycle_gates_complete=true`, `missing_required_gates=[coach_speed_row]`, and `promotion_eligible=false`. | A real Coach-comparable compact speed row with `compact_coach_speed_row:` evidence lands, or the lifecycle smoke is invalidated by a stricter requirement that must be added before speed-row selection. |
| D-057 | 2026-05-30 | Lifecycle completeness is not promotion eligibility without a real Coach speed row. | Nietzsche's audit showed the old schema would compute `promotion_eligible=true` when all local lifecycle gates were true, even with no Coach speed/training row. The unified smoke now passes all local lifecycle gates and correctly leaves only `coach_speed_row` missing. | A real Coach speed/training row supplies structured `curvyzero_compact_coach_speed_row_evidence/v1` evidence for the same unified lifecycle candidate; then local compatibility eligibility may become true, but actual promotion remains blocked on a separate readiness decision. |
| D-058 | 2026-05-30 | `coach_speed_row` must be structured hash-bound evidence, not a prefixed string. | Hooke/Confucius/Peirce audits showed the new gate would otherwise be easy to fake or blur with profile-only active-root rows. The compact-owned promotion gate should reject fake refs, profile-only rows/currencies, fallback denominators, row/result drift, and tampered hashes. | A real non-profile Coach-comparable compact speed/training row validates under `curvyzero_compact_coach_speed_row_evidence/v1`; a separate profile-only support artifact may be added later, but it must not close this gate. |
| D-059 | 2026-05-30 | Do not flip profile labels; create a sibling speed-row producer. | Follow-up audits found the existing hybrid profile manifest path is intentionally profile-only and hardcoded to `calls_train_muzero=false`; relabeling it would recreate the old cross-currency confusion. | A future runner literally performs the required compact trainer work and emits speed-row-specific schemas with non-profile row/result labels, producer provenance, and denominator binding. |
| D-060 | 2026-05-30 | Treat the local CPU speed-row smoke as support evidence, not the final gate. | `optimizer-compact-coach-speed-row-smoke-20260530` validates the schema, evidence, provenance, and denominator arithmetic locally, but it runs tiny CPU settings and reports only `9.2456` physical env rows/sec. | A durable remote/H100 compact-owned row of the same schema shape lands and is judged against the threshold required for Coach compatibility refresh. |
| D-061 | 2026-05-30 | Do not close `coach_speed_row` unless the measured row proves loaded checkpoint identity. | Aristotle's audit showed a candidate id can be copied into an unrelated or freshly initialized speed row. The local CPU smoke now records lifecycle checkpoint identity but remains `candidate_named_support_only`, so it cannot close Coach compatibility. | The remote/H100 row proves `model_identity.scope=candidate_loaded_checkpoint` with loaded result identity matching the unified lifecycle checkpoint id, trainer id, policy/model refs, policy source, checkpoint path/hash when available, and model-state digest. |
| D-062 | 2026-05-30 | Treat the H100 B128/A8 loaded-checkpoint speed row as the current `coach_speed_row` candidate, pending threshold review. | `optimizer-compact-coach-speed-row-h100-b128a8-20260530` validated under `curvyzero_compact_coach_speed_row_evidence/v1`, loaded the unified lifecycle checkpoint, emitted `model_identity.scope=candidate_loaded_checkpoint`, reported `1099.973468682903` `compact_trainer_env_steps_per_sec`, and kept stock-resume/live-run/rating/promotion claims false. | If the row is judged too smoke-sized, rerun the same dedicated producer at a larger/longer H100 shape before setting `coach_speed_row=true`; if accepted, refresh compatibility with the structured evidence ref. |
| D-063 | 2026-05-30 | Require a threshold-sized H100 row before setting `coach_speed_row=true`. | The B128 row was structurally valid but smoke-sized. The B512 and B1024/60 rows passed, and B1024/120 was still under the conservative 10s measured-wall floor. The B1024/A16/180 row passed with `184320.0` env steps over `18.397932468s`. | If future speed-row gates use a different threshold, record the minimum env steps, measured wall time, speed floor, and reason before attaching compatibility evidence. |
| D-064 | 2026-05-30 | Attach the accepted B1024/A16/180 H100 row as `coach_speed_row=true`, but keep `promotion_claim=false`. | `optimizer-compact-coach-compatibility-after-speed-row-h100-threshold-20260530` validates the accepted speed evidence, sets `coach_speed_row_gate=true`, has no missing current local compatibility gates/evidence, and computes `promotion_eligible=true` under local compatibility metadata. Its non-claims keep training speedup, live-run safety, stock resume, rating/promotion quality, and actual promotion false. | A stricter compatibility model adds new required gates, or post-compatibility promotion-readiness review decides which live/quality/resume/rating evidence must be gathered before any real promotion claim. |
| D-065 | 2026-05-30 | Block `promotion_claim=true` until post-compatibility readiness evidence exists. | Faraday/Franklin/Gauss/Beauvoir audits showed local compatibility eligibility could otherwise be overread or rebuilt as a claim. Added `COMPACT_PROMOTION_READINESS_GATE_2026-05-30.md` and code guard: incomplete claims still report missing local gates/evidence, completed local compatibility still raises `post_compatibility_promotion_readiness_required`, and direct `CompactCoachCompatibilityReportV1(..., promotion_claim=True)` construction fails. | A real promotion-readiness bundle lands with matched learning quality, stock resume/load, live safety, sandbox rating/assignment, and longer-horizon metric evidence, and the builder is explicitly extended to validate that evidence. |
| D-066 | 2026-05-30 | Make stock resume/load canary the first readiness producer, but keep the claim narrow. | Darwin recommended proving stock-shaped resume/load selection before claiming anything broader. The canary now reloads the compact checkpoint, re-exports `train/lightzero_exp/ckpt/iteration_0.pth.tar`, proves LightZero iteration discovery selects it, records the `load_ckpt_before_run` patch, strict-loads `state_key=model`, and constructs the tournament loader. | A later stock bridge proves full stock training resume, including optimizer/replay/trainer state restoration. Until then, this canary remains metadata/load/selection evidence only. |
| D-067 | 2026-05-30 | Make matched learning-quality canary the next P0 readiness lane. | Kuhn audited the old matched denominator pair and found it is speed/profile evidence, not quality evidence: profile-only, differing currencies, no eval movement, and no stock `train_muzero` quality curve. | Required a two-arm quality-movement artifact with fixed eval seeds, matching reward/death/noise/RND/horizon settings, visible stock/compact denominators, and non-claims preserved. Later closed by `optimizer-compact-matched-learning-quality-canary-current-env16train2-20260530`. |
| D-068 | 2026-05-30 | Land the matched learning-quality contract before producing the row. | The subagent audit found that the main risk was fake quality proof: reusing the old matched speed/profile pair, treating the H100 speed row as quality, flipping route labels, or comparing mismatched seeds/settings. | Added a local contract-only validator and tests. The later 1024x8 two-arm artifact validates under that contract; the contract alone remains structural evidence, not promotion readiness. |
| D-069 | 2026-05-30 | Make the builder capture-derived, not arm-file-derived. | Pasteur's audit called out that accepting finished `stock_reference_arm` and `compact_candidate_arm` JSON as the public path would keep the easy-overclaim surface open. Raw capture inputs force route/work/non-claim checks before arms are derived. | Later real producers emitted raw `stock_reference_capture` and `compact_candidate_capture` files from actual stock `train_muzero` and compact-owned training/eval work; the builder derived arms and validated the 1024x8 report. |
| D-070 | 2026-05-30 | Separate support-only capture previews from builder-eligible real captures. | Parfit/Ampere/Kierkegaard audits found existing artifacts can identify ingredients but cannot prove matched learning quality without real training plus matched pre/post eval. A no-training extractor must not emit the real capture schema or builder input filenames. | Later stock and compact producers ran training and matched pre/post eval, then emitted provenance-backed `curvyzero_compact_matched_learning_quality_capture/v1` files. Source inventory remains preview-only with `feeds_builder=false`. |
| D-071 | 2026-05-30 | Bind matched-quality report arms back to raw capture files. | Boole's audit showed provenance strings and in-memory arms were still too self-attested. A final report should not remain valid if the raw capture file drifts, and provenance refs must point at hashed training/eval/checkpoint artifacts. | The 1024x8 producers emitted real stock/compact capture files with required artifact kinds. The canary builder hashes those files, validation re-derives arms from them, and any future producer that cannot supply those artifacts stays preview/support-only. |
| D-072 | 2026-05-30 | Add a strict capture-from-artifacts packager, but do not call it the producer. | Rawls/Godel/Sartre audits showed the useful immediate slice is packaging already-produced training/eval/checkpoint artifacts into the real capture schema while keeping upstream real stock and compact producers separate. Calling this script a producer would launder fixture or support JSON into OPT-051. | Later upstream stock and compact producers landed. Stock ran actual `train_muzero` plus matched pre/post eval; compact loaded the unified lifecycle checkpoint, ran actual compact-owned updates, exported/evaled pre/post checkpoints, and computed compact denominators. The packager emitted builder inputs from those artifacts. |
| D-073 | 2026-05-30 | Land a compact-candidate local scaffold, but do not treat capped/no-movement output as a capture. | Kant/Dirac audits showed the smallest compact-side implementation can reuse the unified lifecycle checkpoint, compact-owned trainer, stock export, standalone eval, and strict packager. The real local attempt proved training/export/eval plumbing but both pre/post evals capped at 200, so the packager correctly rejected the capture for no observed quality movement. | A stronger compact producer consumes real compact-owned env/search/replay rows or another explicitly stronger source and produces nonzero matched eval movement under fixed settings. The stock arm still needs Modal `mode=train` orchestration around real `train_muzero` and immutable checkpoint evals. |
| D-074 | 2026-05-30 | Make the stock arm a Modal orchestration/materialization producer, not a local fake producer. | Zeno/Anscombe audits showed the stock side should launch real stock `mode=train`, select immutable checkpoints, run exact checkpoint evals, download/checkpoint-bind artifacts, and derive denominators from learner metrics and target-audit replay counts before calling the strict packager. | Run `scripts/build_compact_matched_learning_quality_stock_reference_producer.py` against real Modal evidence. If stock does not emit `iteration_0.pth.tar`, or learner metrics/replay samples/eval movement are absent, keep the failure as evidence and patch the Modal train path rather than weakening the producer. |
