{
  "schema": "curvyzero_fidelity_batch/v1",
  "description": "Tiny source body metadata batch for AvatarBody old-age death-event metadata. Kept separate from source_body_canary_batch.json so existing vector/default benchmark fixture counts stay unchanged.",
  "scenarios": [
    "scenarios/environment/source_body_old_opponent_overlap_kills_step.json"
  ]
}
